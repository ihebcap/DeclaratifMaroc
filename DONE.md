# DONE — Module Déclaration TVA (GRF)

| Task | Objet | Validé le |
|---|---|---|
| TASK-028 | **Verrou d'intégration déclaration** (tampon `RT_AFFECTATION.DT_Id` + triggers d'immuabilité, portée globale GRFN inclus). Livrables : `003_Verrou_DT_Id.sql` (index non filtré `(MV_Id, DT_Id)` + `TR_RT_AFFECTATION_Immuabilite` : DELETE et UPDATE financier — `AF_Montant`/`MV_Id`/`EC_Id`/`AF_Date` — refusés si `DT_Id NOT NULL`, transitions `DT_Id` val→NULL et NULL→val autorisées ; `TR_RT_MOUVEMENT_Immuabilite` : `MV_Point 1→0` et `MV_Compta 1→0` refusés si ≥1 affectation déclarée), triggers **set-based** + `THROW 50028` avec n° déclaration ; côté module `TamponnerAffectationsAsync`/`DetamponnerAffectationsAsync` (`RT_*` uniquement, batch 1 000, `GrfConnection`). **Re-soumis après REJET PO** : le trou d'intégrité **silencieux** (collision de `dtId = Math.Abs(Guid.GetHashCode())` + détamponnage non borné `WHERE DT_Id=@dtId` libérant le verrou d'une AUTRE déclaration) est fermé — `DeriveDtId = GetHashCode() & int.MaxValue` (plus d'`OverflowException`) et détamponnage **borné aux `MV_Numero` de la déclaration** (miroir de la pose). Preuves : 10 scénarios sur base isolée `GRF_TASK028_TEST` en `QUOTED_IDENTIFIER OFF` (pire cas legacy) tous ✅, + **preuve de collision `GRF_TASK028_COLL`** (réouverture A → A libérées, B toujours verrouillées ; contre-preuve ancien SQL → B tombe à 0). C1 (SET options anti-msg 1934) et C2 (index filtré→ordinaire pour ne pas casser les writers GRFN/Sage en QI OFF) appliqués. `AF_Taux` confirmé absent de `RT_AFFECTATION`. Build 0 erreur, tests non-régression verts (Core 28, Selection 14, Orchestration 28, Export XML 4, Excel 1, Controle 1). **Vérifié en source par l'architecte** (`DeriveDtId` ligne 337, détamponnage borné lignes 328-333). Débloque l'immuabilité exigée par TASK-024. | 2026-07-09 |
| TASK-026 | Réparer `Declaration.Core.Tests` + intégrer les projets de tests au `.slnx` (dette révélée par la revue TASK-022) : migration des lambdas `(id, sens)` → `Func<AffectationADeclarer, DocumentTaxesInfo?>` (CS1593 résorbé), ajout des **5 projets de tests hors solution** au `.slnx` (`Core.Tests`, `Orchestration.Tests`, `Selection.Tests`, `Export.Excel.Tests`, `Export.Xml.Tests` → 6 au total avec `Controle.Tests`) pour que « build/test solution » soit exhaustif, et **3 tests du chemin FGR** de `ConstructeurDeclaration` (`FgrValidationException`→`ECART_FGR`+document partiel ventilé ; `InvalidOperationException`→`CODE_TAXE_INCONNU`+aucune ventilation ; `EC_Type=4`→`SOLDE_INITIAL_NON_GERE` sans appel à `resoudreFacture`). Périmètre strict tenu (tests + `.slnx`, aucun code de production modifié). **Rejoué par l'architecte** : `dotnet build DeclarationTVA.slnx` = 0 erreur, `dotnet test` (hors `Controle.Tests` DB, filtrable comme prévu par la TASK) = **75 réussites / 0 échec** (Core 28, Orchestration 28, Selection 14, Export.Xml 4, Export.Excel 1), code source ↔ assertions vérifiés. | 2026-07-09 |
| TASK-025 | Cadrage TVA du solde initial (`EC_Type=4`) : inventaire réel sur `GR_EMA_DISTRIBUTION` — **34 lignes / 854 793,29 MAD**, 2 sous-types (33 coffres fin d'exercice `S-COF…` = à-nouveaux de trésorerie client + 1 solde initial pur `SI- 4411ELEC` = frais de retard fournisseur). **0 TVA déclarable** prouvée sur données réelles (`RT_HISTCOMPTA` vide pour les 34, `MV_Tva=0`/`MV_TvaMontant=0` sur mouvement lié, comptes trésorerie/client jamais TVA) ; **0 occurrence dans les déclarations TVA passées** (`RT_LigneDeclarationTva`). **Décision PO : maintenir l'alerte (statu quo)** — l'alerte explicite `SOLDE_INITIAL_NON_GERE` (TASK-022) reste active pour tout `EC_Type=4`, aucune exclusion, aucune implémentation (règle n°1 : aucune ligne silencieuse). Investigation lecture seule, requêtes SQL reproductibles livrées. | 2026-07-09 |
| TASK-024 | Cache des ventilations Sage (`EC_Type=0`) : table **SQL Server exclusive** `GRC_VENTILATION_SAGE_CACHE` (clé `(EC_Id, Taux)`, buckets par taux + token de paiement `(MV_Id, MV_Point)` + audit `DateLecture`/`Source`). Écriture à la 1re lecture OM, relecture 100 % SQL. **Fraîcheur = paiement** : validation à la lecture (`RT_AFFECTATION`/`RT_MOUVEMENT`, `MV_Point=1`) — token divergent/absent → cache non servi, OM rappelé (aucun code GRFN modifié, aucun trigger de purge). `VentilationSageCacheRepository` **SQL Server exclusif via MERGE** (fallback SQLite + `Data Source=tva.db` **retiré** après rejet architecte). Preuves : 8 tests unitaires orchestrateur (stub in-memory) + **IT1-IT6 d'intégration réels sur `.\sql2022`/`GR_EMA_DISTRIBUTION`** (DDL, MERGE idempotent, 2e lecture 0 OM, dépointage→OM, égalité cache↔OM), EC_Id ≥ 900000 isolés + teardown. Build 0 erreur. | 2026-07-09 |
| TASK-009 | Mode Contrôle (`Declaration.Controle`) : recalcul corrigé vs résultat GRFN stocké (`RT_LigneDeclarationTva`), aligné par clé `(NumeroFacture \| Taux \| NumeroRapprochement)`, tolérance 0,05. **Comparaison réelle sur déclaration EMA `DT_Id=66` (06/2026, `GR_EMA_DISTRIBUTION`/`SO_Id=1`, lecture seule stricte)** : 4 249 lignes GRFN, 3 173 concordants (99,6 % sur taux>0), 14 `ManquantRecalcul` (bug legacy `DT_Id` posé partiellement sur mouvement multi-factures, chiffrés/causés), 95 `ManquantGRFN` (sauts silencieux ancien module : « Solde=Montant », chèques non rapprochés, espèces hors période — Σ\|AF_Montant\|=475 811,82 MAD, ventilés par cause). Corrections `SelectionnerAffectationsService` (JOIN `RT_ECHEANCE`→`E.DO_Numero` + filtre date conditionné à `dtId`) vérifiées en source ; `GrfnDeclarationRepository` `SELECT` seul ; build 0 erreur. Réconciliation §2/§3.2/§5 (Σ\|AF\| vs Σ algébrique, écart 80 028,30 documenté) corrigée avant approbation. | 2026-07-09 |
| TASK-019 | Poste de travail 4 interrogations (Rapprochement · Affectation · Conformité IF/ICE · Factures à déclarer, 2 faces) — front sur API réelle (`GR_EMA_DISTRIBUTION`/`SO_Id=1`), supersède TASK-018. Réconciliation 5 segments `total = candidates = intégrées+exclues+reportées+écartées+proposées` (règle TASK-013, aucune ligne silencieuse), 3 vues de preuves différenciées (colonnes par domaine + prop `domaine`), actions de masse (Exclure/Réintégrer/Forcer report), clôture verrouillée tant qu'il reste des lignes à traiter. 5 captures e2e Playwright (1 passed), build tsc+vite + oxlint 0 erreur. Bug TVA 0% diagnostiqué = correctif backend Gap A extrait et tracé sous **TASK-030**. | 2026-07-09 |
| TASK-030 | Correction Gap A partiel (Ventilateur et FGR) : lignes avec TVA = 0% et TTC = 0,00 signalées au test E2E de TASK-019, corrigées dans le backend. Filtre `taxe.Type` de `Ventilateur.cs` trop restrictif → excluait silencieusement les taxes FGR (Type `""`) et OM (enum résolu `"0"`). Ajout du type explicite `TaxeTypeTVA` dans `LecteurTvaFgr.cs` + assouplissement du filtre (`"0"`/`""` acceptés). Preuves réelles `GR_EMA_DISTRIBUTION` (taux 20, tva 75,3, ttc 451,8). | 2026-07-09 |
| TASK-021 | Avenant back (Gap B) : sélection des reportées (non-rapprochées `MV_Point≠Oui`, datées `MV_Date`) marquées `Reportee` + motif « Règlement non rapproché » ; garde-fous universels priment sur `NonRapproche` (motif honnête) ; volume réel 304 reportées / 1309 candidats, réconciliation bouclée. Tests 13/13 hors DB. | 2026-07-08 |
| TASK-002 | Worker autonome `SageTaxReader` : lecture facture achat/vente + extraction taxes par taux via Objets Métier Sage (COM) | 2026-07-08 |
| TASK-003 | Nombre de décimales d'arrondi (`n`) lu dynamiquement depuis la devise société via OM (`IBPDossier2.DeviseCompte` → `IBPDevise2.D_Format`) | 2026-07-08 |
| TASK-004 | Ventilation / proratisation : cœur de calcul pur (`Declaration.Core.Ventiler`) — DTO neutres extraits dans `SageTaxReader.Contracts`, calcul `decimal`, prorata direct + `AwayFromZero` | 2026-07-08 |
| TASK-005 | Modèle déclaration + contrôle : `ConstruireDeclaration(...)` pur (`Declaration.Core`, net10.0) — lignes enrichies, récaps source/taux/activité, contrôle d'équilibre à écart nul, 6 familles d'alertes | 2026-07-08 |
| TASK-006 | Corrections revue TASK-004/005 : contrôle d'équilibre réel (résidu non-TVA décomposé affecté-vs-déclaré, plus de tautologie), alerte `TIERS_SANS_IF` symétrique de `TIERS_SANS_ICE`, `Prorata`+`Designation` portés sur `LigneDeclarationEnrichie`, test blanc `Any(char.IsWhiteSpace)` | 2026-07-08 |
| TASK-001 | Vérification synchro rapprochement GRF↔Sage sur base prod `GR_EMA_DISTRIBUTION` : source locale `MV_Point`/`MV_PointDate` prouvée suffisante (352/352 sync, dates identiques, 1008/1008 déclarations historiques hors espèces) → option B validée, module fondé sur le local uniquement | 2026-07-08 |
| TASK-007 | Orchestration + cache : `OrchestrateurDeclaration` (`Declaration.Orchestration`, net10.0 pur) relie worker OM out-of-process ↔ ventilation ↔ `ConstruireDeclaration` ; cache `(facture, sens)` (1 lecture/facture prouvée), invocateur process séparé avec timeout worker robuste (lecture async + WaitForExit borné + Kill), erreur/timeout → alerte sans crash | 2026-07-08 |
| TASK-008 | Sélection SQL locale (GRF) : adaptateur `Declaration.Selection`, `SelectionnerAffectationsService` paramétré, domaines et requêtes testées sur `GR_EMA_DISTRIBUTION` | 2026-07-08 |
| TASK-014 | Cadrage mapping domaines → formulaires & schémas XML DGI (Simpl-TVA) : regroupement décaissement/dépense/frais dans Relevé de déductions, schéma CA introuvable (escalade PO) | 2026-07-08 |
| TASK-015 | Sélection expliquée (surensemble + motifs) : brique `SelectionExpliqueeService` abolissant les sauts silencieux. Code en mémoire (évaluateur) et requêtes validés par test d'intégration stricte vs TASK-008. | 2026-07-08 |
| TASK-013 | Front web `declaration-tva-web` (React 19/Vite/TS) : workflow par domaine (stepper création→intégration→checkup→clôture→génération) sur mock serveur. Grilles paginées/filtrées/triées côté serveur + virtualisation, statut système `Écartée`+motif filtrable (Règle n°1), checkup avec réconciliation/équilibre/récaps (source/taux/activité) + gating clôture, génération multi-artefacts. Build tsc+vite OK, e2e Playwright vert. | 2026-07-08 |
| TASK-010 | Export Excel (artefact dépôt/archive) : adaptateur `Declaration.Export.Excel` (`net10.0` pur, ClosedXML, sans COM Office ni Sage/SQL) sérialisant `DeclarationModele` → `.xlsx` 2 feuilles (Détail = 1 ligne/enrichie ; Récap = totaux source/taux/activité + contrôle d'équilibre/résidu + alertes). Test de génération sur fixture (1/1 vert), .xlsx d'exemple conforme (2 feuilles Détail+Récap). | 2026-07-08 |
| TASK-011 | Export XML Simpl-TVA + zip : adaptateur `Declaration.Export.Xml` (`net10.0` pur) sérialisant `DeclarationModele` → XML zippé (format dépôt DGI). Format reconstitué via rétro-ingénierie stricte sans dépendance GRFN (`<ord>`, `<mht>`, `.`, mapping paiement 1-7). Validation Maroc bloquante (IF 8, ICE 15 sans espaces). Aucun re-arrondi `ToEven`. Modèle enrichi (`EnTeteDeclaration`, `IsReport`). | 2026-07-08 |
| TASK-016 | Ajustements UI : Drill-down depuis le checkup vers la grille filtrée avec maintien du contexte. Refonte de l'action de masse sur filtre (mockServer, UI, contrat API mis à jour dans TASK-012). Tests e2e Playwright ajoutés. Alimentation du récapitulatif par code d'activité branché sur `F_COMPTET` via SQL. | 2026-07-08 |
| TASK-012 | API TVA (`Declaration.API`, Clean Architecture alignée GRC_WEB, JWT Bearer, Dapper) : persistance déclaration isolée (SQLite `tva.db`, écritures limitées à `DeclarationEntete`/`LigneCandidate`, zéro `RT_*`), numérotation `TVA{Societe}-{Exercice}-{Periode}` + unicité 409, endpoints paginés/filtrés/triés côté serveur, état par ligne persisté (unitaire + bulk par ids ou domaine/filtre). **Transparence (règle n°1)** prouvée via `FixtureSelectionExpliqueeService` (2 éligibles Proposée + 3 écartées avec `MotifRejet` explicite, aucune ligne silencieuse). Checkup réel (`GetCheckupAsync`, non mock) exposant les exclues en alerte Info + équilibre ; clôture gardée (alerte Error `AUCUNE_LIGNE_INTEGREE` → 400, preuve négative). Injection SQL `ORDER BY` corrigée (whitelist). Génération/fichiers (TASK-010/011) et contrôle (TASK-009) honnêtement délégués en 501. Captures réelles vérifiées ligne à ligne contre le code source. | 2026-07-08 |
| TASK-017 | Câblage figeage API sur prod réelle + montants réels (avenant TASK-012) : `DbConnectionFactory` lit la config (multi-client, aucune chaîne en dur), vraie chaîne GRF transmise au workflow, bascule Production → sélection réelle sur `GR_EMA_DISTRIBUTION` (`SO_Id=1`). Étape A : `GET /lignes` remonte les vraies candidates, écartées avec motif visible (dont échec worker → `Exclue`). Étape B : forfait 20 % **retiré**, HT/Taux/TVA/TTC issus du worker OM out-of-process + ventilation (cas taux réel 10 % prouvé), comparaison **source (worker) ↔ figé (SQLite)** strictement identique sur échantillon. Checkup à résidu 0 / clôture OK sur déclaration non vide, isolation confirmée (aucune écriture `RT_*`). | 2026-07-08 |
| TASK-022 | Routing par `EC_Type` + lecteur TVA FGR en SQL : `AffectationADeclarer` enrichi (`EC_Type`/`EC_Id`), dispatcher orchestrateur (111→FGR SQL `RT_HISTCOMPTA`, 0→OM inchangé, 4→alerte `SOLDE_INITIAL_NON_GERE`), `LecteurTvaFgr` (reconstruction par `HC_Indice`, contrôle Σ(HT+TVA)=TTC → `FgrValidationException` avec document partiel, aucun drop silencieux). **Taux résolu via `F_TAXE.TA_Taux` (connexion Sage), sans parsing de `HC_TaxeCode`** ; code absent de `F_TAXE` → alerte `CODE_TAXE_INCONNU`. Deux connexions (GRF `RT_HISTCOMPTA` + Sage `F_TAXE`) propagées. Build solution 0 erreur (vérifié par l'architecte), Orchestration.Tests 12/12 (7 tests FGR mono/multi/exo/écart/bucket/C20/inconnu). **⚠️ Réserves : preuve réelle sur `FF260070` + mesure perf avant/après non fournies (tests synthétiques en mémoire uniquement) ; `Declaration.Core.Tests` cassé pré-existant → TASK-026.** | 2026-07-08 |
| TASK-020 | Avenant back (Gap A) — n° de règlement sur la ligne de déclaration : `NumeroRapprochement` ajouté à `LigneCandidate`, renseigné depuis `Affectation.NumeroRapprochement` (=`M.MV_Numero`, déjà réel via sélection TASK-017) dans les **3 branches** de `MapLignesCandidates` (non-éligible / éligible sans taxes / éligible avec taxes), persisté (`INSERT`) et relu (`SELECT *`/Dapper, `NULL`→`""` pour l'historique) dans `DeclarationRepository`, schéma `DeclarationTVA.sql` mis à jour, exposé dans `GET /declarations/{id}/lignes` → regroupement par règlement possible côté front. Périmètre étroit respecté (pas de reshaping, pas de modif sélection/éligibilité). Conformité IF/ICE (§2) **reportée en 2ᵉ vague** (décision PO 08/07). Test unitaire `MapLignesCandidates_ConserveNumeroRapprochement_PourToutesLesBranches` (3 branches, même `REG-001` sur 4 lignes) ; build + `Declaration.Orchestration.Tests` **13/13** rejoués par l'architecte. **⚠️ Réserve : preuve de regroupement fournie sur données mockées, pas sur `GR_EMA_DISTRIBUTION` — la branche éligible-avec-taxes n'est prouvée qu'au niveau test unitaire ; source `MV_Numero` déjà prouvée réelle en TASK-017.** | 2026-07-08 |
| TASK-027 | Conformité IF/ICE calculée au DTO + extraction du validateur TASK-011 : règle IF/ICE extraite dans `Declaration.Core/ValidationIdentiteFiscale.cs` (source unique — `EstIfValide`/`EstIceValide`/`Evaluer`/`ValiderPourExport`), exporter XML rebranché sans régression (messages d'exception conservés, `Export.Xml.Tests` inchangés), `LigneCandidate.Conformite` (getter lecture seule → `EtatConformite`) exposé par ligne dans `GET /lignes`. Build 0 erreur, `Core.Tests` **25/25** + `Export.Xml.Tests` **4/4** (rejoués par l'architecte). Réserves non bloquantes : warnings `xUnit1012`/`CS8602` pré-existant, `Core.Tests` hors `.slnx` (TASK-026). | 2026-07-08 |
| TASK-018 | Refonte Checkup en hub « tour de contrôle » (front `declaration-tva-web`, mock-first) : le tunnel linéaire est remplacé par un hub d'accueil (`DeclarationStepper` affiche `CheckupPanel` par défaut, stepper linéaire retiré). Héros = preuve de transparence : équilibre `Σ sources = Σ candidates` + écart (badge vert à 0), **barre de ventilation 5 segments** (Intégrées/Exclues/Reportées/Écartées/Proposées, « 0 ligne silencieuse ») avec chips cliquables → drill-down filtré (réutilise TASK-016). Tuiles source (avancement + reste-à-décider) → drill-down domaine. Bouton **Clôturer verrouillé** dans le hub avec libellé du reste-à-faire (« Clôturer · N à traiter »). Récaps masqués si vides. 6 règles de transparence TASK-013 préservées. Build `tsc -b && vite build` + `oxlint` OK, e2e Playwright vert. Avenant contrat checkup (ventilation 5 statuts) signalé à TASK-012. **⚠️ Approuvé sur mock — Gate PO résiduelle : rendu à valider sur données réelles (`GR_EMA_DISTRIBUTION`, TASK-001/017) avant mise en production.** | 2026-07-08 |
