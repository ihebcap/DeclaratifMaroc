# DONE — Module Déclaration TVA (GRF)

| Task | Objet | Validé le |
|---|---|---|
| TASK-153 | **Fix bloquant : crash non géré du Setup à l'étape prérequis .NET 4.8** (`Declaration.Setup/Services/NetFrameworkInstaller.cs`, `Declaration.Setup/SetupForm.cs`) — signalement PO (capture écran, 20/07/2026) : boîte de dialogue .NET générique d'exception non gérée pendant l'installation. Cause double : (1) URL Microsoft en dur du redistribuable offline .NET 4.8 renvoyant **404** (lien `download.microsoft.com/download/.../<GUID>` retiré/déplacé) ; (2) l'appel de téléchargement/installation dans `ValidatePrerequisitesStepAsync()` n'était entouré d'aucun `catch`, laissant l'exception remonter non gérée au lieu d'afficher le message d'échec déjà prévu. **Correctif** : URL remplacée par le mirroir stable `download.visualstudio.microsoft.com` (CDN officiel Microsoft) ; ajout d'un `catch (Exception ex)` explicite convergeant vers le même `MessageBox` d'échec que la branche `ok == false`. **Vérifié par l'architecte** : lecture directe des deux fichiers confirmant l'URL et le `catch` ; `dotnet build Declaration.Setup/Declaration.Setup.csproj` rejoué → 0 erreur ; nouvelle URL re-vérifiée en direct (`curl -I`) → HTTP 200, ~72 Mo, `Content-Type: application/octet-stream`. **Réserve non bloquante, documentée non silencieuse** : pas de rejeu réel du Setup WinForms sur poste Windows (environnement de travail ne permet pas l'exécution interactive) — couverture par lecture de code uniquement pour le chemin d'erreur. Voir `VERIFY/TASK-153_verify.md` (supprimé après approbation, contenu tracé ci-dessus) et `DONE_DETAIL/TASK-153-fix-url-morte-installateur-net48-crash-setup.md`. | 2026-07-20 |
| TASK-145 | **CRITIQUE — `Sens` Achat/Vente codé en dur dans la lecture facture-first** (`Declaration.Selection/SelectionExpliqueeService.cs`, `GetFactureFirstSql`) : aucun filtre `RT_ECHEANCE.DO_Domaine` — une échéance de VENTE (`DO_Domaine=0`) avec `EC_Type=0` pouvait être lue par cette requête achat-only puis évaluée avec `Sens=Achat` codé en dur, provoquant un appel Sage `docFactoryAchat.ExistPiece` sur une pièce de vente → fausse erreur « Facture d'achat introuvable » sur des factures réelles (ex. `FA2600619`). **Correctif** : ajout `AND E.DO_Domaine = @achatDomaine` (nouvelle constante `GrfEnums.ErpDomaine_Achat=1`), aucun changement à `SensAffectation`/`MapAndEvaluate` ni au chemin encaissement/vente existant. **Portée réelle du bug plus large que signalé en session** : 2 312 échéances vente potentiellement concernées sur toute la base ; **326 sentinelles `ERREUR` erronées** déjà écrites en cache (`DM_VENTILATION_SAGE_CACHE`) purgées par un `DELETE` ciblé (motif exact `Facture d'achat...introuvable`, jamais un flush global) — liste complète des `EC_Id` purgés dans `DONE_DETAIL/task145-purge-liste-ec-id.txt` (326 lignes). 5 lignes à motif distinct (incohérence de montants) **non purgées délibérément** (hors périmètre de ce bug). **Vérifié par l'architecte** : diff `git show dbb4e1f` confirmant exactement le filtre ajouté et la constante ; fichier de purge relu (328 lignes = en-tête+séparateur+326 données, cohérent avec le VERIFY) ; `dotnet build Declaration.Selection`/`Declaration.API` 0 erreur. **Réserve non bloquante, documentée non silencieuse** : preuve positive de la branche Vente réelle via le worker Sage COM non obtenue (crash natif `STATUS_STACK_BUFFER_OVERRUN` identique en Achat et Vente — incapacité d'invoquer le worker en standalone dans cet environnement, pas une preuve que la branche Vente est cassée) ; `EC_Id=24098`/`FA2502718` reste en erreur d'un type distinct (incohérence de montants), non concerné par ce fix. Voir `VERIFY/TASK-145_verify.md` (conservé pour traçabilité) et `DONE_DETAIL/TASK-145-fix-sens-achat-vente-code-en-dur-facture-first.md`. | 2026-07-20 |
| TASK-146 | **Drill « Voir lignes » (étape ② Vérifier & Intégrer) filtrait par règlement entier au lieu de la ligne précise en anomalie** (`Declaration.API/Controllers/DeclarationsController.cs`, `Declaration.Infrastructure/Repositories/DeclarationRepository.cs`) : le filtre transmis au front était toujours `{ numeroRapprochement }`, jamais par `EC_Id` — un règlement couvrant plusieurs factures faisait remonter TOUTES ses lignes dès qu'une seule était en anomalie (cas réel : 18 lignes retournées pour `FC2501667`/`EC_Id=20650`). **Correctif** : nouvelle clé JSON `ecId` dans `BuildLigneFilterWhere` (`AND EC_Id IN @EcIds`, paramétrée), utilisée en priorité par le contrôleur quand `EC_Id>0`, repli sur `numeroRapprochement` conservé pour les lignes historiques sans `EC_Id` (jamais retiré). **Vérifié par l'architecte** : diff confirmant l'ajout exact ; requête directe confirmant 18→1 ligne sur le cas réel cité ; ne touche pas `IDeclarationRepository` (interface partagée), donc sans impact sur le reste de la solution. Voir `VERIFY/TASK-146_verify.md` et `DONE_DETAIL/TASK-146-drill-anomalie-filtre-precis-par-ligne.md`. | 2026-07-20 |
| TASK-147 | **Recalcul d'une ligne `Proposée` dont le cache Sage a été relu avec succès après création de la déclaration** (`Declaration.Application/Services/DeclarationWorkflowService.cs`, `DiagnosticModal.tsx`) — diagnostic de staleness (compare `DM_VENTILATION_SAGE_CACHE.DateLecture` à `DM_ENTTVA.DateCreation`) + action « Recalculer cette ligne » (`RecalculerLigneDepuisCacheAsync`, reconstruit depuis le cache déjà bon, aucune nouvelle lecture OM Sage). **Rejeté une première fois (20/07/2026)** : le VERIFY initial annonçait « Build OK » en ne buildant que `Declaration.API.csproj`, jamais la solution complète — `dotnet build DeclarationTVA.slnx` échouait avec **77 erreurs CS0535** : 7 méthodes ajoutées à `IDeclarationRepository` par TASK-144/TASK-147 (`GetEcheanceDiagnosticAsync`, `GetEcheancesMemeDoNumeroAsync`, `GetMotifErreurCacheAsync`, `GetDocumentsReglementSageAsync`, `GetDerniereLectureCacheAsync`, `GetBucketsCacheAsync`, `SupprimerLignesParEcIdAsync`) jamais répercutées dans 11 classes `FakeDeclarationRepository` de `Declaration.Orchestration.Tests`. **Correction livrée** : les 7 méthodes ajoutées dans les 11 fakes concernés (stub `NotImplementedException`/`Task.CompletedTask` selon le pattern déjà en usage, aucune n'étant exercée par les tests existants), aucun autre fichier touché. **Vérifié indépendamment par l'architecte** : `dotnet build DeclarationTVA.slnx` → 0 erreur ; `dotnet test Declaration.Orchestration.Tests` → 137/137 réussis ; grep confirmant exactement 7 occurrences dans chacun des 11 fichiers modifiés, aucun fichier hors périmètre touché par ce correctif. Preuve fonctionnelle end-to-end (cas réel `EC_Id=18198`) conservée du VERIFY initial, non rejouée (non nécessaire). Voir `DONE_DETAIL/TASK-147-recalcul-ligne-proposee-cache-perime.md`. | 2026-07-20 |
| TASK-148 | **Debounce du filtre de dates (écran Factures)** (`declaration-tva-web/src/FactureInterrogation.tsx`) : `debut`/`fin` déclenchaient un fetch immédiat à chaque `onChange`, y compris en état transitoire de saisie, produisant des requêtes `400 Bad Request` intermédiaires (`debut=2026-10-29&fin=2026-07-20` observé en session). **Correctif** : `debouncedDebut`/`debouncedFin` (state, `setTimeout`/`clearTimeout` 350ms, aucune dépendance externe) alimentant les seuls effets réseau (`/factures/distincts`, `fetchPage`) — les `<input>` restent branchés sur les valeurs brutes pour la réactivité écran ; garde défensive `debut<=fin` ajoutée en complément. Bouton « Rafraîchir » explicite non touché. **Vérifié par l'architecte** : diff confirmant exactement le pattern décrit ; `tsc -b`/`vite build` 0 erreur. **Réserve non bloquante, même limite que TASK-115/120** : pas de preuve visuelle navigateur réelle dans cet environnement (licence `ApLicence` non disponible) — pattern standard à faible risque, à confirmer par le PO en conditions réelles. Voir `VERIFY/TASK-148_verify.md` et `DONE_DETAIL/TASK-148-debounce-filtre-dates-ecran-factures.md`. | 2026-07-20 |
| TASK-149 | **Ré-audit (lecture seule) des 14 lignes `FACTURE_NON_VENTILEE` de TASK-143 après le correctif TASK-145** : **8/14 résolues** (toutes portaient `DO_Domaine=0`/Vente, signature exacte du bug corrigé — aucune ligne Achat résolue par le passage du temps, confirmation forte que ce n'est pas une coïncidence), **6/14 restent de vraies anomalies de données** (4 Achat authentiques introuvables/incohérentes, 1 Vente à incohérence de montants distincte, 2 non re-vérifiables). Écarts d'équilibre recalculés (`02: −24 462,00`, `03: −20 294,92`, `04: −6 651,99`), identité arithmétique revérifiée. **⚠️ Fait signalé, hors périmètre de correction** : `TVA1-2026-01` a disparu de la base entre l'audit TASK-143 et cette session (cause non investiguée, à signaler au PO) ; `TVA1-2026-02` a été recréée/élargie (730→2005 lignes) la même nuit. Rapport : `DOCS/AUDIT-TASK-143/01-reaudit-post-145.md`. **Vérifié par l'architecte** : rapport relu ligne par ligne, cohérent avec le VERIFY et avec TASK-145/150 ; aucune écriture en base (lecture seule stricte, conforme au garde-fou). Périmètre de TASK-150 clarifié : 5 `EC_Id` (`20650, 21473, 18198, 18199, 24098`). Voir `VERIFY/TASK-149_verify.md` et `DONE_DETAIL/TASK-149-reaudit-lignes-non-valorisees-post-fix-145.md`. | 2026-07-20 |
| TASK-150 | **Traitement des 5 lignes confirmées réellement anormales par TASK-149** : diagnostic individuel (réutilise `DiagnosticModal`/`DiagnostiquerLigneAsync` TASK-144/147, lecture seule stricte) pour chacune — **conclusion : aucune ne peut être « faite aboutir » ni « exclue » sans décision métier/fiscale que le worker ne pouvait pas prendre seul** (3 incohérences de montants Sage réelles, dont `21473`/`FC2501717` avec un écart de **2 043 601,44 MAD** très suspect d'une erreur de saisie — signalé **prioritaire** au PO/service comptable ; 2 pièces réellement introuvables côté Sage OM, tiers ATLANTIC FOODS). Toutes les 5 lignes restent `Proposee`, **aucune écriture en base**, documentées comme « nécessite investigation Sage + arbitrage PO ». Conforme au garde-fou explicite de la task (« pas un échec de terminer avec des lignes ouvertes nécessitant un arbitrage humain »). Rapport : `DOCS/AUDIT-TASK-143/02-traitement-post-149.md`. **Vérifié par l'architecte** : rapport relu, décision par ligne justifiée et non inventée, 0 `UPDATE`/`DELETE` confirmé. Voir `VERIFY/TASK-150_verify.md` et `DONE_DETAIL/TASK-150-traitement-lignes-non-valorisees-restantes.md`. | 2026-07-20 |
| TASK-151 | **Cause racine de l'IF manquant du tiers ZF FOOD** (bloque l'export XML `TVA1-2026-06`) : requête directe sur la fiche tiers Sage réelle (`F_COMPTET`, `CT_Num=C0174`, base `NEW_EMA DISTRIBUTION`) confirme que la colonne `[IF]` est une **chaîne vide réelle** (`LEN=0`), pas une valeur non lue — mapping applicatif (`P_SOCIETE.SO_DecTvaColNameIdentifiantFrs`) vérifié sain par ailleurs (d'autres tiers de la même base ont un IF correctement lu via ce même mécanisme). **Verdict : donnée Sage manquante, pas un bug applicatif** — aucun code modifié côté GRF, conforme au garde-fou (« ne jamais insérer une valeur d'IF arbitraire/placeholder »). Action requise : le PO/service comptable doit compléter l'IF de ZF FOOD dans Sage. Rapport : `DOCS/AUDIT-TASK-143/03-if-manquant-zf-food.md`. **Vérifié par l'architecte** : cause racine et méthode cohérentes, lecture seule stricte confirmée. Voir `VERIFY/TASK-151_verify.md` et `DONE_DETAIL/TASK-151-if-manquant-tiers-zf-food.md`. | 2026-07-20 |
| TASK-152 | **Désambiguïsation préventive de l'affichage du tiers en cas de collision `DO_Numero`** — clôturée **sans code**, décision PO (20/07/2026) en réponse à l'arbitrage explicitement demandé par le worker (garde-fou de la task : « ne doit pas être auto-approuvée par un worker autonome »). **Option 1 retenue** : rien de systématique — le diagnostic de collision existant, exposé à la demande via `DiagnosticModal` (TASK-144), suffit. **Justification** : taux de collision quasi nul confirmé par les données réelles (1 seul cas — `FA2600106`, déjà bien géré par l'application — sur 2 460 `EC_Id` vérifiés par l'audit TASK-143, inchangé après les diagnostics individuels de TASK-150) ; un signal systématique aurait nécessité un calcul de collision cross-base Sage à chaque chargement d'écran, coût jugé disproportionné au bénéfice. Voir `VERIFY/TASK-152_verify.md` (conservé, documente les options) et `DONE_DETAIL/TASK-152-desambiguisation-tiers-collision-do-numero.md`. | 2026-07-20 |
| TASK-143 | **Audit exhaustif (lecture seule) des 6 déclarations TVA existantes contre les données réelles Sage** (`TVA1-2026-01` à `06`, société `SO_Id=1`, 5 182 lignes `DM_LGTVA`) — demande PO en réponse au diagnostic manuel `FA2600106` (collision `DO_Numero` entre tiers), en session : « je veux un tasks complet pour vérifier toutes les déclarations les factures les reglements avec les données réelles ... comme un comptable ... rapport complet pour l'analyser ». **Méthode** : logique de `GET /api/declarations/{id}/checkup` (`DeclarationsController.cs:216+`, `DeclarationWorkflowService.GetCheckupAsync` l.723+) reproduite à l'identique en SQL sur les données persistées (API vivante non atteignable sans rejouer une auth JWT hors périmètre lecture seule), plus **contrôle nouveau** : `RT_ECHEANCE` groupé par `DO_Numero`+`DO_Domaine` avec `COUNT(DISTINCT CT_No)>1` sur les 2 460 `EC_Id` référencés, verdict tranché via `F_DOCREGL` (base Sage `NEW_EMA DISTRIBUTION`, jointure `EC_No=DR_No`) pour chaque collision trouvée. **Verdict comptable : aucune des 6 déclarations n'est signable en l'état.** 14 lignes `FACTURE_NON_VENTILEE` (01:2 · 02:9 · 03:1 · 04:2, motif réel persisté dans `MotifRejet` : facture introuvable OM / incohérence Sage HT+TVA+Parafiscale≠TTC) expliquent **à 100 %** l'écart d'équilibre global (`ΣTTC−(ΣHT+ΣTVA) = −100 576,90 MAD`, identité vérifiée au MAD près) — aucune ligne silencieuse. `TVA1-2026-06` arithmétiquement équilibrée mais 3 lignes du tiers **ZF FOOD sans Identifiant Fiscal** → export XML DGI bloqué (`ValiderPourExport`). `TVA1-2026-05` seule déclaration propre sur tous les contrôles automatiques. **Collision `FA2600106` reconfirmée unique** sur l'ensemble du périmètre (pas seulement le cas initial) : `EC_Id=21849`/`CT_No=166` (LA BUVETTE DU MAROC, document Sage réel `F_DOCREGL DR_No=4947`) référencé par `TVA1-2026-02` — bon choix ; `EC_Id=18608`/`CT_No=188` orphelin (0 `F_DOCREGL`), non référencé par aucune déclaration — le module a résolu correctement la collision, l'anomalie de la ligne référencée est un échec de valorisation OM distinct, pas une confusion de tiers. Tampon `DT_Id` : `RT_AFFECTATION` (3 207 affectations) et `DM_ENTTVA` à 0 tampon posé (cohérent, 6 déclarations *En cours* jamais clôturées) — aucune anomalie. Règlements/affectations : aucun `NumeroRapprochement` sur 2 déclarations (aucune double-déclaration) ; 32 règlements sélectionnés sans ligne (advisory `REGLEMENT_EXCLU`, non bloquant, transparence). Qualité tiers : ICE 100 % conforme, IF 5 179/5 182 (seul ZF FOOD incomplet). **Vérifié par l'architecte** : arithmétique du rapport recalculée et confirmée (sommes de lignes = 5 182 et 5 165, écarts par déclaration = −100 576,90 au total, cohérents avec les 14+3 anomalies) ; cas `FA2600106` recoupé avec les requêtes SQL indépendantes exécutées manuellement plus tôt dans la session (mêmes `EC_Id`/`CT_No`/montants) ; confirmé qu'aucun fichier de code de production n'a été modifié (dates de modification des `.cs` du module toutes antérieures à l'exécution de l'audit) ; VERIFY relu point par point contre la checklist VALIDATION de la task, tous les critères couverts. **Aucune écriture en base, aucun XML généré** — diagnostic strict. **3 anomalies confirmées → TASK de correction à ouvrir séparément** (hors périmètre de cet audit) : valorisation des 14 lignes, IF ZF FOOD, désambiguïsation tiers sur collision `DO_Numero`. Voir `DOCS/AUDIT-TASK-143/00-SYNTHESE-GLOBALE.md` (+ fiches `01`…`06`), `DONE_DETAIL/TASK-143-audit-exhaustif-6-declarations-donnees-reelles.md` et `DONE_DETAIL/TASK-143_verify.md`. | 2026-07-20 |
| TASK-142 | **Drill « Lignes incohérentes (TTC ≠ HT+TVA) » sans visibilité sur l'incohérence** (`declaration-tva-web/src/VerifierIntegrerPanel.tsx`, `declaration-tva-web/src/DomainGrid.tsx`) : signalement PO (capture écran ② Vérifier & Intégrer, `TVA1-2026-02`, drill 556 résultats) — « où se trouve l'incohérence ? ça manque de visibilité ». Diagnostic : le drill compare littéralement `TTC` à `HT+TVA` (`handleDrillIncoherence`) mais la grille affichée (`<DomainGrid>` sans prop `columns` → repli `defaultColumns`) n'affichait pas `Montant TVA`, 2ᵉ opérande de l'égalité — pourtant déjà exposé par l'API (`LigneCandidateDto.MontantTVA`), pur défaut d'affichage. **Correctif front-only** : nouvelle constante `incoherenceColumns` (module `VerifierIntegrerPanel.tsx`) reprenant `defaultColumns` + `Montant TVA` inséré entre `Taux TVA`/`Montant TTC` + colonne dérivée `Écart (HT+TVA−TTC)` (`derived: true`), passée via une prop `columns` dédiée au seul site d'appel du drill incohérence (`kind === 'incoherence'`), avec une clé de persistance de colonnes propre (`colsStorageKey='grf.cols.domain.incoherence'` — évite que la nouvelle colonne hérite d'une préférence de la grille par défaut qui ne la connaît pas et la masquerait). Côté `DomainGrid.tsx` : `montantTVA`/`ecart` ajoutés à `isNumericCol` et à la liste des colonnes formatées en monnaie (`renderCell`, `montantTVA` s'affichait auparavant en brut) ; colonne `derived` rendue non triable/non filtrable (aucune clé inconnue envoyée au filtre/tri serveur) ; ligne surlignée en rouge (`--status-blocking-bg`/`--status-blocking-text`, déjà variabilisés TASK-121) dès que `|écart| > 0,005`. **Vérifié par l'architecte** : lecture source des 2 fichiers confirmant chaque affirmation du VERIFY (ordre des colonnes, clé de persistance dédiée, garde `derived` sur tri/filtre/back, calcul `ligneEcart`) ; `npm run build` (`tsc -b && vite build`) rejoué → 0 erreur (même warning `INEFFECTIVE_DYNAMIC_IMPORT` préexistant, sans lien) ; `useColumnPrefs` relu, confirmant qu'une clé de stockage neuve (`localStorage` vide) rend bien toutes les colonnes visibles par défaut, sans action de l'utilisateur ; non-régression confirmée par lecture (① Sélection, Workstation, ProofModal, DeclarationFinalePanel ne passent aucune prop `columns`/`colsStorageKey`, `defaultColumns` de `DomainGrid` non modifié). Voir `DONE_DETAIL/TASK-142-drill-incoherence-montant-tva-ecart-invisibles.md` et `DONE_DETAIL/TASK-142_verify.md`. | 2026-07-20 |
| TASK-141 | **Badge « À contrôler » (écran ① Sélection) sans motif visible** (`declaration-tva-web/src/ReglementsSelection.tsx`) — signalement PO sur capture `TVA1-2026-01` (2 lignes « À contrôler » sans explication). Diagnostic : le motif unique de ce statut (reste à affecter ≠ 0) existait déjà en donnée (`resteAAffecter`) mais n'était jamais restitué, contrairement aux 4 sous-motifs du badge « Bloqué » ; aggravé par l'arrondi entier de la colonne « Affecté » pouvant afficher « partiel 100 % » avec un reste non nul. **Correctif front-only** : tooltip `title` sur le badge (`Reste à affecter : X MAD`) + libellé inline dans « Affecté » (`partiel X % (reste X MAD)`) — `statutDe`, les 4 sous-motifs `bloque` et le calcul `pct` inchangés. **Rejet 1er passage** : `MAD` littéral ajouté en double du suffixe déjà porté par `formatMoney()` (`utils.tsx:24-26`), donnant « MAD MAD » — corrigé au 2e passage (suffixe porté une seule fois), `tsc --noEmit` revérifié 0 erreur par l'architecte. **Réserve non bloquante assumée** : reste affiché non reproduit contre les 2 lignes réelles citées par le PO (`RC26040056`/`COF25120089`) — contrôle de code uniquement. Voir `DONE_DETAIL/TASK-141-motif-reste-a-affecter-badge-a-controler.md` et `DONE_DETAIL/TASK-141_verify.md`. | 2026-07-19 |
| TASK-098 | **Réintégration manuelle d'une ligne `Exclue`** par motif métier — clôturée **caduque** (décision PO 19/07/2026, en réponse à l'audit de cohérence TODO/TASKS/VERIFY) : principe métier confirmé, l'exclusion d'une ligne (`DM_LGTVA.Etat=Exclue`) **est** la non-déclaration de ce règlement pour la période en cours, pas un blocage définitif à réparer. TASK-097 (figeage/sélection recalculés à chaque déclaration) + TASK-099 (période = simple date de coupure, rattrapage de l'arriéré non déclaré, sans borne basse) garantissent qu'un règlement exclu (cause corrigée entre-temps : ICE renseigné, rapprochement effectué, etc.) redevient naturellement sélectionnable à la déclaration suivante — aucun mécanisme de réintégration dédié n'est nécessaire. Seul motif auto-géré différemment (`DejaEnCoursAilleurs`, TASK-080) reste réintégré automatiquement, hors périmètre de cette clôture. Voir `DONE_DETAIL/TASK-098-reintegration-manuelle-lignes-exclues.md`. | 2026-07-19 |
| TASK-116 | **Fix bloquant : front non servi sur `/` (404 en prod)** (`Declaration.API/Program.cs`) : `UseStaticFiles()` était documenté comme appelé sans `UseDefaultFiles()` en amont, causant un 404 sur `GET /` malgré un `wwwroot` peuplé (constaté en production 17/07/2026). **Constat lors de l'audit de cohérence TODO/TASKS/VERIFY (19/07/2026, demande PO)** : le correctif était déjà présent en code — `Program.cs:125-129` appelle bien `UseDefaultFiles()` avant `UseStaticFiles()`, avec `MapFallbackToFile("index.html")` en complément (commentaire de code référençant explicitement TASK-116) — seule la ligne `TODO.md` (« bloquant, priorité immédiate ») n'avait jamais été mise à jour. **Vérifié par l'architecte** : lecture directe du fichier confirmant l'ordre correct des middlewares. Voir `DONE_DETAIL/TASK-116-fix-front-non-servi-racine-usedefaultfiles.md`. | 2026-07-19 |
| TASK-138 | **`DomainGrid` : migration du rendu `<table>`+`<tr position:absolute>` vers un pattern flexbox** (`declaration-tva-web/src/DomainGrid.tsx`), suite de TASK-113 (paradigme `<table>` abandonné après 2 patchs in-place rejetés en test réel). **Constat lors de l'audit de cohérence TODO/TASKS/VERIFY (19/07/2026, demande PO)** : la migration était déjà réalisée en code — rendu 100 % `<div>` avec rôles ARIA (`role="table"/"row"/"columnheader"/"cell"`), largeur de colonne partagée en-tête/lignes via une fonction unique `colStyle()` (l.57-60, appliquée l.306/356) ; un `VERIFY/TASK-138_verify.md` avec 4 captures avant/après réelles existait déjà mais n'avait jamais été instruit, et la ligne `TODO.md` était restée à « à faire ». **Vérifié par l'architecte** : lecture directe de `DomainGrid.tsx` confirmant l'absence totale de l'ancien paradigme `<table>`/`<tr>`. Captures réelles déplacées vers l'archive de la task. Voir `DONE_DETAIL/TASK-138-domaingrid-migration-flexbox-remplace-table-virtualisee.md`. | 2026-07-19 |
| TASK-113 | **`DomainGrid` : colonnes désalignées** (« traçage de grid » mal formé, signalement PO 17/07/2026) — les `<tr>` virtualisés en `position:absolute` dans une `<table>` désalignaient les colonnes en-tête/lignes. Deux tentatives de patch in-place (`tableLayout:fixed`+`colgroup`, puis `width` explicite par cellule) **rejetées en test réel** malgré build+revue statique favorables à chaque fois. **Clôturée sans correction directe** — paradigme abandonné, remplacé par TASK-138 (migration flexbox). **Constat lors de l'audit de cohérence TODO/TASKS/VERIFY (19/07/2026)** : clôture confirmée par lecture du code — aucune trace résiduelle du paradigme `<table>`/`position:absolute` dans `DomainGrid.tsx`. Voir `DONE_DETAIL/TASK-113-domaingrid-lignes-absolute-colonnes-desalignees.md`. | 2026-07-19 |
| TASK-140 | **Élimination des règlements « déjà déclarés » de l'écran ① Sélection + numéro de déclaration sur l'écran Rapprochement** (`declaration-tva-web/src/ReglementsSelection.tsx`, `declaration-tva-web/src/RapprochementInterrogation.tsx`, `Declaration.API/Dtos/ReglementRapprochementDto.cs`, `Declaration.Application/Entities/ReglementRapprochement.cs`, `Declaration.Infrastructure/Repositories/DeclarationRepository.cs`) : signalement PO (19/07/2026, `TVA1-2026-02`, capture d'écran) — les règlements déjà verrouillés par une déclaration antérieure restaient affichés (non sélectionnables) dans l'étape ① d'une nouvelle déclaration, sans valeur pour l'opération en cours. **Arbitrage PO confirmé (clarification architecte avant rédaction de TASK-140)** : élimination complète en étape ①, **à condition** que le numéro de la déclaration verrou devienne visible dans l'écran Rapprochement global — le principe « rien ne disparaît en silence » (mémoire `grf-objectif-confiance-transparence`) est ainsi respecté par **déplacement** de la traçabilité, pas par suppression. **Implémentée en worker exceptionnel** (à la demande du PO). **Mécanisme** : filtrage `items.filter(r => !r.declare)` appliqué en amont dans `fetchAll` (`ReglementsSelection.tsx`), **après** la boucle de pagination serveur (comparée au `totalCount` non filtré — aucune rupture de pagination) et **avant** `setAllData` — tous les dérivés (compteurs, filtre État, sélection par défaut) opèrent sur le jeu déjà purgé ; seuls les autres statuts `bloque` (non affecté, « Autre (impayé) ») restent affichés (non-régression). Côté back, **aucun filtrage serveur ajouté** (endpoint `GET /api/rapprochement` partagé avec l'écran Rapprochement resté strictement additif, TASK-054) : la sous-requête agrégée gagne `MAX(AF.DT_Id) AS DtId`/`MIN(AF.DT_Id) AS DtIdMin`, puis un **unique appel batché** par page (`ResoudreNumeroDeclarationAsync`, `SELECT DT_Id, Numero FROM DM_ENTTVA WHERE DT_Id IN @dtIds` sur `PersistenceConnection`) résout les `DT_Id` distincts en numéros et fusionne en mémoire — décision technique retenue explicitement pour éviter un JOIN SQL trois-parties fragile entre les deux bases Dapper distinctes (`GrfConnection`/`GRFN_Dummy` et `PersistenceConnection`/`DeclarationTVA`). Cas d'incohérence `DtIdMin ≠ DtId` (théorique, verrouillage atomique TASK-028) : les deux numéros sont explicitement affichés (`⚠ {numMin} / {numMax} (incohérent)`), jamais un choix arbitraire silencieux. Front Rapprochement : nouveau composant `DeclareBadge` (numéro si résolu, repli `OuiNonBadge` Oui/Non sinon — cas sélection dans une autre déclaration `EnCours` non close), appliqué aux 3 endroits (largeur colonne, rendu grille, panneau détail). **Vérifié par l'architecte, tout rejoué** : lecture source des 5 fichiers modifiés confirmant chaque affirmation du VERIFY (filtrage exact avant `setAllData`, pagination sur `totalCount` non filtré, résolution batchée sans N+1, incohérence surfacée) ; `dotnet build Declaration.API` 0 erreur ; `dotnet test Declaration.Orchestration.Tests` **137/137** ; `npm run build` (`tsc -b && vite build`) 0 erreur (même warning `INEFFECTIVE_DYNAMIC_IMPORT` préexistant, sans lien). **Réserve non bloquante, documentée non silencieuse par le VERIFY** : aucune reproduction UI instrumentée sur données réelles (`GR_EMA_DISTRIBUTION`, captures avant/après façon TASK-125) n'a été rejouée dans cette passe — vérification faite au niveau code/contrat uniquement ; à demander explicitement au PO si une preuve visuelle sur données réelles est requise avant clôture définitive. Voir `DONE_DETAIL/TASK-140-elimination-lignes-deja-declarees-ecran-selection.md` et `DONE_DETAIL/TASK-140_verify.md`. | 2026-07-19 |
| TASK-139 | **Badge « Écart détecté » global vs détail « ligne incohérente » filtré par onglet** (`declaration-tva-web/src/VerifierIntegrerPanel.tsx`) : signalement PO (déclaration `TVA1-2026-02`, écart -49 167,99 MAD) — message trompeur « Écart détecté mais aucune ligne incohérente identifiée » quand la ligne incohérente responsable de l'écart (calculé toutes lignes confondues, `DeclarationsController.cs:239-277`) est dans l'onglet **non affiché**, le détail (`recapIncoherence.find(r => r.domaine === selectedTab && ...)`) étant filtré par onglet actif — angle mort laissé par TASK-112/TASK-108. Défaut de restitution pur, aucun recalcul d'écart en cause. **Correctif appliqué (option 1, front seul, recommandation architecte)** : nouveau `ligneIncoherenteAutreOnglet` (useMemo, `recapIncoherence.find(r => r.domaine !== selectedTab && r.incoherente)`) + helper `domaineOngletLabel`, branché dans le bloc `equilibre`/error **avant** l'ancien message de repli (qui reste inchangé pour les autres cas) : quand la ligne incohérente est dans l'autre onglet et que l'écart est globalement expliqué (`ecartExplique !== false`), nouveau message « Écart expliqué : la ou les N ligne(s) incohérente(s) ... se trouvent dans l'onglet « X ». Basculez sur cet onglet pour en voir le détail. ». Aucun changement back — `ecartExplique`/`recapIncoherence` (calcul global) non touchés, contrat JSON identique. **Vérifié par l'architecte, tout rejoué** : lecture source confirmant le correctif exact aux lignes décrites (branche insérée avant l'ancien message, condition `ligneIncoherenteAutreOnglet && ecartExplique !== false`) ; `npm run build` (`tsc -b && vite build`) rejoué → 0 erreur (même warning `INEFFECTIVE_DYNAMIC_IMPORT` préexistant, sans lien) ; `npx playwright test task139` rejoué → **4/4 passés** (A renvoi vers l'autre onglet cas exact PO, B bascule d'onglet affiche le détail, C non-régression TASK-112 même onglet, D non-régression écart réellement inexpliqué par aucune ligne — pas de bascule à tort) ; `dist/` confirmé exempt de toute référence au harnais de test (`task139-harness.tsx`/`task139.html`, dev-only). **Réserve non bloquante, documentée non silencieuse par le VERIFY** : reproduction en environnement backend réel (checkup calculé sur la vraie base pour `TVA1-2026-02`) recommandée au PO avant clôture définitive mais non bloquante — le défaut étant strictement front (contrat `recapIncoherence` inchangé), la preuve navigateur sur le vrai composant est représentative. Option 2 (raccourci de bascule d'onglet direct) explicitement non implémentée, à proposer au PO comme amélioration UX ultérieure. Voir `DONE_DETAIL/TASK-139-badge-ecart-global-vs-detail-incoherence-par-onglet.md` et `DONE_DETAIL/TASK-139_verify.md`. | 2026-07-19 |
| TASK-125 | **Grille de règlements durablement vide après retour sur ① Sélection** (`declaration-tva-web/src/App.tsx`, `declaration-tva-web/src/ReglementsSelection.tsx`) : signalement PO (19/07/2026, `TVA1-2026-01`) — après « Passer au calcul » ou « Détail des lignes » puis retour sur ①, grille et compteurs (Règlements/Sélectionnés/Total) tombent à 0 durablement. 4 hypothèses non tranchées (H1 remontage du stepper, H2 fetch sans garde d'annulation, H3 jointure cross-catalogue, H4 `showToast` non stabilisé) — reproduction instrumentée obligatoire avant tout correctif (règle projet « ne jamais improviser un contexte manquant »). **Reproduction réelle** (API+DB réelles `GR_EMA_DISTRIBUTION`, build front de production — pas `npm run dev`, pour éviter que le double-montage de `React.StrictMode` masque le bug — JWT réel, Playwright) : H3 **réfutée** pour l'environnement réel du PO (`connections.json` réel : `GrfConnection`/`PersistenceConnection` pointent la même base) ; H1 **confirmée nécessaire mais insuffisante seule** (in-app, le state survit à un changement d'onglet ; seule une réouverture réelle de la déclaration le réinitialise, et la restauration TASK-097 fonctionne alors normalement sans panne réseau) ; H2 et H4 **confirmées et combinées à H1** forment la cause racine réelle : `showToast` (`App.tsx`) non mémoïsé change de référence à chaque rendu d'`App` → recrée `fetchAll` (dépendance de son `useCallback`) → redéclenche le `useEffect` de fetch ; en cas d'échec, l'appel à `showToast` re-rend `App`, régénère sa référence et reboucle, **sans garde d'annulation** (H2, contrairement à l'effet voisin `distincts` du même fichier) pour empêcher une réponse tardive et périmée d'écraser un résultat redevenu bon. **Preuve mesurée** : une seule panne réseau simulée déclenche **1061 à 1078 requêtes** `GET /rapprochement` en quelques secondes (reproduit 3 fois) ; tant que la tempête ne retombe pas, `allData`/compteurs restent à 0, et une fois retombée, la restauration de sélection persistée (gatée par `allData.length>0`) ne se relance **jamais** spontanément — blocage durable, confirmé par 18s+ d'attente sans la moindre requête ni amélioration. **Correctif ciblé sur la cause confirmée** : `showToast` mémoïsé (`useCallback([])`, `App.tsx`) — casse la boucle à la racine ; garde `cancelled` ajoutée à l'effet de `fetchAll` (`ReglementsSelection.tsx`), symétrique à `distincts`, défense en profondeur contre toute réponse tardive résiduelle. Aucune modification de `DeclarationStepper.tsx`, de la logique de figeage TASK-097, ni des connexions SQL. **Vérifié par l'architecte, tout rejoué** : lecture source confirmant les 2 correctifs exacts aux lignes décrites ; `npm run build` (`tsc -b && vite build`) 0 erreur (même warning `INEFFECTIVE_DYNAMIC_IMPORT` préexistant, sans lien) ; les 10 captures avant/après (`VERIFY/task125-*.png`) présentes et confirmant le scénario (1078→1 requêtes pour la même panne, récupération fiable par changement d'onglet après correctif contre aléatoire avant) ; non-régression TASK-096 (total pied de grille, dérivé de `selectedRows`)/TASK-097 (sélection persistée, 145/146 restaurés) vérifiée à chaque étape des scénarios. **Réserve non bloquante, documentée non silencieuse par le VERIFY** : une panne réseau réellement isolée (hors tempête) laisse encore la grille à 0/0 jusqu'à une navigation manuelle (changement d'onglet ou réouverture) — pas de retry automatique, hors périmètre de cette task (qui portait sur le blocage permanent, pas l'absence d'un mécanisme de retry), amélioration UX distincte (bouton « Réessayer ») à cadrer séparément si le PO le souhaite. Voir `DONE_DETAIL/TASK-125-grille-reglements-vide-retour-calcul-detail-lignes.md` et `DONE_DETAIL/TASK-125_verify.md`. | 2026-07-19 |
| TASK-123 | **Rétrogradation `net10.0` → `net8.0`** sur la chaîne API (`Declaration.API`/`.Infrastructure`/`.Application`/`.Core`/`.Selection`/`.Orchestration`/`.Controle`/`.Export.Xml`/`.Export.Excel` + tests), suite au signalement PO (« Serveur injoignable » sur déploiement réel, `System.PlatformNotSupportedException: Microsoft.Data.SqlClient is not supported on this platform` — `Microsoft.Data.SqlClient` 7.0.2 ne fournit d'assets que jusqu'à `net9.0`). **VERIFY initialement rejeté (19/07/2026)** : affirmait à tort que `Microsoft.Extensions.Hosting.WindowsServices`/`Microsoft.Extensions.Configuration.Abstractions` (`10.0.9`) n'avaient pas besoin de repli de version, alors qu'un `FileNotFoundException: Microsoft.Extensions.Validation` survenait au démarrage réel. **Resoumission (19/07/2026)** : investigation approfondie révèle que l'hypothèse du rejet était **fausse** — ces deux paquets, bien que versionnés `10.0.9`, embarquent bien un asset `lib/net8.0` dans le même `.nupkg` (schéma de versionnage Microsoft découplé du TFM depuis .NET 9), confirmé indépendamment par l'architecte via `project.assets.json` (sections `compile`/`runtime` résolvant `lib/net8.0/*.dll`). **Cause racine réelle trouvée** : `Deploy-All.ps1` ne nettoyait jamais `deploy\` avant `dotnet publish` — un `Microsoft.Extensions.Validation.dll` orphelin d'un ancien build `net10.0` restait physiquement présent malgré un `deps.json` à jour (comparaison `Get-FileHash` : l'ancien `Microsoft.Data.SqlClient.dll` net9 était resté en place derrière un `deps.json` déclarant net8). **Correctif** : `Remove-Item -Recurse -Force $DeployPath` ajouté avant publish (`Deploy-All.ps1`, symétrique au nettoyage déjà existant pour `installer\`). **Vérifié par l'architecte, tout rejoué** : `project.assets.json` confirmé (asset `lib/net8.0` réellement résolu pour les 2 paquets incriminés) ; `find deploy -iname "*Validation*"` → 0 résultat (orphelin disparu) ; `deploy/Declaration.API.runtimeconfig.json` confirmé `net8.0`/`8.0.29` ; build solution complète rejoué → 0 erreur (1 échec préexistant hors périmètre `PerfTest`, sans rapport avec le TFM). **Confirmation réelle PO (19/07/2026)** : mise à jour rejouée avec succès, plus d'exception au démarrage. Voir `DONE_DETAIL/TASK-123-retrogradation-net8-microsoft-data-sqlclient-incompatible-net10.md` et `DONE_DETAIL/TASK-123_verify.md`. | 2026-07-19 |
| TASK-124 | **`WinSwServiceManager.Stop()` ne vérifiait pas l'arrêt réel du service** avant de rendre la main — `RunWinSw("stop")` n'attendait que la fin du process CLI éphémère `WinSW.exe stop` (simple transmission `ControlService()` au SCM), jamais celle du process réel `Declaration.API.exe` (piloté par l'instance de service WinSW), rendant `RetryOnFileLock` (15s fixe, `SetupForm.cs`) insuffisant de façon intermittente — nécessitait un arrêt manuel du service avant mise à jour (signalement PO 19/07/2026, découvert en marge de l'investigation TASK-123). **Correctif** (`WinSwServiceManager.cs`) : `Stop()` appelle désormais `WaitForApiProcessExit(TimeSpan.FromSeconds(45))` après `RunWinSw("stop")` — poll actif toutes les 500ms sur `Process.GetProcessesByName("Declaration.API")` filtré par chemin d'exécutable exact (`MainModule.FileName`), lève une `TimeoutException` explicite en cas de dépassement. `RetryOnFileLock` conservé tel quel en filet de sécurité secondaire (plus seul mécanisme d'attente). **Vérifié par l'architecte** : code relu, correspond exactement à l'objectif de la task ; `dotnet build Declaration.Setup -c Release` rejoué → 0 erreur. **Limite du VERIFY, comblée depuis** : la logique n'avait été vérifiée que par lecture de code + compilation (pas de test réel en conditions de charge/timeout) — **confirmation réelle PO (19/07/2026)** : mise à jour du service rejouée avec succès, plus d'intervention manuelle nécessaire pour arrêter le service. Voir `DONE_DETAIL/TASK-124-arret-service-winsw-non-verifie-avant-maj.md` et `DONE_DETAIL/TASK-123_verify.md` (§8.3, correctif documenté dans la même resoumission que TASK-123). | 2026-07-19 |
| TASK-120 | **Nouvelle teinte signature noir/vert** (variables CSS cœur `index.css` : `--accent-primary: #178a4c`, `--sidebar-bg: #1a1a1a`, etc., 7 variables au total) + **icône application « DM »** (sidebar, remplace le monogramme texte, + favicon d'onglet navigateur) — substance implémentée par un « implémenteur exceptionnel » sous autorisation PO explicite (nuit du 18 au 19/07/2026). **VERIFY initialement ni APPROVE ni REJECT (BLOQUÉ, 19/07/2026)** : substance jugée saine par l'architecte (grep 0 résidu de l'ancienne palette indigo, contraste WCAG vérifié — 13,18:1 texte sidebar/fond, 6,87:1 texte actif — build 0 erreur), mais les critères de validation explicites de la task (captures (a) écran de connexion, (b) sidebar, (d) écran dense + audit visuel de non-régression de layout/densité) n'étaient pas remplis : seule la capture (c) écran de blocage licence était fournie, (a)/(b)/(d) absentes. **Cause structurelle confirmée en lisant le diff `App.tsx`** : le blocage de licence (TASK-117) empêche d'atteindre `<Auth/>`/`<Dashboard/>` sans serveur ApLicence réel et valide, indépendamment du bug SqlClient (TASK-123) — bloquant la production des captures manquantes dans cet environnement. **Clôturée par décision PO explicite (19/07/2026)**, sans reprise de code ni production des captures (a)/(b)/(d) formellement requises par la task — la substance ayant été jugée saine par l'architecte et l'application confirmée fonctionnelle en conditions réelles par le PO lors de la clôture conjointe de TASK-123/TASK-124. **Réserve documentée, non levée** : l'ambiguïté visuelle potentielle entre le vert signature (`--accent-primary`, élément actif) et le vert de statut déjà existant (`--success`, « ok ») signalée par l'architecte n'a pas été formellement auditée. Voir `DONE_DETAIL/TASK-120-nouvelle-teinte-signature-noir-vert-sidebar.md` et `DONE_DETAIL/TASK-120_verify.md`. | 2026-07-19 |
| TASK-121 | **Variabilisation des badges de statut vers des variables CSS sémantiques** (`declaration-tva-web/src/index.css` — 8 variables `--status-ok-*`/`--status-warning-*`/`--status-blocking-*` ; 14 fichiers `.tsx` migrés) : suite de TASK-120 — décision PO (18/07/2026) d'étendre le re-thème aux 197 couleurs hex codées en dur des badges ok/attention/bloquant (18 fichiers, jamais traitées depuis TASK-083), pour éviter qu'ils restent figés sur l'ancienne palette pendant que le reste du produit change. **Implémentée en worker exceptionnel** (rôle inversé, autorisation PO explicite, démarrée sans attendre l'approbation VERIFY de TASK-120 — risque assumé et documenté, aucune teinte de palette signature réutilisée à ce stade donc aucun conflit introduit). Substitution purement mécanique (valeur hex identique avant/après, aucune nouvelle teinte inventée) fichier par fichier, 2 couples de variables distincts posés (`--status-warning-bg/text` et `--status-warning-border/text-alt`, ce dernier ajouté au correctif ci-dessous), 5 occurrences volontairement laissées en dur (chips catégoriels `OrigineChip`/`DomaineChip`/`TauxBadge` réutilisant coïncidemment une teinte de statut pour un autre usage, pas des badges). **VERIFY initialement rejeté (19/07/2026)** : le grep de contrôle final ne portait que sur 6 des 8 codes hex exhaustivement prescrits par TASK-121 étape 2 — `#fde68a`/`#b45309` avaient été silencieusement omis et reclassés à tort en « découvertes hors périmètre », alors qu'ils sont explicitement dans le périmètre de la task. **Correctif apporté et vérifié** (commit `26af7ec`) : 2 variables ajoutées (mêmes valeurs hex, structuration seule), 30 occurrences réellement migrées (17 committées dans 7 fichiers + 13 dans `VerifierIntegrerPanel.tsx`, working tree non commité — fichier lui-même livrable non commité d'un autre chantier). **Vérifié par l'architecte, tout rejoué** : grep exact des 8 codes prescrits sur l'état réel du dépôt → exactement 5 résidus, tous non-badges et déjà documentés (comptage confirmé indépendamment) ; `npm run build` (`tsc -b && vite build`) rejoué → 0 erreur (même warning `INEFFECTIVE_DYNAMIC_IMPORT` préexistant, sans lien) ; isolation Git confirmée (`git status` : les 4 fichiers portant du travail concurrent non lié — `AffectationsDrill.tsx`/`DeclarationList.tsx`/`DomainGrid.tsx`/`ReglementsSelection.tsx` — restent modifiés en répertoire de travail, non absorbés par le commit du correctif ; `VerifierIntegrerPanel.tsx`/`DeclarationFinalePanel.tsx` confirmés jamais commités, cohérent avec le VERIFY). **Réserves non bloquantes, non tranchées ici** (arbitrage PO différé, conforme à TASK-121 §Étapes point 5) : teintes additionnelles hors des 8 codes prescrits non consolidées (`#fef9c3`/`#fef3c7`/`#16a34a`, bordures `#fecaca`/`#fca5a5`/`#bbf7d0`) ; `App.tsx`/`VerifierIntegrerPanel.tsx`/`DeclarationFinalePanel.tsx` portent la variabilisation en répertoire de travail mais hors commit isolé (livrables non commités d'autres chantiers, TASK-117/TASK-090/TASK-091) ; captures avant/après non produites (backend indisponible cette nuit, compensé par la garantie de construction — substitution token-identique). Voir `DONE_DETAIL/TASK-121-variabilisation-badges-statut-palette-signature.md` et `DONE_DETAIL/TASK-121_verify.md`. | 2026-07-19 |
| TASK-122 | **Corrections formulaire setup (Déclaratif Maroc)** (`Declaration.Setup/SetupForm.cs`, `Declaration.Setup/Models/SetupData.cs`, `Declaration.Setup/Services/ConnectionsFileService.cs`, `Declaration.Setup/Assets/AppIcon.ico`) : corrections post-livraison demandées par le PO (18/07/2026) en observant le formulaire réel après TASK-115/TASK-117. **Implémentée en worker exceptionnel** (rôle inversé, demande explicite PO/architecte 18/07/2026, cf. réserve `CLAUDE.md`, même mode que TASK-101/TASK-075/TASK-114/TASK-117/TASK-118). **Connexion SQL unique** : les deux `ConnectionGroup` (`GRF`/`Persistance`) fusionnés en un seul groupe de saisie, dupliqué vers `SetupData.Grf`/`Persistence` à la lecture — `connections.json` reste inchangé dans sa structure (toujours deux clés). **Décision actée avant codage** (point non tranché par la task) : en mode mise à jour, si un `connections.json` existant a des connexions `Grf`/`Persistence` divergentes (installation antérieure à ce TASK), seule `Grf` (canonique) est pré-affichée et un avertissement explicite apparaît — pas d'écrasement silencieux. **Subject ApLicence fixé** : champ retiré du formulaire, `SetupData.ApLicenceSubject` vaut désormais par défaut la constante `"TRESO_GRM_COM"`, plus jamais relue d'un `connections.json` existant. **Dossier par défaut** : `%ProgramFiles%\APBS\Declaratif Maroc` (résolu via `Environment.GetFolderPath`, échec explicite si non résolvable, pas de repli deviné) au lieu de `C:\DeclaratifMaroc` codé en dur. **Icône** : `AppIcon.ico` régénérée (conversion technique, multi-résolution 16/32/48/256) à partir de `TASKS/assets/task-120-icon-dm/icon-dm-512.png` fourni par l'architecte, remplace l'indigo TASK-119. **Charte graphique** : nouveau bandeau fond `#1a1a1a` (icône DM + titre vert `#3ddc84`) + bouton « Installer » vert `#178a4c` — reste du formulaire en thème système standard, périmètre volontairement limité (validé explicitement, pas de restylage complet improvisé). **Vérification (formulaire réellement exécuté et capturé dans cette session, contrairement à TASK-115/TASK-119)** : `dotnet build` Debug + Release — 0 erreur/0 avertissement ; captures d'écran réelles (`PrintWindow`) confirmant un seul bloc connexion, absence du champ Subject, bandeau/bouton aux couleurs attendues, icône DM correctement embarquée ; `connections.json` réellement généré via un harnais exerçant directement les classes de production (`SetupData`/`ConnectionsFileService`) — `GrfConnection == PersistenceConnection` et `Subject == "TRESO_GRM_COM"` confirmés, y compris en reprenant un `connections.json` préexistant avec un ancien subject différent (ignoré comme attendu) et des connexions `Grf`/`Persistence` divergentes (divergence détectée, telle que voulue). **Réserves non bloquantes** : avertissement de divergence non capturé visuellement en conditions réelles (instabilité de l'injection de touches dans cet environnement multi-écran, logique prouvée par harnais de code réel à la place) ; aucune installation réelle de service Windows (WinSW) déclenchée dans cette session (action système à effet persistant, volontairement non exercée sans confirmation préalable — sans impact sur le périmètre vérifié, l'écriture de `connections.json` étant strictement antérieure dans le flux). Voir `DONE_DETAIL/TASK-122-setup-unification-connexion-sql-unique-subject-dossier-defaut.md` et `DONE_DETAIL/TASK-122_verify.md`. | 2026-07-18 |
| TASK-117 | **Intégration vérification de licence ApLicence dans `Declaration.API`** (`Declaration.API/Program.cs` — middleware de blocage unique + init `LicenceMonitor` ; `Declaration.API/Licence/GrfLicenceConfigProvider.cs` nouveau ; `Declaration.API/Controllers/LicenceController.cs` nouveau ; `Declaration.API/libs/ApLicence/*.dll` nouveau ; `nuget.config` racine nouveau ; `connections.json`/`deploy/connections.json` ; `declaration-tva-web/src/api.ts`, `App.tsx`) : TASK-117 était documentée `⛔ bloquée` (attente d'un repo séparé) — constaté cette session que ce blocage n'est plus d'actualité, `D:\_vibe\GRLicence` (librairie partagée de vérification de licence, protocole `ApLicence` lecture seule) est livré et packagé (`GRLicence.1.1.0.nupkg`, TASK-001/TASK-002 approuvées 17/07/2026 dans ce repo séparé). **Implémentée en worker exceptionnel** (rôle inversé, demande explicite PO 18/07/2026, cf. réserve `CLAUDE.md`, même mode que TASK-101/TASK-075/TASK-114). **Décisions actées avant codage** : subject confirmé par le PO — **`TRESO_GRM_COM`**, codé en dur dans `Program.cs`, jamais lu depuis `connections.json` (corrige le texte original de la task, rédigé avant la décision de sécurité `GRLicence`/TASK-002 qui interdit tout subject configurable côté fichier client) ; périmètre du blocage tranché à **`/api/**` seul** (front statique `wwwroot` jamais bloqué, pour qu'il puisse toujours charger et afficher lui-même le message de blocage via le nouvel endpoint public `GET /api/licence/status`, seul exclu du blocage). **Bug réel découvert et corrigé** (hors périmètre `GRLicence` mais bloquant pour cette task) : `GRLicence.1.1.0.nupkg` ne packe pas les DLL `ApLicence.Common/.Core/.Core.Net` qu'il référence en privé — `Declaration.API` levait une `FileNotFoundException` non gérée dès `DemarrerAsync()` (échappe même au `try/catch` interne de `LicenceMonitor`, résolution de type au JIT) et **crashait au démarrage**, contredisant frontalement le critère central de la CDC (« le process démarre toujours ») ; contourné côté GRF par copie des mêmes DLL que `GRLicence` (`Declaration.API/libs/ApLicence/`) + références explicites dans le `.csproj`, à signaler séparément au propriétaire du repo `GRLicence`. **Vérifié** (revue architecte, tout rejoué) : build back (`Declaration.API`, 0 erreur) + front (`tsc`+`vite`, 0 erreur) ; process réellement démarré (`ASPNETCORE_ENVIRONMENT=Development`, aucun serveur `ApLicence.Server` disponible dans cet environnement) malgré le timeout de vérification (fail-closed conforme, comportement attendu quand le subject/serveur est injoignable) ; bout-en-bout HTTP réel — `GET /api/licence/status` → 200 (jamais bloqué), `GET /api/declarations` et `POST /api/auth/login` → 503 `{"message":"Merci de vérifier la licence"}` (un seul point de contrôle, aucune route oubliée), `GET /`/assets → 200 (front jamais bloqué) ; rendu front réel (Playwright, capture) confirmant l'écran de blocage plein-écran ; scénarios « licence valide » (bannière J-30 et cas sans alerte) vérifiés par interception réseau du seul endpoint statut (captures fournies), faute de serveur `ApLicence.Server` réel disponible dans cet environnement pour un test de bout en bout complet. **Réserves non bloquantes** : scénario licence valide non prouvé contre un vrai serveur (revue de code + interception réseau front, pas une preuve bout-en-bout complète) ; bug de packaging `GRLicence` contourné localement, pas corrigé à la source (duplication de DLL entre repos) ; `TASK-122` (non codée) décrit encore le subject comme un champ `connections.json`, devenue obsolète et à corriger. Voir `DONE_DETAIL/TASK-117-integration-verification-licence-aplicence.md` et `DONE_DETAIL/TASK-117_verify.md`. | 2026-07-18 |
| TASK-119 | **Correction `Declaration.Setup` : retrait des champs Sage/SageOM globaux devenus obsolètes** (`Declaration.Setup/Models/SetupData.cs`, `Declaration.Setup/SetupForm.cs`, `Declaration.Setup/Services/ConnectionsFileService.cs`) **+ icône** (`Declaration.Setup/Assets/AppIcon.ico`, monogramme « DM » sur indigo `#2b4c7e`, cohérent TASK-083) **+ renommage de l'exe livré au client** (`Declaration.Setup.exe` → `DeclaratifMaroc.exe`, `Declaration.Setup.csproj`) : impact direct de TASK-118 (résolution Sage désormais par `SO_Id`/`P_SOCIETE`, plus une valeur globale unique saisie une fois) sur `Declaration.Setup` (TASK-115, `IN_PROGRESS`/VERIFY rejeté pour des réserves indépendantes), conçu sur l'hypothèse précédente. **Décision PO (18/07/2026) : suppression entière, pas de fallback.** Retrait de `SqlConnectionParts Sage`/`SageOmUser`/`SageOmPassword` de `SetupData`, du groupe « Sage (commerciale) » et du groupe « Identifiants applicatifs Sage OM » de `SetupForm` (contrôles, pré-remplissage, lecture au submit), et des lectures/écritures `SageConnection`/`SageOM` de `ConnectionsFileService`. Conservé volontairement (hors périmètre) : `PrerequisiteChecker.CheckSageOm()`/`_chkConfirmSageOm` (case « Sage OM installé », prérequis logiciel TASK-115) et le sélecteur de version Sage 100 (`_cboSageVersion`, TASK-101) — aucun rapport avec une connexion/un identifiant Sage. `DOCS/DEPLOIEMENT.md`/`LANCEMENT_DEV.md` mis à jour (identifiants Sage OM par société désormais dans `P_SOCIETE`, plus saisis au setup). Icône `.ico` multi-résolution (16/32/48/256) produite et référencée dans `<ApplicationIcon>` ; `<AssemblyName>` renommé `DeclaratifMaroc`, propagé aux 6 fichiers l'utilisant en dur (`Deploy-All.ps1`, `Declaration.Setup/deploy/Publish-Setup.ps1`, `Declaration.Setup/Services/DeploymentCopier.cs`, `Declaration.Setup/Services/WinSwServiceManager.cs`, `DOCS/DEPLOIEMENT.md`, `LANCEMENT_DEV.md`) — **bug réel corrigé au passage** dans `DeploymentCopier` : le filtre d'auto-exclusion du dossier d'installation ciblait encore `Declaration.Setup*`, ce qui aurait fait copier le nouvel exe renommé sur lui-même. **Vérification (revue architecte, code relu ↔ preuve, rejoué)** : `SetupData.cs`/`SetupForm.cs`/`ConnectionsFileService.cs` relus en source — aucune occurrence résiduelle de `SageConnection`/`SageOM`/`SageOmUser`/`SageOmPassword`, seules restent les mentions hors périmètre (`SageVersion`, `CheckSageOm`) ; `Declaration.Setup.csproj` confirmé (`AssemblyName=DeclaratifMaroc`, `ApplicationIcon=Assets\AppIcon.ico`), `Assets/AppIcon.ico` présent sur disque ; `dotnet build Declaration.Setup/Declaration.Setup.csproj` **Debug et Release** rejoués à l'identique par l'architecte — **0 erreur, 0 avertissement**, sortie `DeclaratifMaroc.dll`/`.exe` confirmée ; grep exhaustif `Declaration\.Setup\.exe` sur le dépôt : seules subsistent des mentions historiques explicites (« ex-`Declaration.Setup.exe` ») dans les 6 fichiers mis à jour — aucune occurrence active oubliée. **Réserve non bloquante** (identique à TASK-115) : aucune capture d'écran / pilotage réel du formulaire WinForms dans cette session (pas d'accès interactif Windows/UAC) — formulaire non revu visuellement en conditions réelles après retrait des 2 groupes Sage, à confirmer par le PO sur poste réel si jugé nécessaire. **Séquencement rappelé** : ce correctif doit être intégré avant toute clôture définitive de TASK-115 en production. Voir `DONE_DETAIL/TASK-119-correction-setup-formulaire-sage-so-id.md` et `DONE_DETAIL/TASK-119_verify.md`. | 2026-07-18 |
| TASK-118 | **Résolution dynamique de la connexion Sage par `SO_Id`** (`Declaration.Application/Interfaces/IDbConnectionFactory.cs`, `Declaration.Infrastructure/Factories/DbConnectionFactory.cs`, `Declaration.Application/Services/DeclarationWorkflowService.cs`) + **correctif du cache `GRC_VENTILATION_SAGE_CACHE` → `DM_VENTILATION_SAGE_CACHE`** (`DeclarationTVA.sql`, `Declaration.Orchestration/VentilationSageCacheRepository.cs`, `Declaration.Infrastructure/Repositories/DeclarationRepository.cs`) : signalement PO (17/07/2026) — une même instance GRF doit pouvoir orchestrer des sociétés dont les données Sage vivent dans plusieurs bases physiques distinctes (`SO_Id`), alors que `SageConnection` était globale/statique et que le cache de ventilation avait pour clé `(EC_Id, Taux)` — `EC_Id` n'étant unique qu'au sein d'une seule base Sage, ceci exposait un **risque de collision silencieuse de TVA entre sociétés**. **Implémentée en worker exceptionnel** (rôle inversé, cf. réserve `CLAUDE.md`, même mode que TASK-101/TASK-075). **Mécanisme** : nouvelle méthode `IDbConnectionFactory.GetSageConnectionInfoAsync(int soId)` — lit `P_SOCIETE.SO_ErpDb`/`SO_ErpUserApp`/`SO_ErpPasswdApp` via `GrfConnection`, construit la chaîne Sage en réutilisant **Server/User/Password de `GrfConnection` tels quels** (confirmation PO explicite 18/07/2026), seule `Database` étant remplacée par `SO_ErpDb` ; échec explicite (`InvalidOperationException`), jamais de repli silencieux, si `SO_Id` introuvable ou `SO_ErpDb` vide/NULL. Propagé aux 4 sites d'appel de `BuildOrchestrateur` (devenu `BuildOrchestrateurAsync`) via `declaration.SocieteId` déjà en scope. Cache : colonne `SO_Id` (INT, **sans** `FOREIGN KEY` — exigence PO explicite pour ne pas impacter le modèle EF de l'application principale propriétaire de `P_SOCIETE`) ajoutée à la clé primaire composite `(SO_Id, EC_Id, Taux)`, migration `sp_rename` idempotente avec back-fill (`SO_Id=1`, seul cas possible avant cette task) + renommage `GRC_VENTILATION_SAGE_CACHE` → `DM_VENTILATION_SAGE_CACHE` (conformité `DM_*`, TASK-065) dans la même passe ; 2 méthodes de lecture du cache par `EC_Id` seul (`GetEcIdsEnErreurAsync`, `EnrichirFamilleBDepuisCacheAsync`) corrigées au passage (même risque de collision, pas seulement le chemin d'écriture). `connections.json`/`LANCEMENT_DEV.md` mis à jour (`SageConnection`/`SageOM` statiques marquées obsolètes, non lues par l'API, conservées pour compatibilité `Declaration.Setup` — nettoyage tracé par TASK-119). Résolution différée retenue pour le point de validation (pas de fail-fast au login, qui ne détermine aujourd'hui aucune connexion Sage) — l'échec explicite à la première utilisation satisfait déjà le critère fail-fast. **Vérification (architecte, tout rejoué)** : `dotnet build DeclarationTVA.slnx` 0 erreur ; tests — `Declaration.Core.Tests` 32/32, `Declaration.Export.Xml.Tests` 5/5, `Declaration.Export.Excel.Tests` 1/1, `Declaration.Orchestration.Tests` **137/137** (9 IT SQL Server réels inclus, rejoués contre `.\sql2022`/`GR_EMA_DISTRIBUTION`), `Declaration.Selection.Tests` 58/59 et `Declaration.Controle.Tests` 1/2 (les 2 échecs confirmés préexistants et sans rapport — connexion SQL codée en dur `IHEB-PC\ihebc` / déclaration GRFN `DT_Id=66` absente de cet environnement, déjà documentés dans `TODO.md` avant cette task). Migration SQL rejouée en réel sur table de test isolée (`EC_Id` 888001/888002) : la 1ʳᵉ version échouait (`Invalid column name 'SO_Id'`, `ALTER TABLE ADD` et `UPDATE` référençant la colonne dans le **même batch** T-SQL) — corrigé en séparant chaque `ADD COLUMN` par un `GO` de toute instruction la référençant ensuite ; reproduit et corrigé, rejoué 2 fois (idempotence prouvée), lignes réelles préservées, PK reconstruite `(SO_Id, EC_Id, Taux)`. `GetSageConnectionInfoAsync` exercé en conditions réelles (`P_SOCIETE`/`DESKTOP-5BFKKEP`) : `SO_Id=1` → chaîne correcte (Server/User/Password de `GrfConnection`, Database=`SO_ErpDb`) ; `SO_Id=999999` → échec explicite et message actionnable. Spot-checks du code source par l'architecte (indépendants du VERIFY) confirmant chaque affirmation : 4 sites d'appel propagent bien `soId`, absence de `FOREIGN KEY` sur `SO_Id` dans `DeclarationTVA.sql`, séparation par `GO` de la migration, et réalité des 3 lectures résiduelles `EC_Id`/`MV_Id` brutes signalées non corrigées (audit Étape 5). **Réserve non bloquante** : scénario réel « 2 `SO_Id` → 2 bases Sage physiquement distinctes » non prouvable (une seule ligne `P_SOCIETE` en environnement de dev) — anticipée et explicitement autorisée par la task elle-même (documenter plutôt que simuler un faux positif) ; la collision est rendue **structurellement impossible** dès qu'un second `SO_Id` existera (contrainte PK réelle, pas seulement logique applicative), prouvé indépendamment par la migration rejouée en réel. **Suivi ouvert, non bloquant** : 3 lectures brutes `EC_Id`/`MV_Id` sans filtre `SO_Id` sur des tables `RT_*` (propriété de l'application principale, hors périmètre GRF) signalées par l'audit Étape 5, non corrigées — décision à trancher séparément avec le PO si jugé nécessaire (voir `TODO.md`). Voir `DONE_DETAIL/TASK-118-connexion-sage-dynamique-par-so-id.md` et `DONE_DETAIL/TASK-118_verify.md`. | 2026-07-18 |
| TASK-101 | **Support multi-version du DLL Objets Métiers Sage** (`SageTaxReader/SageTaxReader.Core/SageTaxReader.Core.csproj`, `SageTaxReader/SageTaxReader.Console/SageTaxReader.Console.csproj`, `SageTaxReader/libs/{v7,v9,v10,v12}/Interop.Objets100cLib.dll`, `publish-sagetaxreader-workers.ps1` nouveau, `LANCEMENT_DEV.md`, `.gitignore`) : `SageTaxReader.Console` était figé en liaison précoce (`EmbedInteropTypes`) sur le DLL interop v12 au build, alors que le client `DESKTOP-5BFKKEP` tourne en v10 → 100 % d'échec de valorisation (`0xFFFFF562 — fichier non converti`) sur juin 2026 ; débloqué ponctuellement le 16/07 par remplacement manuel du DLL (non pérenne, cf. `DONE_DETAIL/TASK-101...md` NOTES). **Implémentée en worker exceptionnel** (rôle inversé, demande explicite PO/architecte 17/07/2026, cf. réserve `CLAUDE.md`). **Vérification préalable** : SHA256 des DLL v10/v11 fournis par le PO identique (bit-à-bit le même fichier) → 4 variantes de build suffisent (v7, v9, v10 couvrant aussi v11, v12), pas 5 — v8 exclue, aucun DLL fourni à ce jour. **Mécanisme** : nouvelle propriété MSBuild `SageInteropVersion` (défaut `v12`, comportement/nom d'exe inchangés si non spécifiée → zéro régression) pilotant à la fois le `HintPath` de la référence interop (`Core.csproj`) et l'`AssemblyName` de l'exe (`Console.csproj`, ex. `SageTaxReader.Console.v7.exe`) ; DLL restructurés en dossiers versionnés `SageTaxReader/libs/<version>/` (ancien fichier plat + `.bak` supprimés, ce qui corrige au passage la régression documentée — le DLL par défaut du poste de dev était resté en v10 après le test manuel du 16/07, restauré en v12 par construction du nouveau défaut) ; nouveau script `publish-sagetaxreader-workers.ps1` publiant chaque variante en dossier autonome `deploy\workers\<version>\` (nettoyage complet `obj`/`bin` entre variantes — `EmbedInteropTypes` fige les types Sage dans `SageTaxReader.Core.dll` au build, un dossier partagé entre versions serait incompatible). `LANCEMENT_DEV.md` documente la sélection de `WorkerConfig.WorkerExePath` par version chez chaque client. **Vérifié** (build) : les 4 variantes compilent 0 erreur (testé isolément puis via la séquence complète du script) ; build par défaut confirmé strictement identique à l'existant (nom/chemin d'exe inchangés) ; non-régression solution principale — `dotnet build DeclarationTVA.slnx` 0 erreur, suite de tests complète rejouée **234/236** verts (2 échecs préexistants et sans rapport, confirmés identiques à l'état `main` documenté dans `TODO.md` : connexion codée en dur `Declaration.Selection.Tests`, `Declaration.Controle.Tests.ComparateurTests`). **Vérifié (réel, partiel)** : smoke test du `SageTaxReader.Console.v10.exe` **nouvellement compilé** via le nouveau mécanisme (`-p:SageInteropVersion=v10`, pas un remplacement manuel de fichier) contre l'environnement déjà validé `DESKTOP-5BFKKEP`/`NEW_EMA DISTRIBUTION` — activation COM réussie, aucune erreur `0xFFFFF562` ni `REGDB_E_CLASSNOTREG` ; l'échec obtenu (identifiants SQL Server passés à tort en identifiants applicatifs Sage) est un échec d'authentification **applicative** Sage, sans rapport avec le sujet de la task — preuve que le nouveau mécanisme reproduit l'environnement déjà validé manuellement (197/197 + 240/240 pièces OM, 16/07/2026). **Réserve non bloquante honnêtement documentée** : les critères VALIDATION « lecture réelle d'une facture (TVA non nulle) » et « message clair sur incompatibilité de version » n'ont pas été rejoués avec de vrais identifiants applicatifs Sage (non disponibles à ce poste) — seule l'activation COM/DLL est prouvée en conditions réelles pour v10 ; v7/v9 vérifiées en build seul, aucune base Sage v7/v9 disponible pour test réel. Voir `DONE_DETAIL/TASK-101-support-multi-version-objets-metiers-sage.md`. | 2026-07-17 |
| TASK-114 | **Sécurisation JWT + droits SQL dédiés (prérequis installation production)** (`Declaration.API/Program.cs` — garde-fou démarrage ; `Declaration.API/appsettings.json` — annotation dev-only ; `connections.json` — section `JwtSettings.SecretKey` ; `LANCEMENT_DEV.md` ; `Declaration.Orchestration.Tests/Task114JwtProdSecretTests.cs` ; `DeclarationTVA.sql` déjà livré antérieurement) : revue du guide `LANCEMENT_DEV.md` en vue d'une installation production (demande PO 17/07/2026) a révélé la clé JWT de dev committée en clair (`appsettings.json`), jamais surchargée en prod (`connections.json` ne couvrait que `ConnectionStrings`/`WorkerConfig`), et une référence à un `appsettings.Production.json` **inexistant** dans le dépôt — tout déploiement produit signait ses JWT avec une clé publique, connue de quiconque a accès au repo. **Implémentée en worker exceptionnel** (demande explicite PO/architecte 17/07/2026, cf. réserve `CLAUDE.md`). **Mécanisme retenu** : extension de `connections.json` (`JwtSettings.SecretKey`) — `Program.cs` le charge déjà **après** `appsettings.json` (`AddJsonFile`), donc il le remplace automatiquement, sans dupliquer la logique de configuration ; cohérent avec le principe existant « un seul fichier à éditer en prod ». **Garde-fou ajouté** (`Program.cs`) : si l'environnement est `Production` (valeur par défaut de `ASPNETCORE_ENVIRONMENT` quand non positionnée — cas du service Windows `sc.exe`, `dotnet run` restant en `Development` via `launchSettings.json`) et que `JwtSettings:SecretKey` vaut encore une des clés de dev committées, l'application **refuse de démarrer** (`InvalidOperationException`) plutôt qu'une signature silencieuse avec une clé connue. `LANCEMENT_DEV.md` mis à jour (exemple `connections.json` étendu, nouvelle section « Clé JWT de production », référence `appsettings.Production.json` corrigée). **Vérifié** (revue architecte, code relu ↔ preuve, tout rejoué) : build `Declaration.API` 0 erreur ; `Declaration.Orchestration.Tests` **137/137** (2 nouveaux : token signé clé de dev rejeté par validation configurée clé de prod, token clé de prod accepté). **Preuve réelle process/HTTP** (binaire lancé hors `dotnet run`, `ASPNETCORE_ENVIRONMENT=Production`) : sans `JwtSettings.SecretKey` réel → démarrage refusé (`InvalidOperationException` observée) ; avec la clé réelle → démarrage normal, puis bout-en-bout HTTP sur l'instance réellement démarrée — token forgé avec la clé de dev fuitée → **401** sur `/api/declarations` (identique au cas sans token) ; token signé avec la vraie clé de prod → **200** sur `/api/societes`, **400**/**403** (validation métier/autorisation, jamais 401) sur `/api/declarations` — distinction nette confirmant le rejet effectif de tout token signé avec la clé versionnée. **Réserve non bloquante signalée** : `connections.json` est en réalité **suivi par git** (contredit l'hypothèse « déjà hors dépôt » du périmètre de la task) et porte déjà un mot de passe SQL réel (`sa`/`1234`, `DESKTOP-5BFKKEP`) — la nouvelle clé JWT hérite du même défaut ; retirer ce fichier du suivi git est un changement de convention dépôt-large distinct de ce correctif, laissé à l'arbitrage PO/architecte séparé. Voir `DONE_DETAIL/TASK-114-securisation-jwt-droits-sql-prerequis-production.md` et `DONE_DETAIL/TASK-114_verify.md`. | 2026-07-17 |
| TASK-112 | **Drill « Cohérence des totaux déclarés » : filtre tautologique — l'écart n'était jamais isolé** (`Declaration.API/Controllers/DeclarationsController.cs` — `GetCheckup` ; `Declaration.Infrastructure/Repositories/DeclarationRepository.cs` — `BuildLigneFilterWhere` ; `declaration-tva-web/src/RecapSourceTable.tsx` ; `declaration-tva-web/src/VerifierIntegrerPanel.tsx` ; `Declaration.Orchestration.Tests/Task112RecapIncoherenceTests.cs`) : signalement PO 17/07/2026 (`TVA1-2026-01`, onglet Décaissement) — le clic sur « Cohérence des totaux déclarés » filtrait `source=Decaissement` depuis l'onglet Décaissement, où **100 %** des lignes portent déjà cette source (`DM_LGTVA.Source` = doublon du domaine) → les 228 lignes du domaine remontaient sans filtre. Invalidait le postulat d'axe de TASK-107 et clôturait le suivi laissé ouvert par TASK-110 § Risques. **Arbitrage PO préalable** (bloquant, requis par la task) : axe retenu = **lignes incohérentes** (`TTC ≠ HT+TVA`), correspondant exactement à la définition de l'écart posée par TASK-108 (`ecart = Σ(TTC) − Σ(HT) − Σ(TVA)` sur `Integree||Proposee`, décomposable en la somme des résidus par ligne). **Correctif** : nouvel axe additif `recapIncoherence` (groupe `lignesRecap` par `(Domaine, |TTC−(HT+TVA)|>0.01)`) + filtre repository `incoherente` (`true`/`false`) ; `recapSource`/`recapTaux` et leurs autres consommateurs (`DeclarationFinalePanel`, TASK-086) **conservés inchangés, non touchés**. Nouveau champ `equilibre.ecartExplique` : vérifie que l'écart annoncé est intégralement rattaché à la somme des résidus incohérents — sinon (cas théorique « manque »), message honnête + détail de l'artefact d'ensembles TASK-108 (`reconciliation`) au lieu d'un sous-ensemble trompeur. **Exclu du périmètre, non touché** (conforme à la task) : calcul de l'écart lui-même (`DeclarationWorkflowService`), mise en page `DomainGrid` (TASK-113), `recapSource`/`recapTaux`. **Vérifié** (revue architecte, code relu ↔ preuve, tout rejoué) : build solution 0 erreur ; `Declaration.Orchestration.Tests` **135/135** (2 nouveaux : drill isolant exactement la ligne non ventilée résiduelle, non-régression TASK-108 sur déclaration équilibrée) ; 2 échecs préexistants et sans rapport confirmés (`Declaration.Selection.Tests` connexion codée en dur, `Declaration.Controle.Tests.ComparateurTests`) ; `tsc -b` **0 erreur** (rejoués par l'architecte). **Preuve réelle** `GR_EMA_DISTRIBUTION`/`TVA1-2026-01`/Décaissement (capturée avant l'incident DB ci-dessous) : ancien filtre `source=Decaissement` → 228/228 (tautologie confirmée) ; nouveau filtre `incoherente=true` → **2** lignes (`FC2501667`/`FC2501717`, résidu -24 462,00, motif réel « Incohérence Sage ») ; `incoherente=false` → 226 (228 = 2+226, aucune ligne perdue) ; `ecartExplique=true` (résidu du groupe incohérent = écart annoncé, -24 462,00 = -24 462,00). Corroboré indépendamment par le motif Sage réel injecté dans `task110.spec.ts` (`Σ(HT+TVA+Parafiscale)=3960,00 ≠ TTC=3762,00`), identique au motif trouvé en base sur `FC2501667`. Tests e2e `task107.spec.ts`/`task110.spec.ts` adaptés au nouvel axe, rejoués verts contre l'app réelle. **Incident survenu pendant la vérification, signalé et pris en charge séparément par le PO** (hors périmètre du correctif code) : les hooks `beforeAll` préexistants de `task107.spec.ts`/`task110.spec.ts` (`reset.ps1`/`make_eligible.ps1`, non modifiés par cette task) se sont exécutés contre `connections.json`, fichier de connexion **unique et partagé** de tout le repo (aucune base de test isolée) → toutes les déclarations réelles de `GR_EMA_DISTRIBUTION` supprimées (dont `TVA1-2026-01`, source de la preuve ci-dessus, capturée **avant** l'incident) et tampons `DT_Id` réinitialisés. Voir `DONE_DETAIL/TASK-112-drill-source-tautologique-ecart-non-filtre.md` et `DONE_DETAIL/TASK-112_verify.md`. | 2026-07-17 |
| TASK-111 | **Drill « Détail des lignes » (②) : explosion de requêtes HTTP parallèles sur grosse sélection** (`declaration-tva-web/src/AffectationsDrill.tsx`) : signalement via dump console navigateur (`DESKTOP-5BFKKEP`, déclaration `d80b6f0a-c09a-4086-9972-4223e12a0e73`) — une sélection de 150+ règlements ouvrait autant de requêtes `GET /lignes` en parallèle (`Promise.all` non borné, un fetch par règlement) que de centaines, dont la majorité échouait `net::ERR_INSUFFICIENT_RESOURCES` (plafond ~6 connexions HTTP/1.1/origine). **Implémentée en worker exceptionnel** (rôle inversé, demande explicite PO/architecte 17/07/2026, cf. réserve `CLAUDE.md`). **Correctif** (front-only, 1 fichier) : option 2 de la task retenue après vérification effective que le filtre `numeroRapprochement` accepte déjà une liste côté back (`BuildLigneFilterWhere`/`DeclarationRepository.cs:407-415`, TASK-067B) — `fetchLignesReglement` (1 requête/règlement) supprimée, remplacée par `fetchLignesSelection` qui regroupe la sélection par domaine (Encaissement/Decaissement) et émet **un seul appel paginé par domaine** (≤2 requêtes, indépendant de N) ; échec réseau isolé au domaine sans effacer l'autre (`echecs` nommant les règlements en cause, jamais un message générique). Chemin `readOnly` (`fetchAllLignesDeclaration`) et back non touchés. **Vérifié** (revue architecte, code relu ↔ preuve, tout rejoué) : `npx tsc --noEmit` **0 erreur** (rejoué) ; `BuildLigneFilterWhere` confirmé en source — le support multi-valeurs de `numeroRapprochement` est réel et paramétré (`IN @Numeros`, aucune injection) ; suite Playwright complète **rejouée par l'architecte contre l'API réelle** (`GR_EMA_DISTRIBUTION`) — **11/11 tests verts**, dont les 2 nouveaux `tests/task111.spec.ts` : grosse sélection (160 règlements, 150 Décaissement + 10 Encaissement) → **4 requêtes** `/lignes` observées (2 domaines × 2, doublement volontaire `React.StrictMode` en dev — 2 en production), 160 lignes affichées intégralement, aucun échec ; échec réseau isolé simulé sur le domaine Décaissement → toast nommant les 150 règlements (tronqué à 10 + compteur), grille conservant les 10 lignes Encaissement malgré la panne de l'autre domaine. `fetchLignesReglement` confirmé absent du code (`git grep` 0 occurrence). **Réserve non bloquante honnêtement documentée** : la granularité de l'isolation d'échec passe du règlement individuel au domaine (compromis assumé par la task, strictement meilleur que 150 requêtes parallèles) ; cas réel d'origine non rejoué tel quel, preuve apportée par sélection synthétique de taille comparable (160 vs 152 réels). Voir `DONE_DETAIL/TASK-111-drill-affectations-explosion-requetes-paralleles-grosse-selection.md` et `DONE_DETAIL/TASK-111_verify.md`. | 2026-07-17 |
| TASK-110 | **Grille de drill « Vérifier & Intégrer » : mise en page dégradée — colonne Motif Écartement débordait horizontalement** (`declaration-tva-web/src/DomainGrid.tsx`) : signalement PO (`TVA1-2026-01`, drill « Répartition par source » TASK-107) — un motif d'écartement long (incohérence Sage) débordait de sa colonne, forçant un défilement horizontal disproportionné ; cause = cellules `DomainGrid` sans troncature ni largeur bornée (contrairement à `AffectationsDrill.tsx`, déjà conforme). **Implémentée en worker exceptionnel** (rôle inversé, demande explicite PO/architecte). **Correctif** (front-only, pur rendu, 1 fichier) : largeur de colonne bornée (défaut 200px, `motif` 280px) + troncature `overflow:hidden`/`textOverflow:ellipsis` sur en-têtes/cellules, texte complet conservé au survol (`title`) ; mécanisme de filtre **non touché** (signalement PO « le filtre ne fonctionne pas » resté non confirmé côté code, laissé en réserve). **Vérifié** (revue architecte) : diff conforme au VERIFY (`AffectationsDrill.tsx` confirmé non modifié par cette task — diffs présents appartenant à des tasks antérieures déjà approuvées, non commitées), `tsc -b && vite build` 0 erreur (rejoué par l'architecte) ; test Playwright `tests/task110.spec.ts` (motif identique au signalement PO injecté sur `TVA1-2026-06` réel) — largeur de cellule mesurée 280px, captures avant/après ; suite existante rejouée verte (`declaration.spec`, `task088`, `task107`, `column-selector`), aucune régression. **Réserve non bloquante** : cas d'origine `TVA1-2026-01` absent de la base restaurée (même limitation que TASK-105/107/109) ; clarification PO requise sur le signalement filtre Source avant tout correctif. Voir `DONE_DETAIL/TASK-110-debordement-horizontal-grille-drill-source-verifier-integrer.md` et `DONE_DETAIL/TASK-110_verify.md`. | 2026-07-17 |
| TASK-109 | **« Détail des lignes » (drill ②) figeait zéro ligne s'il était ouvert avant « Passer au calcul »** (`declaration-tva-web/src/DeclarationStepper.tsx` — `persisterSelection`/`handleOuvrirDrill` ; `declaration-tva-web/src/AffectationsDrill.tsx` — message de grille vide) : signalement PO sur `TVA1-2026-01` (145 règlements sélectionnés) — le drill affichait 0 ligne / 0,00 MAD / « Aucune affectation ne correspond aux filtres » sans aucun filtre actif. Cause : `GET /declarations/{id}/lignes` fige les lignes `DM_LGTVA` **au premier appel** en filtrant sur la sélection **persistée côté serveur** (`ConstruireLignesFigeesAsync`, garde-fou TASK-097 : jamais « tous les candidats » par défaut) ; seul « Passer au calcul » persistait cette sélection — le bouton « Détail des lignes » (TASK-092) ouvrait le drill sans jamais le faire, donc figeait zéro ligne si cliqué en premier. **Correctif** (front-only) : extraction de `persisterSelection` (même appel `POST /declarations/{id}/selection`), appelée désormais aussi par `handleOuvrirDrill` avant `setShowDrill(true)` — le serveur reste seul juge de l'éligibilité (TASK-097 non contourné), on s'assure seulement qu'il voit la bonne sélection au bon moment. Message de grille vide distingué selon `activeFilterCount` (« Aucune affectation trouvée pour les règlements sélectionnés » si aucun filtre actif, message « filtres » réservé au cas filtré). **Vérifié** (revue architecte) : build front rejoué, 0 erreur (warning Vite préexistant sans lien) ; test Playwright `tests/task109.spec.ts` reproduisant le scénario PO (nouvelle déclaration, sélection, clic direct « Détail des lignes » sans passer par « Passer au calcul ») — grille non vide dès le premier appel (12 lignes), captures `task109-01/02.png` ; suite existante rejouée verte (`task096`, `task087`, `task088`, `task102`, `declaration.spec`), aucune régression `readOnly`/incohérence. Second symptôme (non-refigeage sur changement de sélection après un premier figeage réussi) explicitement laissé hors périmètre, signalé pour arbitrage PO séparé. Voir `DONE_DETAIL/TASK-109-drill-affectations-vide-avant-passer-au-calcul.md` et `DONE_DETAIL/TASK-109_verify.md`. | 2026-07-17 |
| TASK-108 | **Faux « écart détecté » à l'étape ② — les 3 termes du contrôle d'équilibre n'agrégeaient pas le même ensemble** (`Declaration.API/Controllers/DeclarationsController.cs:271` — `GetCheckup` ; `Declaration.Orchestration.Tests/Task108EcartEquilibreEnsembleUniqueTests.cs`) : `totalTva` était calculé sur les seules lignes `Integree`, alors que `TotalDeclareTtc`/`TotalMontantAffecte` (`DeclarationWorkflowService.GetCheckupAsync`) sont déjà agrégés sur `Integree \|\| Proposee` (`lignesRecap`, déjà défini en TASK-103). Sur une déclaration `EnCours` (0 ligne `Integree`), l'écart affiché valait donc `Σ_Proposee(TVA)` — la TVA totale de la déclaration mal étiquetée « écart détecté » systématique. **Correctif** (back-only, 1 ligne) : `totalTva = lignesRecap.Sum(l => l.TVA)` (même ensemble que `recapSource`/`recapTaux`), variable `integrees` (devenue inutilisée) supprimée ; `recapSource`/`recapTaux`/`DeclarationWorkflowService` et tout front **non touchés** (périmètre strict respecté). **Vérifié** (revue architecte) : build solution 0 erreur (warnings préexistants uniquement) ; `Declaration.Orchestration.Tests` **133/133** (3 nouveaux : écart ≈0 sur `EnCours`/`Proposee` équilibrée cas réel, non-régression écran ⑤ `Cloturee`/`Integree`, résidu réel `TTC≠HT+TVA` toujours détecté) ; preuve réelle `GR_EMA_DISTRIBUTION` sur les deux déclarations `EnCours` disponibles — `TVA1-2026-06` (1 538,27 → **0,000000**) et `TVA1-2026-07` (1 480,50 → **0,000000**), `isValid=True` ; aucun montant déclaré/ligne/sélection modifié (vérifié par relecture, seul le libellé « écart » change de valeur). Débloque TASK-107 (à recadrer PO, cf. `TODO.md`). Voir `DONE_DETAIL/TASK-108-ecart-equilibre-artefact-ensembles-etape2.md` et `DONE_DETAIL/TASK-108_verify.md`. | 2026-07-17 |
| TASK-107 | **Localiser l'écart jusqu'à la facture/au règlement — drill « Répartition par source » (option 3)** (`declaration-tva-web/src/RecapSourceTable.tsx`, `declaration-tva-web/src/VerifierIntegrerPanel.tsx`) : arbitrage PO (17/07/2026, requis par le blocage posé après TASK-108) — **option 3 retenue** (renvoi vers le drill filtré existant TASK-016/`DomainGrid`, front-only, sans nouveau back) ; options 1 (ventilation descendante enrichie) et 2 (écart = différence d'ensembles) écartées par le PO. `RecapSourceTable` reçoit une prop optionnelle `onRowClick` (aucun changement de rendu si absente, non-régression TASK-103/087) ; `VerifierIntegrerPanel.handleDrillSource` réutilise l'état `drillFiltre` déjà câblé pour le drill anomalie (TASK-016/088), avec un champ `kind` pour distinguer le libellé du bandeau. Filtre `source` = même colonne `DM_LGTVA.Source` que celle groupée par `/checkup` pour `recapSource` (`BuildLigneFilterWhere`) : aucune transformation, aucune divergence possible entre la source cliquée et les lignes retrouvées. Aucune modification du calcul de l'écart, de la valorisation, ni du back. **Vérifié** (revue architecte, code relu ↔ preuve) : `tsc --noEmit` et `npm run build` 0 erreur (rejoués par l'architecte) ; test Playwright réel (`tests/task107.spec.ts`, `GR_EMA_DISTRIBUTION`, `TVA1-2026-06`, 5 règlements réels sélectionnés) — clic sur la source « Decaissement » ouvre le drill et retrouve **3 factures réelles** (`FC2502204`/`FC2600789`/`FC2600895`), bandeau lecture seule, retour au contrôle sans figeage confirmé (bouton « Confirmer intégration » resté actif). Captures `VERIFY/task107-step3-avant-drill.png` et `VERIFY/task107-step3-drill-source.png`. **Réserve non bloquante honnêtement documentée** : le cas réel ne présentant plus d'écart naturel après le correctif TASK-108, `equilibre.isValid` est forcé à `false` par interception réseau côté test pour rendre le contrôle visible — `recapSource` n'est à aucun moment modifié, reste la valeur réelle du back. Voir `DONE_DETAIL/TASK-107-drill-ecart-facture-reglement-repartition-source.md` et `DONE_DETAIL/TASK-107_verify.md`. | 2026-07-17 |
| TASK-106 | **Token de paiement de l'espèce non valorisée — `MV_Point` bornée à `Espece`** (`Declaration.Orchestration/VentilationSageCacheRepository.cs` — `GetCurrentPaiementToken` ; `Declaration.Orchestration.Tests/Task024CacheVentilationSageTests.cs` — `Task106TokenEspeceTests`) : un règlement espèce fournisseur franchissait l'éligibilité (`SelectionExpliqueeEvaluator.cs:85` exempte l'espèce de `MV_Point`) mais recevait un token `NULL` à la valorisation, car `GetCurrentPaiementToken` exigeait `MV_Point=1` pour **tout** `EC_Id` → facture traitée comme dépayée, disparition **silencieuse** de la ligne (TVA espèce jamais déclarable, 0/8 sur données réelles). **Correctif** (back-only) : token reconnu si `MV_Point=1` **OU** (`MV_Domaine=1` **ET** `MV_Type=0`) — miroir **exact** de la définition Espèce de la sélection (`ModePaiementId ← MV_Type`, vérifié), **strictement borné** à l'espèce fournisseur (jamais aux non-espèces — risque fiscal TASK-099). Token de fraîcheur = `MV_Id`+`MV_Point` tel que lu, auto-invalidant → cache TASK-024 cohérent ; sélection/éligibilité **non touchée** (hors périmètre respecté). **Vérifié** (revue architecte, correctif relu ↔ code de sélection, mirror confirmé exact) : tests d'intégration rejoués **contre le SQL Server réel** (`GR_EMA_DISTRIBUTION`, lecture seule, aucune mutation) — 8/8 règlements espèce cités → token non nul (contre 0/8), non-régression stricte confirmée (virement non rapproché `RF26010005` reste `NULL`, virement rapproché `RF26010001` inchangé) ; `Task106TokenEspeceTests` 3/3 verts en live (264 ms), suite `Declaration.Orchestration.Tests` **130/130**, build 0 erreur. **Réserve non bloquante honnêtement documentée** : confirmation visuelle PO sur l'écran ① (ligne valorisée réelle + `valorisation.log` propre) laissée en aval — la matérialisation complète dépend du pipeline OM/FGR existant (hors périmètre, couvert par TASK-024). Voir `DONE_DETAIL/TASK-106-token-paiement-espece-non-valorisee-mvpoint.md` et `DONE_DETAIL/TASK-106_verify.md`. | 2026-07-17 |
| TASK-105 | **Traçabilité d'une incohérence validée — marqueur persistant + filtre, drill « Détail des affectations »** (`declaration-tva-web/src/AffectationsDrill.tsx`) : après clic sur « Valider l'incohérence », la facture sortait de `facturesIncoherentes` (dérivé des seules alertes **actives** — `AffectationsDrill.tsx:431`) → bandeau rouge, surlignage et boutons **disparaissaient totalement**, alors que le flag `incoherenceValidee` (déjà remonté du back, TASK-078) restait `true` en base — décision assumée devenue invisible, en violation du principe « aucune ligne silencieuse ». **Implémentée en worker exceptionnel** (rôle inversé, demande explicite PO/architecte 17/07/2026 — cf. réserve `CLAUDE.md`). **Correctif** (front-only, pur rendu, 1 fichier) : nouvelle colonne filtrable `Incoh.` (badge ambre « Validée » quand `incoherenceValidee===true`, `—` sinon) qui ne disparaît jamais après validation contrairement au bandeau ; léger fond ambre sur la ligne (seulement si l'alerte n'est plus active, pour ne pas entrer en conflit avec le rouge d'une incohérence encore ouverte) ; filtre colonne liste fixe `Oui`/`Non` aligné sur le mécanisme existant (`statutConformite`). Aucune modification du calcul (montants/TVA/écart), de la détection TASK-077/078, ni de « dé-validation » (hors périmètre). Qui/quand (`IncoherenceValideePar`/`Le`, existants en base/entité) **non exposés** dans `LigneCandidateDto` — périmètre strict respecté, signalé pour arbitrage PO séparé. **Vérifié** (revue architecte, code relu ↔ preuve, build/lint rejoués) : `tsc -b && vite build` 0 erreur, `oxlint` 0 nouveau warning sur le fichier modifié ; diff conforme point par point à la description (colonne/rawCell/filtre/badge/teinte, DTO non touché — vérifié en source). Preuve réelle `GR_EMA_DISTRIBUTION`/`TVA1-2026-06` (captures `TASK-105-01/02/03`) : avant = 10 lignes, colonne `Incoh.` à `—` partout, Total TVA 2 125,58 MAD ; après = facture `FA2600664` porte le badge + teinte ambre sur ses 2 lignes de taux, Total TVA **inchangé** 2 125,58 MAD (rendu pur confirmé) ; filtre `Oui` → grille réduite à 2/10 lignes, TVA filtrée 282,26 MAD. **Réserve non bloquante honnêtement documentée** : `TVA1-2026-01` (cas d'origine du signalement PO) absent de la base restaurée, 0 ligne `IncoherenceValidee=1` réelle disponible → valeur simulée par interception réseau (même technique que `tests/task102.spec.ts`) sur un objet `LigneCandidateDto` réel, seule la valeur du flag est simulée ; rafraîchissement post-validation (`handleValider`→`reload()`, comportement existant inchangé) non re-testé en conditions réelles faute de cas disponible — à confirmer par le PO au prochain cas réel. Voir `DONE_DETAIL/TASK-105-tracabilite-incoherence-validee-marqueur-filtre.md` et `DONE_DETAIL/TASK-105_verify.md`. | 2026-07-17 |
| TASK-104 | **Payé/TTC/Prorata affichés une seule fois par facture — drill « Détail des affectations »** (`declaration-tva-web/src/AffectationsDrill.tsx`) : la grille étant 1 ligne par (facture × taux), les colonnes facture-level Payé/TTC/Prorata étaient recopiées à l'identique sur chaque ligne de taux (`FA2502941` affichait 4× 27 170,50 MAD) — calcul sain (seul le total TVA déclarée, par taux, est sommé), mais risque de re-somme erronée au tri/export. **Correctif** (front-only, pur rendu) : marqueur `premiereLigneDuGroupe`, symétrique de `derniereLigneDuGroupe` déjà existant (même clé `(numeroReglement, factureNumero)`, sens inverse) ; `renderGridCell` n'affiche Payé/TTC/Prorata que sur la 1ère ligne du groupe, cellule vide sinon — `tauxTVA`/`baseTva`/`tva` (par-taux) inchangées ; filtres (`rowMatches`/`rawCell`) continuent de lire les valeurs réelles, jamais vidées en interne. **Revue architecte** : 1er VERIFY **REJETÉ** — capture avant/après manquante alors qu'atteignable sans action destructive (le worker avait renoncé par excès de prudence, croyant `reset.ps1` nécessaire). **Preuve réelle fournie** après correction : `TVA1-2026-01` (cas cité par la TASK) n'existe plus dans l'environnement restauré — cas de substitution `TVA1-2026-06`/`FA2503257` (4 lignes de taux 9/20/10/0 %, identifié par requête réelle) ; capture avant (Payé/TTC/Prorata répétés sur les 4 lignes) et après (affichés uniquement sur la 1ère ligne, cellules vides ensuite, Taux inchangé, totaux identiques 1 255,93 MAD filtré / 3 663,85 MAD général) sans reset ni mutation. Build `tsc --noEmit`/`vite build` 0 erreur, `oxlint` 0 warning. Voir `DONE_DETAIL/TASK-104-affichage-paye-ttc-une-fois-par-facture-drill-affectations.md` et `DONE_DETAIL/TASK-104_verify.md`. | 2026-07-17 |
| TASK-103 | **Détail d'écart (« Répartition par source ») invisible en pré-intégration : `recapSource`/`recapTaux` alignés sur `Integree \|\| Proposee`** (`Declaration.API/Controllers/DeclarationsController.cs` — `GetCheckup` ; `Declaration.Orchestration.Tests/Task103RecapSourceProposeeTests.cs`) : à l'étape ②, le contrôle « Cohérence des totaux déclarés » affichait bien l'écart mais **pas** le tableau « Répartition par source » — l'écart agrège `Integree`+`Proposee` (`DeclarationWorkflowService.cs:742`) alors que `recapSource`/`recapTaux` n'agrégeaient que `Integree`, donc vides tant que la déclaration est `EnCours` (lignes `Proposee`), et `RecapSourceTable` (`VerifierIntegrerPanel.tsx:1033`, monté seulement si `recapSource.length>0`) ne s'affichait jamais avant clôture. **Correctif back** : ajout de `lignesRecap = lignes.Where(Integree \|\| Proposee)`, `recapSource`/`recapTaux` construits dessus ; `integrees` (`Integree` seul) conservé tel quel pour `totalTva` et le **calcul de l'écart** (inchangé) ; aucune modif front. **Revue architecte** : 1er VERIFY **BLOQUÉ** — preuve faite sur un cas **source unique** (`TVA1-2026-06`, écart 1 538,27) où le tableau ne fait que recopier le badge, l'objectif « localiser d'où vient l'écart » n'étant pas démontré ; exigence d'un cas multi-sources représentatif. **Réserve levée par preuve réelle multi-sources** (`GR_EMA_DISTRIBUTION`, déclaration `TVA1-2026-07`, 3 règlements réels) : écart global 1 480,50 MAD **ventilé** — onglet Achats/`Espece` TVA 1 243,33 + onglet Ventes/`Encaissements` TVA 237,17 = 1 480,50 — le tableau discrimine réellement la source de l'écart (ventilation par domaine → un onglet par domaine, par construction). Build `Declaration.API` 0 erreur/0 warning ; `Declaration.Orchestration.Tests` **127/127** (2 nouveaux : `recapSource` non vide sur `Proposee` en écart ; non-régression écran ⑤ sur `Cloturee`/`Integree`) ; `TargetFramework` du projet de tests passé `net10.0`→`net10.0-windows` pour référencer `Declaration.API` (sans impact fonctionnel). **Réserve non bloquante honnêtement documentée** : ventilation `Espece`+`Decaissement` **dans un même onglet** non démontrée — bloquée par un **bug distinct découvert et tracé** (règlements espèce « Éligibles » mais jamais valorisés : `VentilationSageCacheRepository.cs:63` exige `MV_Point=1` alors que `SelectionExpliqueeEvaluator.cs:85-90` exempte les espèces ; 0/8 sur données réelles) → **à ouvrir en TASK dédiée**. Voir `DONE_DETAIL/TASK-103-detail-ecart-invisible-preintegration-recapsource.md` et `DONE_DETAIL/TASK-103_verify.md`. | 2026-07-17 |
| TASK-102 | **Tracer le n° de règlement (`RC…`) dans les anomalies de facture — écran ② Vérifier & Intégrer** (`Declaration.Application/Services/DeclarationWorkflowService.cs`, `Declaration.Orchestration.Tests/Task102NumeroReglementAnomaliesFactureTests.cs`) : le bloc « Anomalies bloquantes / Avertissements » n'affichait que la pièce `FC…`, jamais le règlement `RC…` d'affectation (pourtant déjà présent via `NumeroRapprochement`, déjà utilisé l.396), en contradiction avec le pivot règlement-first. Enrichissement **texte seul** des deux messages : `FACTURE_NON_VENTILEE` (« Ligne en anomalie de recalcul (facture FC…, règlement RC…) : … ») et incohérence Sage sur ligne exclue au figeage `LIGNE_FIGEE_A_REVERIFIER` (« Facture FC…, règlement RC… exclue de la valorisation … »), avec repli propre si `NumeroRapprochement` vide/nul (message d'origine conservé, aucune mention parasite). Formulation reprise à l'identique de l'alerte existante l.396. Aucun changement de calcul, d'écart d'équilibre ni de liste de lignes exclues. **Vérifié** (revue architecte, source relue ↔ preuves) : build solution 0 erreur ; `Declaration.Orchestration.Tests` verts (4 nouveaux tests : avec/sans règlement × 2 alertes, dont non-régression `DoesNotContain("règlement")`) ; capture réelle écran ② montrant les deux anomalies enrichies (`RC25040088`, `RF26040040`). Voir `DONE_DETAIL/TASK-102-numero-reglement-anomalies-facture.md`. | 2026-07-17 |
| TASK-100 | **Règlement d'échéance `EC_Type` hors liste blanche (0/4/111) disparaît silencieusement du tunnel** (`declaration-tva-web/src/ReglementsSelection.tsx`, `Declaration.Application/Services/DeclarationWorkflowService.cs`, `Declaration.Orchestration.Tests/Task100ReglementEctypeInconnuTests.cs`) : afin d'éviter qu'un règlement avec un `EC_Type` non géré (par exemple `EC_Type = 1`, impayé client, ou autre hors-périmètre) ne disparaisse silencieusement du tunnel après sa sélection, l'application remonte désormais des alertes de synthèse explicites `REGLEMENT_EXCLU` (Warning) au checkup. Côté front, la grille dense de sélection ② (ou ①) affiche des badges de statut explicites (« Impayé (Bloqué) » pour `EC_Type = 1` et « Hors périmètre (Bloqué) » pour les autres `EC_Type` inconnus, reportés à la phase 2). La détection front a été corrigée pour utiliser une égalité exacte `row.origine === 'Autre (1)'` afin d'éviter les faux positifs sur d'autres codes d'origine (`Autre (10)`, `Autre (12)`, etc.). | 2026-07-17 |
| TASK-097 | **Scoper le figeage + la clôture à la sélection réelle de l'écran ①** (`Declaration.Application/Services/DeclarationWorkflowService.cs` — `ConstruireLignesFigeesAsync`) : la sélection de règlements de l'écran ① est désormais persistée (`DM_SELECTION_REGLEMENT`, migration `009_DM_SELECTION_REGLEMENT.sql`) et consommée pour filtrer les candidates **avant** figeage/clôture, au lieu du mois entier des règlements éligibles. `MapLignesCandidates` ne produit plus d'état `Exclue` du tout (simplification actée PO) ; `CloturerDeclarationAsync` tamponne `DT_Id` uniquement sur les règlements dont les lignes ont été figées, donc scopées par construction. **Retour de revue architecte** : 1er VERIFY REJETÉ — `ConstruireLignesFigeesAsync` ne filtrait que `if (selection != null && selection.Any())`, donc si aucune sélection n'était encore persistée (`/lignes` appelé avant tout `POST /selection`), **aucun filtre n'était appliqué** et tous les candidats éligibles du mois entier étaient figés (exactement le bug d'origine), en violation de la contrainte architecturale explicite de la task (logique de périmètre interdite côté UI seul). **Corrigé** : condition retirée, filtre désormais inconditionnel (`selection ?? Enumerable.Empty<string>()`) — une sélection vide produit zéro candidat, jamais tous ; régression détectée et corrigée sur une fixture TASK-080 qui s'appuyait sans le vouloir sur l'ancien comportement laxiste ; nouveau test de non-régression ciblé. 2e VERIFY **BLOQUÉ** (réserve code levée, mais aucune preuve base réelle sur une task CRITIQUE touchant le périmètre fiscal réellement déclaré — précédent TASK-099). **Preuve réelle fournie** (rejouée sur `GR_EMA_DISTRIBUTION` via l'API réelle, déclaration jetable créée/supprimée, aucune donnée existante touchée) : 3 règlements réels sélectionnés parmi 761 éligibles non déclarés → seuls ces 3 dans `DM_LGTVA` (45 lignes, multi-factures, confirmé par requête directe) ; clôture → `DT_Id` posé exclusivement sur leurs 20 affectations `RT_AFFECTATION` (témoin non sélectionné resté `NULL`) ; réouverture → `DT_Id` effacé, sélection restaurée à l'identique depuis la base ; suppression → 0 résidu confirmé. Build 0 erreur, `Declaration.Orchestration.Tests` **116/116** (dont le nouveau test de régression). Voir `DONE_DETAIL/TASK-097-figeage-scope-selection-reglements.md` et `DONE_DETAIL/TASK-097_verify.md`. | 2026-07-14 |
| TASK-099 | **Redéfinition du périmètre déclarable de l'écran ① — la période devient une simple date de coupure** (`Declaration.Selection/SelectionExpliqueeEvaluator.cs`, `SelectionExpliqueeService.cs`, `SelectionnerAffectationsService.cs`, `Declaration.Infrastructure/Repositories/DeclarationRepository.cs`) : suite au signalement PO sur `RF26060125` (règlement affiché « Éligible » alors que non rapproché) — un règlement (espèce ou hors-espèce) est désormais proposé à l'écran ① si `DT_Id IS NULL` **ET** `DateReference < fin de période` (**plus de borne basse** — corrige la perte définitive d'un règlement rapproché/payé un mois donné mais jamais déclaré ce mois-là) **ET** déclarable (espèce OU rapproché banque). 3 chemins de sélection réalignés (borne basse `>= @debut` retirée) ; `RapprochementFromWhere` (endpoint partagé écran ①/Interrogation Rapprochement TASK-037) reçoit un gate conditionné à `@declarationId` : coupure seule + `EstDeclarableSqlM` obligatoire pour l'écran ①, comportement **strictement inchangé** (fenêtre pleine, non-rapprochés visibles) pour TASK-037 (décision PO : périmètre limité à ①). **Retour de revue architecte** : 1er VERIFY BLOQUÉ (3 des 6 critères VALIDATION sans preuve base réelle, task CRITIQUE à risque rétroactif) + énoncé de build inexact (« 0 avertissement » vs 18 réels, préexistants sans lien). **Réserves levées par preuve directe sur `GR_EMA_DISTRIBUTION`** (aucune donnée modifiée) : `RF26020079`/`RF26020004` (hors-espèce rapproché en janvier/avril, `DT_Id IS NULL`, absent sous l'ancienne fenêtre → apparaît en juin sous la nouvelle), `RC26050014` (espèce Client payée en mai, jamais déclarée, même constat), `RF26060125` (non rapproché → absent du jeu gated écran ①, **toujours visible** dans le jeu non gated TASK-037 — non-régression confirmée en réel, pas seulement par construction du code). Build 0 erreur (18 avertissements préexistants détaillés et disculpés un par un) ; tests **57/57** `Declaration.Selection.Tests` (dont 4 nouveaux TASK-099), **115/115** `Declaration.Orchestration.Tests`, **32/32** `Declaration.Core.Tests` ; `Declaration.Controle.Tests` 1/2 (échec préexistant sans lien, dépendance base réelle absente de l'environnement). Voir `DONE_DETAIL/TASK-099-redefinition-perimetre-declarable-rattrapage.md` et `DONE_DETAIL/TASK-099_verify.md`. | 2026-07-14 |
| TASK-096 | **Fiabiliser le total sélectionné de l'écran ① Sélection** après un cycle démontage/remontage (« Détail des lignes » → retour) : passage de `selectedRows` en prop à `ReglementsSelection` pour calculer `selectedTotal` directement à partir de ce prop (Option B), au lieu du `useMemo` sur `knownRowsRef` (`useRef` non suivi par React) qui restait figé à 0 après remontage. Initialisation de `knownRowsRef` via `useEffect` écoutant `selectedRows` pour que `toggleRow`/`toggleAllVisible` ne perdent pas la trace des règlements déjà sélectionnés avant la fin du rechargement. **Vérifié** (revue architecte) : `tsc --noEmit` rejoué, 0 erreur ; scope respecté (`toggleRow`/`toggleAllVisible`/endpoint `/rapprochement` non touchés — les changements de bandeau bas visibles dans le diff relèvent de TASK-092, déjà approuvée séparément) ; test Playwright `tests/task096.spec.ts` rejoué (total identique 5 864 704,79 MAD avant/après, non nul) ; captures dans `VERIFY/` (`task096-01-avant.png`, `task096-02-apres-retour.png`). Voir `DONE_DETAIL/TASK-096-total-selectionne-remis-a-zero-retour-drill.md`. | 2026-07-14 |
| TASK-095 | **Bouton « Réouvrir » en UI** (`declaration-tva-web/src/DeclarationList.tsx` — `peutRouvrir`/`ReopenConfirmModal`/`handleConfirmReopen` ; `App.tsx` — prop `currentUserName`) : concrétise le Périmètre C de TASK-073 (exposition UI, jusqu'ici laissé à la discrétion du PO). Bouton visible uniquement `isAdmin && Statut=Cloturee` (garde symétrique à `peutSupprimer`), confirmation explicite non édulcorée (effet réel : `EnCours` + libération `DT_Id`), restitution de traçabilité **C1** (toast utilisateur+horodatage — C2/historique consultable différé, non demandé explicitement par le PO). Front-only, aucune ligne touchée dans `ReouvriDeclarationAsync`/`JournaliserAudit`/`003_Verrou_DT_Id.sql` (mécanisme backend TASK-073 réutilisé tel quel). **Vérifiée** : build front 0 erreur ; preuve réelle rejouée (`.\sql2022`/`GR_EMA_DISTRIBUTION`) — réouverture via bouton, `Statut` passé à `EnCours` en base, ligne `[REOUVERTURE]` dans `logs/audit.log`, toast C1 affiché ; compte non-admin refusé en profondeur (`403` backend, garde `UT_Admin` non modifiée). **Réserve non bloquante** : la capture de preuve du masquage UI pour un compte non-admin montre une liste vide plutôt que la même liste peuplée avec boutons absents — preuve visuelle faible sur ce point précis (la logique de code `isAdmin &&` est correcte et vérifiée par lecture, et la frontière de sécurité réelle — le 403 backend — est prouvée séparément). Voir `DONE_DETAIL/TASK-095-bouton-reouverture-declaration-cloturee.md`. | 2026-07-14 |
| TASK-094 | **Traçabilité du tampon `DT_Id` — Option A (diagnostic) + Option B (persistance)** (`Declaration.Application/Services/DeclarationWorkflowService.cs` — `DiagnostiquerDtIdAsync`, pose/effacement dans `CloturerDeclarationAsync`/`ReouvriDeclarationAsync` ; `Declaration.Application/Interfaces/IDeclarationRepository.cs` + `Declaration.Infrastructure/Repositories/DeclarationRepository.cs` — `GetToutesDeclarationsAsync`/`GetDistinctDtIdsAffectationsAsync`/`SetDtIdDeclarationAsync` ; `Declaration.API/Controllers/DeclarationsController.cs` — `GET /api/diagnostic/dt-id` ; migration `008_DM_ENTTVA_DT_Id.sql`) : suite à l'incident du 14/07 (610 affectations tamponnées par des `DT_Id` orphelins, cf. `CHANGELOG.md`), gap de conception comblé en 2 temps. **Option A** : recalcule `DeriveDtId` pour toutes les déclarations existantes et qualifie chaque `DT_Id` observé de correspondance trouvée ou d'orphelin explicite (endpoint admin lecture seule). **Option B** (demande PO explicite après découverte du constat ci-dessous) : colonne additive `DM_ENTTVA.DT_Id`, posée à la clôture (même valeur que le tampon `RT_AFFECTATION`) et effacée à la réouverture — le diagnostic priorise désormais cette valeur persistée sur le recalcul, immunisant tout diagnostic futur contre une divergence de runtime .NET. Implémentée en rôle worker exceptionnel (demande explicite PO). **Vérifiée** : build solution 0 erreur, `Declaration.Orchestration.Tests` **115/115** (7 nouveaux tests dont priorité valeur persistée) ; preuve réelle rejouée sur `.\sql2022`/`GR_EMA_DISTRIBUTION` (2 harnais jetables supprimés après usage, migration réellement appliquée avec idempotence vérifiée, clôture/réouverture test confirmant pose puis effacement exact de `DT_Id`). **Bug trouvé et corrigé pendant la vérification** : propriété C# nommée `DtId` (sans underscore) ne matchait pas la colonne SQL `DT_Id` dans le mapping POCO par défaut de Dapper (pas de normalisation d'underscore) — écritures silencieusement invisibles à la relecture, détecté par élimination sur la preuve réelle (jamais par les tests unitaires, qui n'utilisent pas Dapper), corrigé en renommant `DT_Id` (même convention que `LigneCandidate.EC_Id`/`MV_Id`). **Constat significatif, désormais résolu** : le recalcul réel (runtime .NET 10, identique à la production) avait initialement divergé des valeurs notées au moment de l'incident pour `TVA1-2026-06` — une des 3 valeurs traitées comme orpheline (`830393939`, 10 lignes) lui appartenait en réalité. Le cycle réouverture/reclôture réel exécuté pendant la vérification de TASK-095 a naturellement re-posé ce tampon (confirmé en base : `RT_AFFECTATION` et `DM_ENTTVA.DT_Id` cohérents à `830393939`) — aucune action corrective manuelle nécessaire. Voir `DONE_DETAIL/TASK-094-tracabilite-dt-id-diagnostic-orphelins.md` et `TODO.md`. | 2026-07-14 |
| TASK-084 | **Ouverture du domaine « TVA Collectée » (règlements clients `MV_Domaine=0`)** dans le tunnel règlement-first, avec **distinction stricte** collectée/déductible. Branchement de `SensAffectation.Vente` dans le chemin de figeage réel (`DeclarationWorkflowService`), réutilisation du SQL miroir `domaineClient` (`SelectionnerAffectationsService`, TASK-015/021) aux mêmes garde-fous que les fournisseurs (motifs `SelectionExpliqueeEvaluator`, verrou `DT_Id`, date période TASK-062). Totaux séparés par domaine via `/checkup` (`recapSource`/`recapTaux`) — **jamais additionnés**. Encaissement client **exclu** du générateur XML Simpl-TVA (`DeclarationXmlExporter.cs:22`), export DGI hors périmètre permanent (confirmé PO). **Vérifié** (VERIFY re-soumis avec preuve data réelle sur `GR_EMA_DISTRIBUTION`) : déclaration `TVA1-2026-05`, client réel figé `FA2600407` + fournisseur `FF260057`, totaux distincts (collectée 3 063,59 / déductible 28 200,00), XML réel excluant `FA2600407` ; tests `GenererXml_ExclutEncaissementClient` + miroir Client `Evaluer_ClientEncaissement{Valid,NonRapproche,Impaye}_Task084` + `SelectionnerAffectationsVente_Task084` ; `Selection.Tests` 56/56, `Export.Xml.Tests` 5/5, build front 0 erreur. Réserve non bloquante : règlement client de démo à taux 0 %. Voir `DONE_DETAIL/TASK-084-ouverture-domaine-reglements-clients-tva-collectee.md`. | 2026-07-14 |
| TASK-088 | **Détail des avertissements « Ligne exclue »** (`declaration-tva-web/src/VerifierIntegrerPanel.tsx` mis à jour) : typage TS `Alerte` étendu pour inclure `code`, `refLigne` et `filtre` ; affichage de la référence de facture `refLigne` entre parenthèses pour chaque alerte correspondante (ex: `Ligne exclue (FC2501717) : ...`) ; drill-down direct via le bouton « Voir lignes » vers la `DomainGrid` filtrée réutilisant le mécanisme TASK-016. **Vérifié** (Playwright test `task088.spec.ts` vert, screenshots de l'avertissement et du drill-down générés sous `VERIFY/`). Voir `DONE_DETAIL/TASK-088-detail-avertissements-lignes-exclues.md`. | 2026-07-14 |
| TASK-087 | **Détail de l'écart + renommage du contrôle « Équilibre comptable » → « Cohérence des totaux déclarés »** (`declaration-tva-web/src/RecapSourceTable.tsx` nouveau ; `VerifierIntegrerPanel.tsx` Écran ④ + `DeclarationFinalePanel.tsx` Écran ⑤ mis à jour ; retargetée sur les écrans fusionnés post-TASK-090/091) : le contrôle bloquant n'affichait qu'un écart global (« Écart détecté : 368 517,56 MAD ») sans détail. **Extraction d'un composant partagé `RecapSourceTable`** (table Source/HT/TVA/TTC + total) à partir du bloc « Répartition par source » déjà présent sur ⑤ — un seul rendu, deux usages, plus de JSX dupliqué. Sur ④ « Vérifier & Intégrer » : montage du composant sous le badge du contrôle **uniquement en cas d'écart** (`c.id==='equilibre' && c.status==='error' && recapSource?.length>0`) → aucun changement visuel quand l'équilibre est OK. Libellé renommé « Cohérence des totaux déclarés » (acté PO parmi 4 options). `CheckupResult` front typé `recapSource` (déjà posé by TASK-090). Front-only, aucun endpoint touché, calcul de l'écart (`DeclarationsController.cs`) inchangé. **Vérifié** (revue architecte, source relue ↔ preuves) : `RecapSourceTable` défini une seule fois, utilisé sur ④ et ⑤ ; ancien libellé « Équilibre comptable » = 0 occurrence ; affichage conditionné sur `status==='error'` ; `tsc --noEmit` **0 erreur** ; **captures réelles** — ④ sur le cas PO (écart 368 517,56 MAD, libellé + ventilation par source visibles, `task087-step3-ecart.png`) et ⑤ sans régression (« Répartition par source » via le composant partagé + badge Équilibre OK, `task087-step5-no-regression.png`). Voir `DONE_DETAIL/TASK-087-detail-ecart-equilibre-comptable.md`. | 2026-07-14 |
| TASK-092 | **② Affectations → drill à la demande + barre finale 3 étapes** (`declaration-tva-web/src/DeclarationStepper.tsx` ; `AffectationsDrill.tsx` **inchangé**) : 3ᵉ et dernière des 3 fusions du tunnel (090 : ③+④ ; 091 : ⑤+⑥ ; 092 : ② en drill) → **tunnel final 3 étapes** « ① Sélection · ② Vérifier & Intégrer · ③ Déclaration ». « Affectations » retirée de la barre d'étapes ; le drill devient un plein écran à la demande ouvert depuis ① via un bouton « Détail des lignes » dans la barre du bas (actif dès qu'une sélection existe, **toujours** actif en mode intégré), avec retour « Retour à la sélection » vers ①. Redirection auto de `fetchInfo` : statut ≠ 0 → écran « Déclaration » (constat + exports), plus l'écran d'engagement. Bandeaux `readOnlyStep` adaptés (Sélection figée, drill en lecture). **Préservation TASK-077/078 garantie par construction** : `AffectationsDrill.tsx` non modifié (git : inchangé depuis TASK-055) et propagation `readOnly={readOnlyStep}` conservée — c'est ce mode intégré qui charge toutes les lignes et rend jouable le workflow valider/resynchroniser post-figeage. **Vérifié** (revue architecte, source relue ↔ VERIFY + captures) : `STEPS` = exactement 3 entrées (confirmé code + capture `05`) ; bouton « Détail des lignes » `disabled={!hasSelection && !integree}` (accessible avant/après intégration) ; drill plein écran avec « Retour à la sélection » (capture `06`) ; `fetchInfo` force `activeStep='declaration'` si `statut≠0` (e2e vérifie « Générer XML » visible post-intégration) ; build `tsc + vite` **0 erreur** ; e2e Playwright **2 passed**. **Réserve non bloquante** : pas de capture directe du badge incohérence sur le cas réel `FC2501717`/`EC_Id=21473` post-figeage (task « si rejouable » ; préservation structurelle par non-modification du drill). Voir `DONE_DETAIL/TASK-092-affectations-drill-a-la-demande-tunnel-3-etapes.md`. | 2026-07-14 |
| TASK-091 | **Fusion ⑤ Contrôle + ⑥ Synthèse → écran « Déclaration »** (`declaration-tva-web/src/DeclarationFinalePanel.tsx` nouveau, `DeclarationStepper.tsx` mis à jour, `ControleDeclarationPanel.tsx` renommé→`DeclarationFinalePanel.tsx` et `SynthesePanel.tsx` supprimé) : 2ᵉ des 3 fusions du tunnel (090 : ③+④ ; 091 : ⑤+⑥ ; 092 : ② en drill). Assemblage sans réécriture de logique des sections contrôle de ⑤ (répartition par source, vue par taux, anomalies bloquantes 🔴 / avertissements 🟠, drill « Voir lignes » `readonly`) et des exports Excel/XML de ⑥ (`POST /generation` + téléchargement blob inchangés) — **un seul** `GET /declarations/{id}/checkup` au chargement. **Retrait du bouton « Clôturer » mort par construction** (l'acte d'engagement unique a déjà lieu en ④ « Vérifier & Intégrer », TASK-090 ; triple verrou : `isUnlocked` statut≠0, `clotureDisabled`, garde back `EnCours`) et de tout son câblage front (`cloturer()`/`onClotured`/`cloturing`) — déplacement pur de JSX. Garde d'export conservée (`exportDisabled = hasBloquant || generating`). Stepper transitoire à **4 étapes** (`StepId` = `declaration`). **Vérifié** (revue architecte, source relue ↔ VERIFY) : `git grep "ControleDeclarationPanel\|SynthesePanel"` = **0 occurrence** dans `src/` ; un seul `api.get(.../checkup)` dans le panneau fusionné ; toutes les sections de ⑤ et exports de ⑥ présents ; build `npm run build` (tsc+vite) **0 erreur** + lint `oxlint` **0 erreur** (warnings préexistants seuls), rejoués par l'architecte ; e2e Playwright vert (2 passed). **Réserve non bloquante** : `onClotured` subsiste dans `WorkstationPanel.tsx`/`GenerationPanel.tsx`, fichiers **orphelins préexistants** (aucun import) hors périmètre de cette task. Voir `DONE_DETAIL/TASK-091-fusion-controle-synthese-declaration.md`. | 2026-07-14 |
| TASK-090 | **Fusion ③ Calcul + ④ Intégration → écran « Vérifier & Intégrer »** (`declaration-tva-web/src/VerifierIntegrerPanel.tsx` nouveau, `DeclarationStepper.tsx` mis à jour, `CalculTvaPanel.tsx` et `IntegrationPanel.tsx` supprimés) : fusion des deux écrans du tunnel en un seul écran de vérification des taux de TVA et d'engagement (`POST /cloture`). Suppression du pipe d'état session `calcTVA`/`onCalcSummary` (cause racine du récap vide TASK-086, absorbée ici). `RecapCard` rendu robuste au rechargement de page (F5) et chargement direct par lecture de la réponse `/checkup` (`recapSource`/`recapTaux`). **Vérifié** : build `npm run build` (tsc+vite) vert (0 erreur), Playwright E2E vert avec tous les tests passés. Voir `VERIFY/TASK-090_verify.md`. | 2026-07-14 |
| TASK-089 | **Suppression de `SummaryPanel.tsx` (code mort)** (`declaration-tva-web/src/SummaryPanel.tsx` supprimé, `CalculTvaPanel.tsx` mis à jour) : suppression définitive du composant de récapitulatif par taux TVA non câblé dans le tunnel réel de déclaration. Commentaire obsolète de principe dans `CalculTvaPanel.tsx` nettoyé. `mockData.ts` conservé car toujours importé by `ControlGrid.tsx` (également non branché mais hors périmètre). **Vérifié** : `git grep SummaryPanel` = 0 occurrence dans le code source ; build `tsc` + `vite build` vert (0 erreur). Voir `VERIFY/TASK-089_verify.md`. | 2026-07-14 |
| TASK-085 | **Colonne « Total TTC » sur le tableau des sous-totaux TVA** (`declaration-tva-web/src/CalculTvaPanel.tsx`, écran ③ Calcul TVA) : demande PO (capture écran) — ajouter `Total TTC = Total HT + Total TVA` sur chaque ligne de taux et sur `Σ Total`. **Implémenté** : 3 emplacements strictement conformes au périmètre (entête `<th>`, `<td>` par taux `formatMoney(st.totalHT + st.totalTVA)`, `<td>` `Σ Total` `formatMoney(totalHT + totalTVA)`), même style que la colonne Total TVA (poids 700, tabular-nums). Front-only, aucune donnée nouvelle, `sousTotauxParTaux`/`agregParFactureTaux` et le back non touchés. **Vérifié** : diff = exactement 3 insertions dans `CalculTvaPanel.tsx` (aucune suppression, aucun autre fichier métier) ; tableau de preuve chiffrée (déclaration réelle juin 2026, `TTC=HT+TVA` exact sur 20 %/10 %/0 %/`Σ Total`) ; capture d'écran `10-synthese-overview.png`. Voir `DONE_DETAIL/TASK-085_verify.md`. | 2026-07-14 |
| TASK-083 | **Thème visuel sobre + touche couleur signature « Déclaratif Maroc »** (`declaration-tva-web/src/index.css`, `App.tsx`) : substitution de la teinte d'accent générique (bleu Material `#1976d2`) par une teinte signature indigo profond via les 5 variables CSS globales (`--accent-primary` `#2b4c7e`, `--accent-secondary` `#3d6099`, `--accent-hover` `#1f3a63`, `--sidebar-active-bg` `#e8ecf3`, `--sidebar-active-text` `#2b4c7e`) — toutes les autres variables (`--bg-*`, `--text-*`, `--border-color`, `--danger/success/warning`, ombres, radius, transitions) inchangées. Ajout d'un monogramme « DM » (span CSS, pas d'image) en en-tête de sidebar entre l'icône `LayoutDashboard` et le libellé `TVA` (branche `isSidebarOpen` uniquement → replié proprement en mode `collapsed`, icône intacte). `Auth.tsx` non modifié (le bouton hérite de `var(--accent-primary)`). **Zéro écran dense retouché** : seuls `index.css` + l'en-tête `App.tsx` touchés → régression de layout structurellement impossible (aucun code de grille modifié). `grep` `#1976d2\|#2196f3\|#1565c0\|#e3f2fd` = **0 résidu en dur**. Build front `tsc -b && vite build` **0 erreur** (warning `INEFFECTIVE_DYNAMIC_IMPORT` préexistant, hors sujet). **Réserve non bloquante** : captures visuelles avant/après laissées au PO ; teinte signature ajustable dans le même tableau sans impact structurel. Voir `DONE_DETAIL/TASK-083-...` (VERIFY archivé). | 2026-07-14 |
| TASK-093 | **Renommage du titre de la page d'authentification en « Déclaratif Maroc »** (`declaration-tva-web/src/Auth.tsx:79`, `declaration-tva-web/index.html:7`) : le titre affiché était « Déclaration TVA » alors que la TVA n'est qu'un premier module d'une plateforme appelée à en accueillir d'autres (roadmap R1 RAS / R2 Télé-Déclaration / R3 Tableau de bord) — le nom de produit doit être distinct du nom de module. **Implémenté** (front-only, texte seul, aucune logique touchée) : `<h1 className="auth-title">` → « Déclaratif Maroc » ; `<title>` de l'onglet (`declaration-tva-web` → « Déclaratif Maroc »). Sous-titre générique conservé ; sidebar post-connexion (`App.tsx:133`, `<span>TVA</span>` = nom du **module actif**) **non touchée** (hors périmètre, distinction produit/module préservée). **Renumérotée depuis 082** à l'archivage (n° déjà pris par le bandeau incohérence ci-dessous). **Vérifié** : grep « Déclaration TVA » sur `Auth.tsx`+`index.html` = 0 après correctif ; build front (`tsc -b && vite build`) 0 erreur (seul warning Vite `INEFFECTIVE_DYNAMIC_IMPORT` préexistant, sans lien) ; **confirmé visuellement par le PO** (capture réelle `localhost:5173` : « Déclaratif Maroc » onglet + carte de connexion). Implémentée en rôle worker exceptionnel (demande explicite PO). Voir `DONE_DETAIL/TASK-093_verify.md`. Suivi : TASK-083 (thème visuel sobre + couleur signature, même fichier `Auth.tsx` en zone disjointe) reste ouverte. | 2026-07-14 |
| TASK-082 | **Bandeau incohérence absent en ② Affectations pour une ligne `Exclue` dès le figeage** (`Declaration.Application/Services/DeclarationWorkflowService.cs`, méthode `RevaliderLignesFigeesAsync`) : cas réel `FC2501717`/`EC_Id=21473` — l'incohérence Sage (Σ(HT+TVA)≠TTC) est détectée **pendant** la lecture du cache au figeage (`OrchestrateurDeclaration.cs:427-449`), la ligne est créée directement en `Etat=Exclue` (jamais `Proposee`). `RevaliderLignesFigeesAsync` filtrait `Etat==Proposee\|\|Integree`, excluant structurellement ces lignes du contrôle d'incohérence — le badge ② restait silencieux alors que Synthèse/Contrôle affichait déjà le motif via l'alerte indépendante `LIGNE_EXCLUE` (`GetCheckupAsync`), d'où l'écart observé entre écrans. **Implémenté** : lignes `Exclue` avec `EC_Id>0` non encore validées (`IncoherenceValidee`) désormais incluses dans le contrôle `LIGNE_FIGEE_A_REVERIFIER` (sans contrôle MV_Point, non pertinent pour une ligne jamais déclarée) ; aucune ligne/valeur figée modifiée. Découvert et corrigé dans le prolongement direct de TASK-081 (même signalement PO, bandeau ② toujours absent après le correctif 081 seul). Nouveau test `Task082LigneExclueDesLeFigeageTests` (3 tests, reproduit exactement le cas réel) + 105 existants → **108/108** `Declaration.Orchestration.Tests` verts, 0 régression. **Confirmé en réel par le PO** après rebuild/redémarrage de l'API (bandeau visible dès le premier chargement de ②). | 2026-07-14 |
| TASK-081 | **Bandeau incohérence absent au premier figeage d'une déclaration** (`Declaration.Application/Services/DeclarationWorkflowService.cs`, `ChargerCandidatesSiNecessaireAsync`) : `ChargerCandidatesSiNecessaireAsync` retournait inconditionnellement `Array.Empty<Alerte>()` après le tout premier figeage, alors que la sentinelle `CodeTaxe='ERREUR'` venait d'être écrite en cache par ce même appel — seule la branche « déjà figé » (`RevaliderLignesFigeesAsync`) la relisait. Reproduit sur toute déclaration recréée (TASK-079) ou nouvellement créée. **Implémenté** : appel à `RevaliderLignesFigeesAsync` juste après `SaveLignesCandidatesAsync`, avant le retour, au lieu du tableau vide — même détection réutilisée, aucune règle dupliquée, lecture seule stricte préservée. Nouveau test `Task081PremierFigeageBandeauTests` (2 tests) → 105/105 `Declaration.Orchestration.Tests` verts. **Réserve levée** : le correctif seul ne suffisait pas pour le cas réel signalé (ligne `Exclue` dès le figeage, hors périmètre de ce filtre) → complété par TASK-082. Voir `VERIFY/TASK-081_verify.md` (archivé). | 2026-07-14 |
| TASK-080 | **Exclusivité d'un règlement/affectation entre déclarations `EnCours`** (`Declaration.Selection/SelectionExpliqueeModels.cs`, `Declaration.Application/Interfaces/IDeclarationRepository.cs`, `Declaration.Infrastructure/Repositories/DeclarationRepository.cs`, `Declaration.Application/Services/DeclarationWorkflowService.cs`) : rien n'empêchait qu'un même règlement soit sélectionné et marqué `Integree` dans deux déclarations `EnCours` simultanées — la seule exclusion existante (`DejaDeclare`/`DT_Id`) n'est posée qu'à la clôture (TASK-028), trop tard. **Implémenté** : nouveau motif `MotifRejet.DejaEnCoursAilleurs` posé en aval de l'évaluateur par `DeclarationWorkflowService.AppliquerExclusiviteInterDeclarationAsync` (compare, via `GetConflitsAutreDeclarationAsync`, les clés `NumeroFacture`/`NumeroRapprochement` déjà `Proposee`/`Integree` dans une **autre** déclaration de la société — base de persistance uniquement, aucun JOIN cross-base) ; message honnête référençant le numéro de la déclaration concurrente. **Réintégration automatique** : `RevaliderLignesFigeesAsync` (TASK-077) détecte qu'un conflit s'est résolu (déclaration concurrente supprimée TASK-079, ou ligne changée d'état) et rejoue le pipeline complet de figeage pour réintégrer le règlement libéré (alerte `REGLEMENT_LIBERE_REINTEGRE`), plutôt que de rebasculer l'état d'une ligne sans valorisation. **Bug latent trouvé et corrigé en cours de preuve** : `CreerDeclarationAsync` générait un `Numero` ignorant le `Type` — deux déclarations valides (`Type` différent, même période) produisaient donc le même `Numero` et l'`INSERT` échouait sur la contrainte unique, rendant **impossible à créer** le scénario même que cette task devait couvrir ; corrigé par un suffixe `-T` réservé à `Trimestrielle` (aucun impact sur les `Numero` existants). Implémentée en rôle worker exceptionnel (demande explicite PO). **Preuves réelles rejouées contre `.\sql2022`/`GR_EMA_DISTRIBUTION`** (harnais jetable, supprimé après usage) : exclusion posée (548 lignes exclues face à la déclaration réelle `TVA1-2026-01`, 1270 lignes vérifiées inchangées avant/après) et réintégration effective (360 lignes réintégrées après suppression de la déclaration concurrente, alerte confirmée, 0 résidu) — base restaurée à l'identique en fin de preuve. Build solution 0 erreur CS, `Declaration.Orchestration.Tests` 103/103 (4 nouveaux `Task080ExclusiviteInterDeclarationTests`), aucune régression sur `DejaDeclare`. Voir `VERIFY/TASK-080_verify.md`. | 2026-07-14 |
| TASK-079 | **Suppression d'une déclaration `EnCours` + enrichissement écran liste** (`Declaration.API/Controllers/DeclarationsController.cs`, `Declaration.Application/Services/DeclarationWorkflowService.cs`, `Declaration.Application/Interfaces/IDeclarationRepository.cs`, `Declaration.Infrastructure/Repositories/DeclarationRepository.cs`, `declaration-tva-web/src/DeclarationList.tsx`, `Auth.tsx`, `App.tsx`) : aucune suppression physique n'existait ; le tampon `DT_Id` (TASK-028/064) n'étant posé qu'à la clôture, « libérer » une déclaration `EnCours` se résume à supprimer l'entête `DM_ENTTVA` + ses lignes `DM_LGTVA` (aucune écriture `RT_AFFECTATION`/`RT_MOUVEMENT`). **Décision PO 13/07** : réservé `UT_Admin=1` (même garde que TASK-073), traçabilité `JournaliserAudit` réutilisée, déclaration `Cloturee` non supprimable directement (chemin obligatoire = réouverture TASK-073 puis suppression séparée). Ajout d'un **garde-fou défensif** (`CountAffectationsTamponneesAsync`) qui vérifie plutôt que suppose l'absence de tampon `DT_Id` avant toute suppression physique. Écran liste enrichi de 2 colonnes (Lignes/Montant TVA, agrégat 1 requête `GROUP BY`, pas de N+1) + bouton Supprimer (visible `EnCours`+admin) avec modale de confirmation dédiée (pas de `window.confirm`). Claim `UT_Admin` désormais propagé jusqu'au front (`User.isAdmin`), jusqu'ici absent côté front bien que porté par le JWT depuis TASK-074. Implémentée en rôle worker exceptionnel (demande explicite PO). **Vérifiée par build solution complète (0 erreur) + suite de tests complète rejouée** (`Declaration.Orchestration.Tests` 99/99, `Declaration.Core.Tests` 32/32, `Declaration.Export.Xml/Excel.Tests` verts ; 2 échecs pré-existants non liés au diff, modules non touchés) + front (`tsc`/`vite build`/`oxlint` 0 erreur). **Réserve non levée, à traiter avant confiance totale** : aucune preuve réelle en environnement (base de données) dans cette session — les 3 preuves exigées par la TASK (403 non-admin, 400 sur `Cloturee`) restent à rejouer par le PO sur `GR_EMA_DISTRIBUTION`. **Mise à jour 2026-07-14** : la preuve « suppression réelle par un admin » a depuis été exercée en pratique contre `GR_EMA_DISTRIBUTION` — d'abord par le PO lui-même via l'écran (suppression de `TVA1-2026-02`/`TVA1-2026-05`), puis à plusieurs reprises par le harnais de preuve TASK-080 (`SupprimerDeclarationAsync` sur des déclarations `EnCours` jetables, succès à chaque fois) ; seuls les cas 403/400 restent non rejoués. **Complément** : TASK-080 (exclusivité règlement entre déclarations `EnCours` concurrentes) ✅ **livrée et approuvée** (2026-07-14, voir ci-dessus). | 2026-07-13 |
| TASK-078 | **Workflow de décision sur une incohérence signalée** (écran ② Affectations, suivi direct de TASK-077) : demande PO en testant 077 en réel — « il faut se mettre à la place d'un comptable [...] il attend que le système lui remonte tout seul l'incohérence ». **Implémenté** : bandeau plein-largeur automatique « N ligne(s) incohérente(s) » (aucune recherche requise), clic → filtre sur ces seules lignes, 2 actions par pièce signalée — **Valider l'incohérence** (décision tracée qui/quand via `IncoherenceValidee/Par/Le`, migration `007`, aucune ligne/total modifié) et **Corriger/Resynchroniser** (relecture Sage réelle ciblée sur l'`EC_Id`, endpoints `POST /{id}/lignes/valider-incoherence`/`resynchroniser`). **Bug trouvé et corrigé en relecture de code** (avant toute confirmation visuelle) : `ResynchroniserLigneAsync` appelait l'orchestrateur sans purger le cache — une sentinelle ERREUR portant déjà des montants bruts capturés (TASK-076) aurait été traitée comme définitivement réglée par `TryServireDepuisCache`, rendant le bouton « Resynchroniser » sans aucun effet réel sur le cas `EC_Id=21473`. Corrigé par une nouvelle méthode `IVentilationSageCacheRepository.SupprimerEntrees` (purge sans réécrire), appelée avant la relecture. Implémentée en rôle worker exceptionnel (demande explicite PO). **Vérifiée par relecture de code exhaustive plutôt que par confirmation visuelle du PO** (contrairement à TASK-076/077) — build 0 erreur, 99/99 tests (`Declaration.Orchestration.Tests`). **Réserve non bloquante, à lever au fur et à mesure de l'usage réel** : bandeau/filtre/actions non encore cliqués sur un cas réel après ce dernier correctif ; aucun test unitaire dédié aux 2 nouveaux endpoints eux-mêmes (seule la gate de non-resignalement est couverte). Voir `VERIFY/TASK-078_verify.md`. | 2026-07-13 |
| TASK-077 | **Purge/recalcul des lignes déjà figées avant le garde-fou TASK-072** (`Declaration.Application/Services/DeclarationWorkflowService.cs`, `Declaration.Infrastructure/Repositories/DeclarationRepository.cs`) : `DM_LGTVA` était un instantané figé une seule fois (`ChargerCandidatesSiNecessaireAsync`, garde `count>0`), jamais revalidé après figeage — cas réel PO (déclaration TVA1-2026-01) : `EC_Id=21473`/`FC2501717` toujours visible avec TVA déclarée (344 050,24 MAD) très supérieure au TTC (20 700,00 MAD), figée avant TASK-072. **Décisions PO actées** : revalidation automatique à chaque `GET /{id}/lignes` (même déjà figée, pas de bouton manuel) ; périmètre = incohérence Sage (cache `ERREUR`) + règlement dépointé (`MV_Point`) ; action = signaler seulement (`LIGNE_FIGEE_A_REVERIFIER`, Warning), aucune ligne/total modifié. Ajout `LigneCandidate.EC_Id`/`MV_Id` (migration `006_DM_LGTVA_EcId_MvId.sql`) + **auto-guérison** des lignes figées avant cette migration (backfill automatique par sélection SQL lecture seule, jamais de relecture Sage, jamais d'action manuelle requise). Alerte remontée à la fois en étape ② (`GET /lignes`) et ④ (`GET /checkup` — trou trouvé en testant en réel : sans ce second câblage le montant gonflé passait silencieusement jusqu'à l'intégration). **2 bugs distincts découverts en testant en réel, corrigés dans le même diff** : (1) front lisait le mauvais champ API pour le contrôle d'équilibre (`controleEquilibre` au lieu d'`equilibre`) → badge « BLOQUANT » systématique peu importe l'état réel ; (2) crash `SqlTypeException` non géré sur date invalide transitoire (input date natif) dans `FacturesController` → garde back (400 explicite) + garde front. Implémentée en rôle worker exceptionnel (demande explicite PO). **Suivi ouvert** : TASK-078 (workflow décision PO complet — bandeau/filtre/valider/resynchroniser sur ② Affectations), implémentée dans la même session mais non encore documentée/clôturée formellement. Preuve réelle : bandeau vu à l'écran par le PO pour `EC_Id=21473` ; build 0 erreur ; 99/99 tests (`Declaration.Orchestration.Tests`, dont 4 nouveaux `Task077RevalidationLignesFigeesTests`) ; `tsc --noEmit` 0 erreur. Voir `VERIFY/TASK-077_verify.md`. | 2026-07-13 |
| TASK-076 | **Visibilité des montants bruts Sage sur une facture exclue pour incohérence HT/TVA/TTC** (écran Factures) — suite TASK-072 : une pièce exclue (sentinelle `Taux=-1, CodeTaxe='ERREUR'`) n'affichait que le motif texte, montants `NULL`. **Décision PO** : périmètre = montants bruts Sage (HT/TVA/Parafiscale/TTC tels que lus avant exclusion) **et** identification de pièce (déjà couverte par la famille A existante). Capture au moment de la détection (`SageTaxReaderService.cs`), persistance sur la sentinelle (`GRC_VENTILATION_SAGE_CACHE.BrutHT/BrutTva/BrutParafiscale/BrutTtc`, migration `005`), jamais mêlée aux colonnes valorisées déclarables. **Affichage évolué en cours de test réel** (demande PO : « je dois voir l'anomalie directement sur la grille, pas cliquer ») : la valeur brute s'affiche directement dans les cellules Total HT/TVA/Autre taxe (grisée + icône ⚠), pas seulement dans le panneau détail. **3 correctifs de fond découverts en testant sur le cas réel `EC_Id=21473`/`FC2501717`** : (1) une sentinelle ERREUR écrite avant ce correctif restait figée sans bruts indéfiniment (`TryServireDepuisCache`, fix auto-cicatrisant une seule relecture) ; (2) une écriture normale ne purgeait jamais une sentinelle ERREUR résiduelle du même `EC_Id` → coexistence de 2 lignes cassant les invariants de lecture (`VentilationSageCacheRepository.UpsertEntries`, purge ajoutée) ; (3) auto-guérison de l'état hérité (2 lignes déjà coexistantes en base), sans dépendre de l'ordre de lecture SQL. Implémentée en rôle worker exceptionnel (demande explicite PO). Preuve réelle : requête SQL confirmant une ligne unique avec bruts capturés (`BrutHT=1 720 251,20`, `BrutTva=344 050,24`, `BrutTtc=20 700,00`) ; PO confirme visuellement la valeur brute dans la grille (pas seulement le détail) ; build 0 erreur ; 99/99 tests. Voir `VERIFY/TASK-076_verify.md`. | 2026-07-13 |
| TASK-075 | **Relecture vide des étapes ② Affectations / ③ Calcul depuis ④ Intégration** (`declaration-tva-web/src/AffectationsDrill.tsx`, `CalculTvaPanel.tsx`, `DeclarationStepper.tsx`) : remarque PO (13/07/2026) — après réouverture/rechargement, ④ Intégration s'affiche mais ②/③ apparaissent vides avec le placeholder « aucun règlement sélectionné », trompeur pour une déclaration qui a bien des données figées. **Cause confirmée** : `selectedRows`/`calcTVA` (state local `DeclarationStepper.tsx:57-60`) ne sont jamais réhydratés depuis l'API quand `fetchInfo` bascule directement sur `integration` pour une déclaration déjà intégrée — les deux composants dépendent exclusivement de ce state, jamais peuplé hors traversée manuelle ①→②→③ en session. **Correctif** : nouveau prop `readOnly` sur les deux composants (branché sur `readOnlyStep`, déjà calculé dans `DeclarationStepper.tsx`) ; en mode `readOnly`, chargement de **toutes** les lignes de la déclaration via `GET /declarations/{id}/lignes` par `declarationId` seul (endpoint déjà existant, sans filtre de règlement) — **aucun nouvel endpoint back nécessaire** (garde-fou 3 de la task vérifié). Garde « rien sélectionné » (placeholder « retournez à l'étape ① ») limitée au parcours normal (`!readOnly`) ; nouveau garde distinct « aucune affectation/ligne trouvée pour cette déclaration » si le chargement en relecture retourne réellement zéro ligne (périmètre B de la task). **Aucune ligne de `ConstructeurDeclaration.cs`/logique de calcul TVA touchée** (périmètre A uniquement — restitution front). Implémentée en rôle worker exceptionnel (demande explicite PO). **Vérification limitée à `tsc --noEmit` + `npm run build` (0 erreur)** — **pas de preuve réelle en environnement navigateur/backend** (aucun back/DB disponible dans cette session), contrairement au standard habituel de ce journal. **Réserve non levée, à traiter avant confiance totale** : à rejouer par le PO sur une vraie déclaration intégrée pour confirmer visuellement que ②/③ affichent les données réelles. Périmètre C (cohérence avec TASK-073 réouverture) non traité — non bloquant, à la discrétion du PO. | 2026-07-13 |
| TASK-074 | **Authentification réelle GRF (`P_UTILISATEUR`/`P_SOCUTILISATEUR`), remplacement du mock** (`Declaration.API/Controllers/AuthController.cs`, `Declaration.API/Controllers/DeclarationsController.cs`, `Declaration.API.csproj`) : `POST /api/auth/login` vérifiait jusqu'ici n'importe quel login avec le mot de passe en dur `"admin"`, JWT à rôle statique unique. Remplacé par une vérification réelle contre `P_UTILISATEUR` en réutilisant le hashage legacy déjà validé dans `GRC_WEB` (`Tresorerie.Infrastructure.PasswordHasher` contre `UT_Hash`/`UT_Salt`, `Tresorerie.Dapper.UtilisateurRepository`) — jeu **volontairement minimal** de 4 DLL (`Tresorerie.Core`/`.Dapper`/`.Infrastructure` + `DapperExtensions`), sans le reste de l'arbre Trésorerie (Sage/DevExpress/Ninject) pour ne pas coupler `Declaration` à `GRC_WEB`. Le JWT porte désormais `UT_Id`, `UT_Admin` et `Societes` (CSV des `SO_Id` autorisés via `P_SOCUTILISATEUR`, vide si `UT_Admin=1`). **Garde société ajoutée** sur `GET /api/declarations` (`EstSocieteAutorisee`) : un utilisateur non-admin ne peut plus interroger que les sociétés listées dans son token — auparavant `societeId` en query string donnait accès à n'importe quelle société. Implémentée en rôle worker exceptionnel (demande explicite PO, TASK-074 prérequis dur de TASK-073). **Preuve réelle** (pas mockée) sur `GR_EMA_DISTRIBUTION`/`.\sql2022` : login réel avec le compte `Admin`/`Admin` existant (`UT_Admin=1`) → JWT correct (`UT_Id=1`, `UT_Admin=1`) ; mot de passe erroné → 401 sans exception (chaîne de dépendances legacy chargée et exécutée sans crash runtime) ; `GetAll` sans token → 401, avec token admin → 200 ; avec un JWT non-admin forgé pour test (faute d'un 2ᵉ compte réel en base) restreint à `SO_Id=1` → société 2 refusée (403), société 1 acceptée (200). **Réserve non bloquante** : le test du chemin non-admin s'appuie sur un JWT forgé (signé avec la même clé) plutôt qu'un vrai compte `P_UTILISATEUR` non-admin — aucun n'existe en base de dev ; à rejouer avec un compte réel si le PO en fournit un. | 2026-07-13 |
| TASK-073 | **Gouvernance et traçabilité de la réouverture d'une déclaration clôturée** (`Declaration.API/Controllers/DeclarationsController.cs`, `Declaration.Application/Services/DeclarationWorkflowService.cs`) : le verrou d'intégration (TASK-028/064) et la réouverture (`ReouvriDeclarationAsync`, `DT_Id→NULL`) fonctionnaient techniquement mais sans garde de rôle ni audit — décision produit tranchée par le PO (13/07/2026) : réservé aux utilisateurs `UT_Admin=1`, sans motif obligatoire. **Périmètre A** : `POST /{id}/reouverture` renvoie désormais 403 si le token ne porte pas `UT_Admin=1` (dépend de TASK-074, livrée avec cette task). **Périmètre B** : `ReouvriDeclarationAsync` journalise qui/quand/quelle déclaration dans `logs/audit.log` — réutilise le **même mécanisme** (ILogger + fichier texte dans `logs/`) que `JournaliserValorisation` déjà existant, **aucune nouvelle table** introduite (aucun équivalent d'audit n'existait déjà pour la clôture, contrairement à l'hypothèse du garde-fou de la task). Le mécanisme technique de verrou/dé-tamponnage (`003_Verrou_DT_Id.sql`) n'a pas été touché (confirmé par `git diff` limité aux fichiers auth/API/gouvernance). Implémentée en rôle worker exceptionnel (demande explicite PO), après TASK-074 (prérequis dur signalé par l'architecte avant tout développement). **Preuve réelle** sur `GR_EMA_DISTRIBUTION` : réouverture d'une déclaration clôturée réelle (`TVA1-2026-01`) par le compte admin → HTTP 204 + entrée `audit.log` (`[REOUVERTURE] Déclaration TVA1-2026-01 (...) rouverte par 'Admin'.`) ; tentative avec un JWT non-admin forgé pour test → 403. **Réserve non bloquante** : même limite que TASK-074 (pas de compte non-admin réel disponible pour le test #1 du VERIFY). **Périmètres C (exposition UI) et D (test d'intégration automatisé) non traités** — non bloquants, à la discrétion du PO. | 2026-07-13 |
| TASK-072 | **Incohérence Σ(HT+TVA) vs Σ(montant rapproché) à l'écran ③ Calcul TVA** (cas PO 68 règlements — 1 193 158,70 MAD rapproché vs HT 2 748 914,90 + TVA 488 087,05 affichés, ×2,7) : piste initiale (doublon front `CalculTvaPanel.tsx` par agrégation `numeroReglement`) **infirmée avec preuve réelle** (`RT_MOUVEMENT.MV_Numero` unique/non-blanc sur les 1349 mouvements de la période). **Cause racine réelle** tracée en base : incohérence de donnée **Sage elle-même** (pièce `FC2501717`, `EC_Id=21473`) — `DO_TotalHT`/`DO_TotalTTC` incohérents à la source ERP (Σ(HT+TVA)=2 064 301,44 ≠ TTC=20 700,00, écart ×99,72), expliquant 99,99 % de l'écart signalé par le PO. **Correctifs** (`SageTaxReader/SageTaxReader.Contracts/DTOs.cs` + `SageTaxReader.Core/SageTaxReaderService.cs:363-369`) : garde-fou `IncoherenceHtTvaTtc.EstIncoherent` (seuil combiné **ET** >5 MAD **ET** >1 % du TTC, tolère les écarts d'arrondi légitimes) posé à la lecture OM ; contrôle croisé indépendant TTC Sage vs `RT_ECHEANCE.EC_MtDevise` (`Declaration.Orchestration/OrchestrateurDeclaration.cs:254-303`, demandé par le PO) ; colonne `MotifErreur` (migration additive idempotente `004_Cache_Ventilation_MotifErreur.sql`) matérialisant une pièce exclue en sentinelle unique (`Taux=-1, CodeTaxe='ERREUR'`), jamais mêlée aux buckets réels ; **revalidation rétroactive** à la lecture du cache (`TryServireDepuisCache`, `OrchestrateurDeclaration.cs:314-374`) avec auto-guérison — sans ce point, une pièce déjà en cache avant le correctif (cas réel `EC_Id=21473`, payé/pointé, jamais dépointé) serait restée servie indéfiniment. Motif rendu visible à l'écran Factures (`FactureInterrogationRow.AppliquerCacheB`, motif spécifique prioritaire sur le motif générique « OM non lue »), sur demande complémentaire du PO. **Correction rétroactive appliquée en base réelle** (`GR_EMA_DISTRIBUTION`, `EC_Id=21473` remplacé par la sentinelle) — confirmée par capture d'écran réelle du PO sur l'écran Factures après le premier BLOQUÉ (absence de trace versionnée de l'écriture/accord). **1er VERIFY non traité comme un APPROVE normal** : rédigé par la même partie qui a codé (exception ponctuelle PO, hors rôle normal architecte), transparence explicite en en-tête ; **1 point BLOQUÉ levé** après confirmation PO sur l'écran réel. Tests : 90/90 (`Declaration.Orchestration.Tests`, dont `IncoherenceHtTvaTtcTests` T10-T14 + IT-7/IT-8 reproduisant les montants réels sur SQL Server réel) + 32/32 (`Declaration.Core.Tests`, dont 4 `IncoherenceHtTvaTtcTests`), tous vérifiés réellement rejoués (pas seulement déclarés). **Limite documentée, non silencieuse** : le contrôle croisé `EC_MtDevise` ne s'applique qu'à la reconstruction de déclaration, pas à `TryServireDepuisCache` — suivi si un futur cas n'est détectable que par ce contrôle en lecture cache pure. **Suivi ouvert** : TASK-076 (visibilité des montants bruts Sage sur une facture exclue, demande PO en cours de test). | 2026-07-13 |
| TASK-071 | **Déblocage intégration — `EtatLigne.Integree` jamais posé (deadlock ④↔⑤)** (`Declaration.Application/Services/DeclarationWorkflowService.cs`) : `EtatLigne.Integree` n'était posable que via l'écran ⑤ Contrôle (`DomainGrid.tsx`), lui-même verrouillé tant que ④ n'est pas confirmé — deadlock total, aucune déclaration ne pouvait jamais être close. `GetCheckupAsync` traite désormais les lignes `Proposee` comme éligibles (au même titre que `Integree`) pour le contrôle d'équilibre et les alertes bloquantes (`AUCUNE_LIGNE_INTEGREE`, `TIERS_SANS_ICE`) — débloque le pré-flight ④ (`GET /checkup` appelé par `IntegrationPanel.tsx` avant tout clic). `CloturerDeclarationAsync` valide d'abord (aucune écriture si rejet — pas de transaction SQL explicite dans le repository), puis transitionne les lignes `Proposee` éligibles vers `Integree` via `UpdateLignesEtatBulkByIdsAsync` (transition existante, aucune duplication de logique), avant `UpdateStatutAsync`/tamponnage `DT_Id` (TASK-028, inchangé). Écart documenté vs décision par défaut de la TASK (validation avant écriture plutôt que l'inverse), justifié par l'absence de transaction. Implémentée en rôle worker exceptionnel (demande explicite PO). **1er VERIFY REJETÉ** : preuve réelle absente, build `Declaration.API` non vérifié (process de dev verrouillant les DLL), verrou DT_Id/trigger d'immuabilité non re-prouvés. **Corrigé et rejoué en conditions réelles** : build solution complète (`Declaration.API` inclus) 0 erreur ; rejeu réel sur `GR_EMA_DISTRIBUTION` (déclaration `TVA1-2026-01`, 1270 lignes réelles — volume supérieur au cas PO 68/245) — `GET /checkup` sans alerte bloquante puis `POST /cloture` → HTTP 204, lignes `Proposee`→`Integree` confirmées en base (27 `Exclue`/3 `Reportee` inchangées) ; tampon DT_Id vérifié (555 affectations réelles tamponnées, DT_Id unique dérivé) ; trigger d'immuabilité (TASK-064) re-testé sur ces affectations réelles — UPDATE, DELETE et dérapprochement (`MV_Point`) tous refusés avec le message attendu. 3 tests unitaires ajoutés (`Task071DeblocageIntegrationTests.cs`), suite complète 83/83. **Point non bloquant relevé, hors périmètre** : `DM_ENTTVA.DateCloture` reste `NULL` après clôture (`UpdateStatutAsync` ne renseigne que `Statut`, défaut pré-existant). | 2026-07-13 |
| TASK-070 | **Allégement de l'écran ③ Calcul TVA (retrait tableau détail facture, redondant avec ②)** (`declaration-tva-web/src/CalculTvaPanel.tsx`) : retrait du bloc « Tableau principal » (détail facture par ligne, doublon du détail déjà disponible en ② Affectations avec colonne Taux filtrable) — l'écran ③ ne conserve que « Sous-totaux par taux TVA » + le bloc « lignes non valorisées — détail motif » (transparence traçabilité) + le bandeau pied total (inchangé, option A TASK-069 reconduite). **Aucun changement de logique** : `agregParFactureTaux`/`sousTotauxParTaux`/`onCalcSummary` strictement identiques, seul du JSX d'affichage retiré ; `rows` reste calculé en interne (alimente sous-totaux/bandeau/motifs). Corrige au passage le bug de visibilité révélé au VERIFY de TASK-069 (le bloc sous-totaux n'est plus écrasé, il n'y a plus rien pour l'écraser). Nettoyage forcé de `CheckCircle2` (import lucide-react) et `statutValoLabel` devenus morts (`noUnusedLocals: true`). Réalisée en rôle worker exceptionnel (demande explicite PO, confirmation obtenue avant exécution). **Vérifié par l'architecte** : diff relu (périmètre strictement JSX, aucune fonction de calcul touchée), build `npm run build` (tsc+vite) 0 erreur, **capture d'écran réelle PO** sur le volume cas réel (68 règlements / 245 lignes) confirmant l'écran allégé (sous-totaux 20/10/9/0 % + bandeau pied 488 087,05 MAD, aucun scroll nécessaire, tableau détail absent). | 2026-07-13 |
| TASK-069 | **Correctif mise en page écran ③ Calcul TVA (bandeau pied rogné)** (`declaration-tva-web/src/DeclarationStepper.tsx:155`) : ajout de `minHeight: 0` (+ `display:'flex', flexDirection:'column'`) sur le conteneur d'étape partagé (`flex:1, overflow:'hidden'`), qui ne transmettait pas la contrainte de hauteur à `CalculTvaPanel` (anti-pattern flexbox classique : un enfant `flex:1` sans `min-height:0` ne rétrécit pas sous son contenu). Décision PO 13/07 **option A** : `CalculTvaPanel.tsx` inchangé (bandeau pied interne conservé tel quel). Correctif générique, partagé par les 6 étapes du stepper, aucun changement back/API/SQL. Build front vert (0 erreur tsc, bundle identique). **Vérifié par l'architecte** : lecture directe du code confirmant le correctif exact, rebuild rejoué (0 erreur), **preuve visuelle produite par le PO après relance** (capture réelle : bandeau « Total TVA à intégrer » et barre stepper tous deux visibles sans recouvrement). **1er VERIFY rejeté** : soumis sans capture d'écran alors que la TASK l'exigeait explicitement (environnement sans navigateur) — re-testé et confirmé par le PO avant approbation. **A mis en évidence un second bug, hors scope de cette TASK** : le bloc « Sous-totaux par taux TVA » (`CalculTvaPanel.tsx:366-402`), partageant la même zone de scroll que le tableau détail facture, reste peu/pas visible sur fort volume — extrait vers TASK-070 (alléger l'écran, décision PO 13/07). | 2026-07-13 |
| TASK-068 | **Sélecteur de colonnes persistant (`localStorage`) sur toutes les listes** (`declaration-tva-web/src/useColumnPrefs.ts` nouveau, `ColumnSelector.tsx` nouveau, `ReglementsSelection.tsx`, `AffectationsDrill.tsx`, `RapprochementInterrogation.tsx`, `FactureInterrogation.tsx`, `DomainGrid.tsx`, `ControlGrid.tsx`) : hook générique `useColumnPrefs(storageKey, allColumns)` (repli sûr clé absente/corrompue/vide ⇒ toutes colonnes visibles, garde-fou ≥1 colonne toujours visible) + composant `ColumnSelector` (bouton + popup cases à cocher, patron `ExcelFilter`, `createPortal`) câblés aux 6 grilles, clé `localStorage` propre par écran (`grf.cols.*`), entête et corps dérivés de la **même** `visibleColumns` (jamais deux sources). `AffectationsDrill` : ligne « non valorisé » et ligne « Total » ré-écrites en rendu conditionnel par clé plutôt qu'indexation en dur sur `GRID_COLUMNS[i]`. **Réserve non bloquante** : `DomainGrid` (réutilisé par `WorkstationPanel`/`ProofModal`/`ControleDeclarationPanel`) partage une clé unique `grf.cols.domain` entre ses 3 contextes — décision simplificatrice documentée, repli sûr du hook évite tout écran vide, pas d'isolation stricte entre usages. Front-only. **Vérifié par l'architecte** : wiring relu dans les 6 fichiers, rendu conditionnel `AffectationsDrill` et partage de clé `DomainGrid` confirmés en source ; `npm run build` (tsc+vite) et `npm run lint` (oxlint) rejoués, 0 erreur (warnings pré-existants uniquement) ; e2e Playwright (`column-selector.spec.ts`) 1/1 passed. | 2026-07-13 |
| TASK-067B | **Filtres « valeurs disponibles » (Excel-like), Part B back+front server-side** (`Declaration.Application/Entities/ReglementRapprochement.cs`, `FactureInterrogation.cs`, `Declaration.Application/Interfaces/IDeclarationRepository.cs`, `Declaration.Infrastructure/Repositories/DeclarationRepository.cs`, `Declaration.API/Controllers/RapprochementController.cs`, `FacturesController.cs`, `DeclarationsController.cs`, `declaration-tva-web/src/RapprochementInterrogation.tsx`, `FactureInterrogation.tsx`, `DomainGrid.tsx`) : endpoints `distincts` étendus (Rapprochement : `numeroReglement`/`numeroExtrait`/`banque` ; Factures : `numero`/`reference`) + nouvel endpoint distincts `DomainGrid` (`GetLignesDistinctsAsync` : `numeroRapprochement`/`source`/`tauxTVA`/`origine`/`statutLigne`) — **même `FromWhere`/WHERE que la liste/COUNT** (invariant TASK-040, aucune valeur infiltrable). `list` par défaut y compris forte cardinalité (n° pièce, décision PO 13/07) ; `tiers`/`fournisseur` restent `text`/LIKE (cardinalité non bornée et instable, documenté). Bonus corrigé au passage : `DomainGrid` ignorait/tronquait silencieusement plusieurs filtres (`source`/`tauxTVA`/`origine` jamais appliqués, `numeroRapprochement` tronqué au 1er élément) — `BuildLigneFilterWhere` désormais partagé entre `GetLignesAsync`/`GetLignesCountAsync`, filtres réellement appliqués. Lecture seule stricte (aucun write path modifié). **Vérifié par l'architecte — code relu (pas seulement le rapport du worker)** : invariant WHERE confirmé en code (mêmes constantes `RapprochementFromWhere`/`FacturesFromWhere`, même `BuildLigneFilterWhere` liste/count DomainGrid) ; lecture seule confirmée (`GetLignesDistinctsAsync` = `SELECT` pur). **1er VERIFY REJETÉ** : affirmait faussement l'absence d'accès à une base réelle et l'existence d'un index naturel sur `MV_ExtraitNum`/`DO_Numero`/`DO_Reference`. **Mesure perf refaite sur base réelle** `.\sql2022`/`GR_EMA_DISTRIBUTION` par l'architecte : `SELECT DISTINCT TOP 500` négligeables sur le jeu de dev actuel (93-214 lectures logiques, 0-15ms) mais **seule `MV_Numero` a un index dédié** (`IX_SO_Id_Numero_Domaine`) — `MV_ExtraitNum`/`DO_Numero`/`DO_Reference` n'en ont aucun ; suivi documenté (revalidation obligatoire à volumétrie de production + tâche d'ajout d'index séparée), sans remettre en cause la décision PO `list`. Build back 0 erreur, `dotnet test` Orchestration 80/80 (12 nouveaux tests `Task067BFiltresListeServerSideTests`), front `tsc`+`vite build`+`oxlint` 0 erreur. Clôture le fil TASK-067 (A+B) ; débloque TASK-068. | 2026-07-13 |
| TASK-067A | **Filtres « valeurs disponibles » (Excel-like), Part A front-only** (`declaration-tva-web/src/AffectationsDrill.tsx`, `ReglementsSelection.tsx`, `ControlGrid.tsx`) : les 3 grilles client-side (jeu complet déjà en mémoire) passent en `filterType:'list'` (cases à cocher + recherche, `ExcelFilter` existant) sur les colonnes énumérables restantes — `numeroReglement`/`factureNumero` (`AffectationsDrill`), `numeroReglement`/`tiers` (`ReglementsSelection`), `factureNumero`/`designation`/`tiers`/`identifiantFiscal`/`ice` (`ControlGrid`) — `list` uniforme y compris forte cardinalité (n° pièce), décision PO 13/07 : pas de fallback LIKE. Colonnes montant/date inchangées (plage). `ReglementsSelection` : `tiers`/`numeroReglement` retirés des query params serveur `/rapprochement`, filtrage déplacé côté client sur `allData` (même patron que `statut`) — aucun appel back ajouté, lecture seule stricte. `ControlGrid` : mécanisme générique `distincts` déjà en place, aucune modification de logique. **Vérifié par l'architecte** : scope conforme à la scission TASK-067→067A/067B (front-only, aucune dépendance back) ; build `npx tsc --noEmit` 0 erreur, `npx oxlint` 0 erreur (2 warnings react-refresh + 1 exhaustive-deps préexistants non liés), `npx vite build` ✓ ; vérification visuelle des 3 écrans (filtres Excel-like + compteur exact après filtrage, aucune régression TASK-040) confirmée par le PO. Ouvre la voie à TASK-067B (server-side). | 2026-07-13 |
| TASK-066 | **Sélection de société au login (`P_SOCIETE`) + `SO_Id` source de vérité** (`Declaration.API/Controllers/AuthController.cs`, `DeclarationsController.cs`, `FacturesController.cs`, `RapprochementController.cs`, `Declaration.Selection/SelectionnerAffectationsService.cs`, `declaration-tva-web/src/Auth.tsx`, `App.tsx`, `DeclarationTVA.sql`) : `/api/societes` lit désormais réellement `P_SOCIETE` (`SELECT SO_Id, SO_RaisonSocial … ORDER BY SO_RaisonSocial`) au lieu du `{Id="001", Nom="Société Test"}` figé ; `AuthController.Login` ne renvoie plus que le token (littéraux `SocieteId="001"`/`SocieteName="GR_EMA_DISTRIBUTION"` supprimés) ; combobox login alimenté par l'endpoint, soumission bloquée sans société, `SO_Id` (INT) + raison sociale portés en session (`User.societeId: number`). **Zéro littéral société résiduel** (grep `.cs`/`.tsx` vide). Tous les endpoints de données bornées société exigent `soId`/`societeId` (**paramètre obligatoire + `400` explicite si absent/≤0** — durcissement au-delà du scope : les défauts silencieux `=1`/`=0` de `Factures`/`Rapprochement`/`Declarations.GetAll` supprimés) et le propagent au SQL (`WHERE M.SO_Id = @so`). Réconciliation idempotente `DeclarationTVA.sql` (gardée `sys.types t.name <> 'int'`) : `UPDATE '001'→1` **avant** `ALTER COLUMN SocieteId INT`, drop/recreate `IX_DM_ENTTVA_Unicite`. `Selection.tsx` (code mort) supprimé. **Vérifié par l'architecte — code relu ↔ VERIFY** (endpoint `P_SOCIETE`, `AuthController` sans littéral, `Auth.tsx`/`App.tsx`, 4 filtres SQL `SO_Id`, migration idempotente, `Selection.tsx` absent) ; preuve réelle `.\sql2022`/`GR_EMA_DISTRIBUTION` (colonne migrée `INT`, déclaration `TVA1-2026-06` réconciliée `SO_Id=1`, endpoints `400` sans `soId` puis `200` filtrés) ; build back + `npm run build` front à 0 erreur, `dotnet test` **148/149**. **Note non bloquante actée** : seul échec `ComparateurTests.GenererRapportVerification` — dépendance de données **legacy** `RT_DeclarationTva` (module contrôle GRFN, `DT_Id=66` absent, table vide dans l'env), **hors périmètre**, code du test inchangé (`git diff` vide) → échec préexistant à traiter séparément. | 2026-07-13 |
| TASK-033 | **Cohérence des défauts de ports dev (front ↔ API)** (`declaration-tva-web/.env.development`, `declaration-tva-web/vite.config.ts`) : les valeurs par défaut livrées divergeaient (front `:5005`, API `:5018`) → `ERR_CONNECTION_REFUSED` sur un clone frais (le front appelant en URL absolue court-circuitait le proxy Vite). Défaut front aligné sur le port de l'API : `VITE_API_BASE=http://localhost:5018/api` et proxy `vite.config.ts` `target: http://localhost:5018`. Les deux ports restent surchargables sans toucher au code (front `VITE_API_BASE`, API `--urls`/`launchSettings.json`). Config dev only, **aucun** code C#, chaîne de connexion / CORS / `.env.test` inchangés. **Checkup architecte (base code réelle)** : cohérence des 3 points confirmée en source — `launchSettings.json applicationUrl=http://localhost:5018` (+ profil https `7103;5018`), `.env.development VITE_API_BASE=…:5018/api`, `vite.config.ts target …:5018`, `.http` sur 5018 ; **grep `5005` = 0 occurrence** dans tout `declaration-tva-web` → cause racine (divergence des défauts) éliminée. | 2026-07-13 |
| TASK-052 | **Valoriser les factures sans règlement (`NonAffecte`)** (`Declaration.Selection/SelectionExpliqueeModels.cs`, `EstValorisable`) : `EstValorisable => Eligible \|\| NonRapproche \|\| NonAffecte` (l.36) → les factures fournisseur sans aucune `RT_AFFECTATION` sont lues OM et mises en cache **token NULL** (`Source=OM`) pour affichage, **jamais déclarables**. Complète le Périmètre B de TASK-050 (« toutes les factures, avec ou sans règlement »). Bug de fond corrigé côté évaluateur : `AF_Id==null` testé **avant** `MV_Compta/Annule/Impaye` ([SelectionExpliqueeEvaluator.cs:67-68](Declaration.Selection/SelectionExpliqueeEvaluator.cs#L67-L68)) → `NonAffecte` n'est plus classée `NonComptabilise` (elle était inatteignable). **Garde-fous** : déclaration reste gated `EstEligible` (`RecalculerLignesCandidatesAsync`/`GenererDeclaration` inchangés, l.124/227), token NULL jamais servi comme ventilation déclarable, lecture seule stricte (aucune écriture `RT_*`, `DT_Id` intouché TASK-028). **Vérifié par l'architecte — preuve base réelle `GR_EMA_DISTRIBUTION` (SO_Id=1)** : classement `Motif` de l'évaluateur reproduit en SQL pur sur juin 2026 → **NonAffecte = 162 factures** (valorisables), NonRapproche = 40, NonComptabilise = 28 ; **FC2600867 (`EC_Id=23999`)** désormais en cache — `Token_MV_Id=NULL`, `Source=OM`, `BaseHT=5786.16`, `MontantTva=1157.23`, `TotalTtc=6943.26` **= `EC_Montant` en-tête** (recoupé) ; bilan cache global **3814 lignes dont 1349 token-NULL** (familles NonRapproche+NonAffecte valorisées, jamais déclarées) ; invariant déclaration `EstEligible` confirmé en code. Dépend de TASK-050 (DONE) + TASK-024. | 2026-07-13 |
| TASK-049 | **Valorisation d'affichage avant rapprochement (`NonRapproche`)** (`Declaration.Selection/SelectionExpliqueeModels.cs` + `Declaration.Application/Services/DeclarationWorkflowService.cs`) : `RafraichirValorisationAsync` sélectionne `EstValorisable` (`Eligible ∪ NonRapproche ∪ NonAffecte`, l.161) au lieu de `EstEligible` → une facture affectée mais **non rapprochée** (`MV_Point≠1`, ex. FC2600909) est lue OM + mise en cache **token NULL** → famille B affichée, statut famille C `NonDeclarable` (honnête). **Invariant clé (relu en code)** : la génération de déclaration garde strictement `EstEligible` — `candidates.Where(c => c.EstEligible)` (l.124) et gate `if (!c.EstEligible)` (l.227) inchangés → aucune facture non rapprochée n'entre dans une déclaration, aucune ligne token-NULL déclarée. Lecture seule (Sage OM + cache), aucun `DT_Id` écrit (TASK-028). **Vérifié par l'architecte — preuve base réelle `GR_EMA_DISTRIBUTION` (SO_Id=1, juin 2026)** : **NonRapproche = 40 factures** valorisables mais non déclarables (breakdown évaluateur reproduit en SQL) ; 1349 lignes cache token-NULL au total, jamais servies comme déclarables (garde-fou `EstEligible` confirmé). S'appuie sur le correctif cache token-NULL (TASK-050). | 2026-07-13 |
| TASK-047 | **TTC document sourcé de la valorisation OM** (`SageTaxReader/SageTaxReader.Core/SageTaxReaderService.cs`, `ExtraireTaxes`) : `TotalTtc` sourcé de `valo.TotalTTC` (OM, source qui fait foi comme la TVA) au lieu de l'en-tête SQL `doc.DO_TotalTTC` — une seule ligne fonctionnelle (`TotalTtc = Math.Round(valo.TotalTTC, decimales, MidpointRounding.AwayFromZero)`, l.285). L'OM **conserve le signe** des avoirs (contrairement au HT qu'il normalise positif) et recoupe l'en-tête sur pièces saines → non-régression. **Anti-fabrication** : l'écart réel de la pièce Sage (`FC2501166` −5,74, en-tête=`DO_NetAPayer`=`DO_MontantRegle`=4522 vs Σ lignes 4527,74) **reste visible** (`EcartArrondi` −5,74), aucune substitution Σ lignes. net48 pur, lecture seule stricte, zéro écriture GRFN. **Vérifié par l'architecte** : correctif relu en source (l.285, `DO_TotalTTC` abandonné) ↔ VERIFY ; preuves base réelle `NEW_EMA DISTRIBUTION` (OM==en-tête sur FC2501166/310/649/768, avoir `AV2500043` −1920 signe conservé, non-régression FC2600001-004). **Point ouvert non bloquant** (hors périmètre worker) : signe des avoirs/retours au front (`RT_MOUVEMENT.MV_Montant` toujours positif sur écran Rapprochement) à confirmer par le PO au test. Origine : découverte connexe TASK-046 §6. | 2026-07-13 |
| TASK-046 | **Rollup HT document faux (`DO_TotalHT` nul) → faux `EcartArrondi`** (`SageTaxReader/SageTaxReader.Core/SageTaxReaderService.cs`, `ExtraireTaxes`) : sur certaines comptabilisées l'en-tête Sage a `DO_TotalHT=0` alors que le HT réel est dans `DO_TotalHTNet` → `TotalHT=0` et `EcartArrondi≈TTC` (des milliers). Correctif : ajout de `DO_TotalHTNet` au `SELECT F_DOCENTETE` (l.233) + repli **ciblé** `if (htLignes == 0 && htNet != 0) htLignes = htNet;` (l.254-256), appliqué **avant** le calcul d'escompte. **Choix `DO_TotalHTNet` (SQL) et non l'OM** : l'OM normalise le HT en positif (inverserait le signe des avoirs) alors que `DO_TotalHTNet` conserve valeur ET signe (recoupé = Σ `DL_MontantHT`). Conforme REX (`DO_TotalHT` interdit). **Non-régression** : branche non prise si `DO_TotalHT≠0` → sortie identique bit à bit ; TVA par taux strictement inchangée ; buckets cache TASK-024 inchangés. net48 pur, lecture seule stricte. **Vérifié par l'architecte** : correctif relu en source (l.233/244/254-256/262) ↔ VERIFY ; preuves base réelle `NEW_EMA DISTRIBUTION` (10 pièces à en-tête cassé `EcartArrondi`→0 signes corrects dont avoirs négatifs ; 199 documents `DO_TotalHT=0 ∧ DO_TotalHTNet≠0` recoupés Σ lignes ; non-régression FC2600001-004). Le résidu `DO_TotalTTC` en-tête ≠ Σ lignes isolé hors scope → TASK-047. Défaut préexistant révélé par TASK-045. | 2026-07-13 |
| TASK-065 | **Renommer les tables en `DM_ENTTVA` / `DM_LGTVA` (nommage conforme)** (`DeclarationTVA.sql`, `Declaration.Infrastructure/Repositories/DeclarationRepository.cs`) : Renommé uniquement les tables SQL (`DeclarationEntete` → `DM_ENTTVA` et `LigneCandidate` → `DM_LGTVA`), ainsi que tous les index (`IX_DM_ENTTVA_Numero`, `IX_DM_ENTTVA_Unicite`, `IX_DM_LGTVA_DeclarationId_Domaine`) et la contrainte de clé étrangère (`FK_DM_LGTVA_DM_ENTTVA`), en utilisant un bloc `sp_rename` idempotent placé au début du script unique de DDL `DeclarationTVA.sql`. Suppression définitive du fichier SQL obsolete `Declaration.Infrastructure/SQL/001_Schema_TVA.sql`. Mise à jour des 22 littéraux de requête SQL dans `DeclarationRepository.cs` sans altérer les classes, types et DTO applicatifs. Validation sur le moteur local avec succès (6 avertissements de renommage SQL Server), build complet de la solution à 0 erreur/0 avertissement, tests automatiques de non-régression validés. | 2026-07-13 |
| TASK-061 | **Aligner les montants persistés en DECIMAL(18,6) au lieu de FLOAT** (`DeclarationTVA.sql`) : conversion des colonnes de montants et taux (`HT`, `Taux`, `TVA`, `TTC`, `Prorata`, `MontantAffecte`) de `FLOAT` à `DECIMAL(18,6)` dans le `CREATE TABLE` et les clauses `ALTER TABLE ... ADD COLUMN` de `DeclarationTVA.sql`. Ajout d'un bloc de migration idempotent en fin de script qui utilise `sys.columns` et `sys.types` pour migrer automatiquement et sans erreur les bases de données déjà existantes de `FLOAT` vers `DECIMAL(18,6)`. Le script gère dynamiquement la suppression et la recréation des contraintes `DEFAULT` sur les colonnes non-nullables `Prorata` et `MontantAffecte` lors de la migration pour éviter les blocages SQL Server. Aucun code applicatif C# n'a été modifié (le modèle C# étant déjà typé en `decimal`). Exécution du script validée avec succès sur une base neuve ainsi que sur une base existante déjà déployée. | 2026-07-13 |
| TASK-064 | **Trigger : immuabilité TOTALE d'une affectation déclarée** (`Declaration.Infrastructure/SQL/003_Verrou_DT_Id.sql`) : interdiction totale d'UPDATE ou de DELETE sur toute affectation déclarée (`DT_Id NOT NULL`) avant et après la transaction. Autorisation de dé-tamponnage PUR (`DT_Id : valeur → NULL`) uniquement si toutes les autres colonnes de la table restent inchangées (comparaison null-safe par clause T-SQL `EXCEPT`), toute modification simultanée (cheval de Troie) étant rejetée. Adaptation du message d'erreur 50028 pour retirer la mention restrictive des colonnes financières. Vérifié par tests SQL (0 erreur build, tests C# de non-régression verts, script de test PowerShell validant tous les cas de blocages et autorisations réelles). | 2026-07-13 |
| TASK-063 | **Filtres cohérents par type de données sur la liste de rapprochement + fin du « filtre menteur »** (`Declaration.Application/Entities/ReglementRapprochement.cs` (`RapprochementFilter` + `ToFlags`), `Declaration.Application/Interfaces/IDeclarationRepository.cs`, `Declaration.Infrastructure/Repositories/DeclarationRepository.cs`, `Declaration.API/Controllers/RapprochementController.cs`, `declaration-tva-web/src/RapprochementInterrogation.tsx`, `Declaration.Orchestration.Tests/Task063FiltresParTypeTests.cs`) : chaque colonne porte un `filterType` adapté (date → plage, montant/compteur → plage, énumération → cases + recherche, texte → LIKE). **Bug menteur corrigé** : `declare`/`mode` ne transmettaient que la 1re case cochée (`params.declare = dc[0]`) → passés en **multi-sélection serveur réelle** (`ToFlags` → SQL `CASE … IN @flags`), comme `origine`/`domaine`. **Tous les filtres côté serveur** via le fragment **partagé** `RapprochementFromWhere` (liste **et** COUNT) → invariant TASK-040 préservé ; plages comparées à la **même** expression que la projection (`reste`, `nbFactures`, `DateRapprochement`, `MV_Echeance`), jamais à une colonne physique absente. Plages `NULL`-safe (borne vide ⇒ pas de contrainte), bornes hautes de date exclusives (`+1 j`), helpers `SqlSafeDate`/`SqlSafeUpperExclusive` neutralisant toute borne hors plage `datetime` (anti-overflow runtime). **Lecture seule stricte** — 100 % SELECT/COUNT, `DT_Id` uniquement lu (TASK-028). **Vérifié par l'architecte** : entité/interface/repo/controller/front relus ↔ VERIFY (WHERE partagé unique liste+COUNT, expressions dérivées répliquées, `ToFlags` union réelle, LEFT JOIN `vBanque` conservé, lecture seule confirmée) ; `dotnet build Declaration.API` **0 avertissement / 0 erreur** rejoué ; `dotnet test` **67/67** orchestration (dont `Task063FiltresParTypeTests`) + `npx tsc --noEmit` **0 erreur** rejoués ; preuve réelle `GR_EMA_DISTRIBUTION` (SO_Id=1, période 2026-01→08) : baseline 1082, montant `[1000,5000]`→222, extrait `LIKE '%1%'`→387, date rappro `[05-01,06-01)`→162, mode multi {chèque 432, virement 376}→**808 union**, déclaré {Oui 0, Non 1082}→**1082 = baseline** (fin du filtre menteur). **Note non bloquante** : le tableau « Critères » du VERIFY indique « 24/24 » (compte périmé) alors que la suite exécute **67/67** — libellé cosmétique, résultat réel supérieur. Paramètre Dapper `finInclude` orphelin (TASK-062) non nettoyé ici — reste à faire à l'occasion. | 2026-07-13 |
| TASK-062 | **Source unique de la date de période (`DateReference`)** (`Declaration.Selection/RegleDatePeriode.cs` (nouveau), `Declaration.Infrastructure/Repositories/DeclarationRepository.cs`, `Declaration.Selection/SelectionnerAffectationsService.cs`, `Declaration.Selection/SelectionExpliqueeService.cs`, `Declaration.Selection/SelectionExpliqueeEvaluator.cs`, `Declaration.Application/Entities/ReglementRapprochement.cs`, `Declaration.Orchestration.Tests/Task062DateReferenceTests.cs`) : une **seule** règle de date répliquée à l'identique SQL ⇄ C# (`DateReferencePour`/`DateReferenceSqlM` = `CASE WHEN MV_Type<>0 AND MV_Point=1 THEN MV_PointDate ELSE MV_Date END` ; `EstDeclarable = MV_Type=0 OR MV_Point=1`), référencée par les 3 chemins (interrogation, règlement-first ×4, facture-first pré-filtre+gate). Interrogation corrigée (`RapprochementFromWhere`) **sans gate** — non-rapprochés conservés (PO) ; borne haute homogène `< @finExclude` (fin du double comptage espèce). Espèce facture-first **gelée** (aucune branche codée, décision PO en attente). **Lecture seule stricte** — `DT_Id` intouché (TASK-028). **Vérifié par l'architecte** : `RegleDatePeriode`, interrogation, les 4 requêtes règlement-first, `SelectionExpliquee`/`Evaluator`, `ReglementRapprochementRow.DateReference` relus ↔ VERIFY ; `dotnet build Declaration.Infrastructure` **0 erreur** rejoué ; `dotnet test` **61/61** orchestration (dont `Task062DateReferenceTests` 14/14) + **52/52** sélection rejoués ; preuve réelle `GR_EMA_DISTRIBUTION` (SO_Id=1) : `RF26060064` 0→1 janvier, 479 règlements changeant de mois, 0 `MV_Point=1` sans `MV_PointDate`, 88/88 espèces `MV_Point=0`. **Note non bloquante** : paramètre Dapper `finInclude` désormais orphelin (à nettoyer, TASK-063 même fichiers). | 2026-07-12 |
| TASK-042 | **Rapprochement : colonnes de preuve bancaire (`MV_Point`/`MV_PointDate`/`MV_ExtraitNum`/`MV_Echeance`/`BanqueCode`) + règle espèce auto-rapprochée** (`Declaration.Application/Entities/ReglementRapprochement.cs`, `Declaration.Infrastructure/Repositories/DeclarationRepository.cs`, `Declaration.API/Dtos/ReglementRapprochementDto.cs`, `declaration-tva-web/src/RapprochementInterrogation.tsx`, `Declaration.Orchestration.Tests/Task036RapprochementProjectionTests.cs`) : 5 colonnes de preuve exposées **en lecture seule** ; `BanqueCode` via `LEFT JOIN dbo.vBanque` (`B.[No]=M.BN_Id AND B.SocieteNo=M.SO_Id`) placé dans le **fragment partagé `RapprochementFromWhere`** (liste+`COUNT` alignés, `TotalCount` inchangé). **Dérivation à source unique** (`ReglementRapprochementRow`) : `EstRapprocheBanque = MvPoint==1 \|\| MvType==0`, `DateRapprochement = MvType==0 ? MvDate : MvPointDate` ; le SQL du filtre `@rapproche` réplique **exactement** l'expression (`CASE WHEN M.MV_Point=1 OR M.MV_Type=0 THEN 1 ELSE 0 END`) → aucun « filtre menteur ». **Aucune écriture** (`MV_Point`/`MV_PointDate` jamais écrits, `DT_Id` intouché — TASK-028). **Découverte VERIFY (transparence)** : `MV_PointDate`/`MV_Echeance` sont `datetime NOT NULL` avec sentinel `1753-01-01` sur les lignes non renseignées → `NULLIF(col,'17530101')` dans la projection → `NULL` → « — » au front (jamais de date fictive). Front : marqueur « espèce » sur le rapproché auto, n° extrait vide (jamais fictif). **Vérifié par l'architecte** : entité/repo/DTO/front/tests relus ↔ VERIFY (source unique, filtre répliqué à l'identique, NULLIF sentinel, lecture seule confirmée = 100 % SELECT) ; `dotnet build Declaration.Application` **0 erreur** rejoué ; `dotnet test …Task036RapprochementProjectionTests` **18/18** rejoué ; preuve réelle `GR_EMA_DISTRIBUTION` (SO_Id=1) fournie (types colonnes, vue `vBanque` non-duplication COUNT 1073=1073, espèce `RC26010004` rapprochée date=`MV_Date`, chèque/virement rapprochés `EXT …`+`SGMB`). **Réserve non bloquante** : gap volume 595/352 quantifié (595 GRF = 595 référence via `MV_Domaine IN(0,1)` TASK-039 ; les 55 espèces n'expliquent pas seules l'écart de 243) → recommandation PO : re-mesurer le « 352 » sur le build post-039/040 (aucune TASK de diagnostic ouverte, compte courant déjà aligné). | 2026-07-12 |
| TASK-058 | **Écran ⑤ Contrôle déclaration (répartition source/taux + anomalies typées + drill + garde-fou export)** (`declaration-tva-web/src/ControleDeclarationPanel.tsx` nouveau, câblé dans `DeclarationStepper.tsx` étape ⑤ ; **correctif back** `Declaration.API/Controllers/DeclarationsController.cs`) : écran de contrôle **lecture seule** consommant `GET /declarations/{id}/checkup` — répartition par source, vue par taux (tri décroissant), anomalies 🔴 bloquantes / 🟠 avertissements (aucune masquée), drill → `DomainGrid` (TASK-016) filtré, export/clôture bloqués si 🔴 (garde-fou TASK-012, `disabled={hasBloquants}` sur CTA ⑥). **1er REJECT (rôle worker) — contrat inexistant** : la 1ʳᵉ livraison front consommait `recapSource`/`recapTaux`/`alertes.type`/`equilibre{isValid,ecart}` que le back **ne renvoyait pas** (`/checkup` n'émettait que `ControleEquilibre`/`Alertes.Niveau`/`Reconciliation`) → `recapSource.reduce` = TypeError (écran blanc) + `a.type==='bloquant'` toujours faux (garde-fou mort). **Correction** : projection JSON du controller enrichie (`GetCheckup` l.116-185) — agrégation des lignes **INTÉGRÉES uniquement** (`recapSource` GroupBy Source, `recapTaux` GroupBy Taux tri décroissant), `equilibre` dérivé (`TotalDeclareTtc − (TotalMontantAffecte + ΣTVA)`, tolérance 0,01), `alertes` typées (mapping `Niveau→type` : Error→bloquant, Warning/Info→avertissement, aucune masquée, drill câblé sur `numeroRapprochement`), `reconciliation` compteurs par état. **Aucun recalcul TVA** — `GetCheckupAsync` (TASK-009) intouché, pure projection en lecture. **Vérifié par l'architecte + preuve E2E réelle (rôle worker, base `GR_EMA_DISTRIBUTION`, API :5018 rebuild du correctif)** : base remise à zéro, déclaration `TVA001-2026-01` créée → **1318 candidates réelles** chargées (TVA valorisée — la valorisation OM fonctionne ici, plus le blocage 170/170 `grf-valorisation-tracabilite-blocage-om`) → 1240 proposées intégrées → `GET /checkup` **HTTP 200** contrat aligné : `equilibre {isValid:true, ecart:0.00}` ; `recapSource` Décaissement (HT 2 748 914,90 / TVA 488 087,05 / TTC 3 237 001,95) + Encaissement (HT 1 488 287,68 / TVA 224 480,75 / TTC 1 712 768,43) ; `recapTaux` 20/10/9/8/0 % tri décroissant ; **35 alertes typées** (34 avertissement + drill `numeroRapprochement`) ; `reconciliation` candidates 1318 / intégrées 1240 / exclues 34 / reportées 44. **Chemin 🔴 bloquant vérifié** par perturbation ciblée **réversible** (ICE vidé sur 1 ligne intégrée `AV2500043`) → `/checkup` renvoie **1 alerte `type:"bloquant"`** (`TIERS_SANS_ICE`) → `alertes.some(a=>a.type==='bloquant')`=true → CTA « Passer à la synthèse » bloqué ; ICE restauré (0 vide résiduel, base laissée cohérente). Les 3 défauts de la 1ʳᵉ revue sont levés **sur données réelles**. Builds : `dotnet build Declaration.API` 0 erreur, `npm run build` (tsc -b + vite) 0 erreur TS (8 warnings préexistants hors fichiers livrés). **Anti-régression** : recalcul back TASK-009 / drill TASK-016 / hub abandonné TASK-018 (absent) / clôture gardée TASK-012 / IntegrationPanel TASK-057 / ControlGrid.tsx **tous intouchés**. Étape ⑤ du tunnel 053→059. | 2026-07-12 |
| TASK-057 | **Écran ④ Intégration des lignes (confirmation + verrou DT_Id)** (`declaration-tva-web/src/IntegrationPanel.tsx` nouveau, câblé dans `DeclarationStepper.tsx` étape `integration` ; `CalculTvaPanel.tsx` expose `onCalcSummary` comme source unique des montants ③→④) : écran de confirmation pré-figeage — récap dense (nb règlements/nb lignes/total TVA **repris de ③ via props, aucun recalcul front**), check-list de 5 contrôles (règlements, lignes, affectations[warning], équilibre, bloquants) alimentée par `GET /declarations/{id}/checkup`, bouton **Confirmer intégration** (`id=btn-confirmer-integration`) → `POST /declarations/{id}/cloture` **endpoint existant**. `canConfirm = !integree && !confirmed && !hasBloquant && !loadingCheckup && !submitting` : toute alerte `bloquant`/`Error` désactive le CTA. Après succès : `isReadOnly = integree \|\| confirmed` → bandeau vert « Déclaration intégrée — lecture seule », badge « Intégrée », CTA retiré, passage ⑤ gaté par `integree` (statut rechargé via `fetchInfo`). **Anti-régression** : back verrou TASK-028 (`DT_Id` + triggers immuabilité) et figeage TASK-012/017 **intouchés** — l'écran n'appelle que les endpoints existants, aucune écriture front hors cet appel. **1er REJECT (2026-07-12)** : VERIFY ne fournissait que des preuves code/build, pas la preuve d'intégration réelle exigée aux §Livrables. **Correction** : preuve runtime end-to-end ajoutée (`probe_task057.ps1` → `probe_task057_out.txt`, base SQL Server réelle `GR_EMA_DISTRIBUTION`, 06:26). **Vérifié par l'architecte** : trace runtime relue et artefacts confirmés existants — §1 `POST /cloture` → **HTTP 204** (44 ms) ; §2 `GET /{id}` → **statut=1 (Cloturee)** → cascade lecture seule ; §3 requête directe `RT_AFFECTATION` → **3 lignes DT_Id=148884204** (AF 22697/22914/22919, MV 11899/11978/11982) posées → exclusion `WHERE DT_Id IS NULL` ; guard double-clôture → **HTTP 400**. Code cité recoupé (WorkflowService.cs:379/402/404/407/444 `CloturerDeclarationAsync`/`DeriveDtId`/`GetNumerosRapprochementIntegresAsync`/`TamponnerAffectationsAsync` réels ; IntegrationPanel btn l.248, `handleConfirm` l.114, `isReadOnly` l.132, `!isReadOnly` l.246 exacts). `tsc --noEmit` **0 erreur rejoué**, `oxlint src/` **0 erreur, 7 warnings tous pré-existants** (aucun dans les fichiers TASK-057). **Réserve non bloquante** : le VERIFY annonce « 8 warnings » là où l'outil en compte 7 (inexactitude cosmétique, sans impact critères). Étape ④ du tunnel 053→059. | 2026-07-12 |
| TASK-056 | **Écran ③ Calcul TVA (valorisation agrégée avant intégration)** (`declaration-tva-web/src/CalculTvaPanel.tsx` nouveau, câblé dans `DeclarationStepper.tsx` à la place du placeholder ③) : vue de contrôle **lecture seule** qui agrège la valorisation retournée par le back (`GET /declarations/{id}/lignes?domaine=Decaissement`, même endpoint que ② `AffectationsDrill`) par **(facture × taux)** puis par **taux**. **Aucun front-calcul** : les montants `montantHT`/`montantTVA` sont les valeurs directes du back, le seul calcul est une **agrégation** (`+=` sur des valeurs déjà valorisées) — aucune formule `TVA=HT×taux`. **Σ par construction** : `totalTVA = Σ(sous-totaux par taux)` = somme des lignes valorisées du tableau (calcul unique, deux usages) → cohérence garantie ; même source et même filtre `NON_VALORISE={2,3,4}` que ② → total aligné. **Transparence** : lignes non valorisées (statut Exclue/Reportée/Écartée) affichées avec `—` + badge orange « Non valorisé — {motif} » (fallback `STATUT_LIGNE_LABELS` si motif vide) **jamais un 0 muet**, compteur d'alerte en en-tête + panneau dédié « détail motif » (mémoire `grf-valorisation-tracabilite-blocage-om`). Bandeau pied « Total TVA à intégrer » mis en évidence. Densité 0 espace perdu (padding `0.42rem`, deux tableaux jointifs). **Anti-régression** : `ControlGrid.tsx`/`SummaryPanel.tsx`/`WorkstationPanel.tsx`/`AffectationsDrill.tsx`/`ReglementsSelection.tsx`/`mockServer.ts` **intouchés** (aucun nouvel endpoint), arrondis DGI `AwayFromZero` portés par le back et affichés tels quels. **Vérifié par l'architecte** : source relue ↔ VERIFY (agrégation = seul calcul, `NON_VALORISE` identique à ②, totaux dérivés d'un calcul unique, non valorisé jamais silencieux), câblage `DeclarationStepper.tsx` confirmé (import + étape `calcul` + CTA → ④), `ControlGrid`/`SummaryPanel`/`WorkstationPanel` **absents** de la liste des fichiers modifiés (git status), `tsc --noEmit` **0 erreur rejoué**, `npm run lint` (oxlint) **0 erreur, 8 warnings préexistants** (aucun dans `CalculTvaPanel.tsx`) rejoué, `npm run build` (tsc -b + vite) **vert rejoué** (390 ms, seul warning `INEFFECTIVE_DYNAMIC_IMPORT` préexistant sur `api.ts`). **Réserves non bloquantes (documentées par le worker)** : (1) endpoint dédié `/valorisation` agrégé serait plus propre pour ④ (reporté TASK-057) ; (2) mock mono-taux (`tauxTVA=20`) → rendu multi-taux structurellement correct, à confirmer sur données réelles multi-taux. Étape ③ du tunnel 053→059. | 2026-07-12 |
| TASK-055 | **Écran ② Affectations (drill règlement→factures + prorata multi-taux)** (`declaration-tva-web/src/AffectationsDrill.tsx` nouveau, câblé dans `DeclarationStepper.tsx` à la place du placeholder ②) : consomme `GET /declarations/{id}/lignes` (même endpoint que `DomainGrid`/`ProofModal`), regroupement par facture côté front (pur affichage). **Gap comblé côté back** : `Ventilateur.Ventiler` calculait déjà `Prorata` mais il était jeté au mapping — 2 champs `Prorata`/`MontantAffecte` ajoutés à `LigneCandidate`/`LigneCandidateDto`/`MapLignesCandidates` (valeurs déjà produites par `Declaration.Core`, aucun recalcul) + colonnes SQL (`DeclarationTVA.sql`/`001_Schema_TVA.sql`, `NOT NULL DEFAULT 0` idempotent). Écran affiche l'opération complète `payé÷TTC=%` puis `TVA×%=déclarée` par taux ; `ttcFacture`/`tvaSage` (opérandes pré-prorata) retrouvés par **inversion algébrique pure** côté front (traçabilité écran, pas une nouvelle valorisation — `montantHT/TVA/TTC` restent strictement les valeurs du back). Facture non valorisée → « Non valorisé — {motif} », jamais un 0 muet. `ProofModal.tsx`/`Declaration.Core`/`DomainGrid.tsx` **non modifiés**. **Vérifié par l'architecte** : diffs relus fichier par fichier (entité/DTO/service/repo/SQL conformes à la description), `ProofModal.tsx`/`Declaration.Core` confirmés intacts (git diff vide), `dotnet test Declaration.Orchestration.Tests --filter DeclarationWorkflowServiceTests` **1/1 rejoué**, `tsc -b --noEmit` **0 erreur rejoué**, `vite build` **rejoué** (371 kB, cohérent), fixture arithmétique du règlement `REG-2026-010` recalculée et confirmée (prorata 42,3729 %, TVA déclarée 677,97+84,75=762,72, inversion TTC exacte 11800,00). Colonnes `Prorata`/`MontantAffecte`/`EcType` confirmées **absentes** de `GR_EMA_DISTRIBUTION` en direct (`sys.columns`) — migration non encore jouée. **Réserves non bloquantes** : build complet `Declaration.API` bloqué par verrou de copie DLL (process en cours, **0 erreur C#** confirmé, cf. TASK-041/051 même classe d'incident) ; migration SQL prod + preuve sur données réelles **à jouer par l'administrateur/PO** une fois `DeclarationTVA.sql` exécuté et l'API redémarrée. | 2026-07-12 |
| TASK-054 | **Écran ① Règlements (sélection dans la déclaration)** (`declaration-tva-web/src/ReglementsSelection.tsx` nouveau, `DeclarationStepper.tsx` câblage) : adaptation de `RapprochementInterrogation.tsx` (TASK-037), même endpoint `GET /api/rapprochement` (TASK-036/039/040), **aucune ligne back touchée**. Grille dense pivot règlement, sélection multiple + total vivant (bandeau bas du shell TASK-053), statuts 🟢/🟠/🔴 (`statutDe()` : 🔴 déclaré ou sans affectation, 🟠 partiel, 🟢 éligible) **jamais masqués par défaut**, scope borné à la période de la déclaration, filtre `MV_Domaine IN (0,1)` (pas `MV_DECAISSE`, back inchangé). **1er REJECT (2026-07-11)** : preuve réelle `GR_EMA_DISTRIBUTION` manquante (accès DB jugé indisponible) + filtre État du périmètre STRICT non câblé. **Corrections** : (1) l'indisponibilité DB était une fausse alerte (résolution du nom d'instance `.\SQL2022`, service réellement démarré) ; (2) filtre État câblé en respectant la leçon TASK-040 (`fetchAll()` charge l'intégralité de la période en mémoire via pagination interne, filtre appliqué sur le jeu complet — compteur toujours = lignes rendues, classe de bug TASK-040 structurellement exclue). **Vérifié par l'architecte** : connectivité `GR_EMA_DISTRIBUTION` re-testée en direct (1116 lignes `MV_Domaine IN(0,1)`), `git diff` sur `DeclarationRepository.cs` confirme que le bloc `RapprochementFromWhere`/`MV_Domaine IN(0,1)` provient de TASK-036/039 (non modifié par ce worker), lecture de `ReglementsSelection.tsx` confirme `fetchAll()` + filtre sur `allData` complet. Build/lint verts. **Réserves non bloquantes** : pas de screenshot visuel (environnement sans affichage), aucun cas réel 🔴 observé dans le jeu de données actuel (594 🟢/1 🟠/0 🔴 sur 595 décaissements juin 2026 — vérifié par lecture de code pour la branche 🔴). Écart assumé non résolu : libellé « fourn. sans ICE » (conformité IF/ICE absente de la projection règlement-pivot, aurait nécessité une modif d'endpoint interdite). Débloque TASK-055 (écran ②). | 2026-07-11 |
| TASK-053 | **Tunnel déclaration règlement-first — shell + barre d'étapes 6 étapes** (`declaration-tva-web/src/DeclarationStepper.tsx`, front-only, seul fichier modifié pour cette task) : remplacement du stepper 2 étapes (`Workstation → Génération`) par un tunnel règlement-first à 6 étapes (`① Règlements → ② Affectations → ③ Calcul → ④ Intégration → ⑤ Contrôle → ⑥ Synthèse/Export`, conforme `reflexion dectva.md` + ordre tasks 054→059). `StepBar` dense (icône + n° + libellé, état ●/🔒/✔), tient sur une ligne. `③ Calcul` = `WorkstationPanel` (TASK-019) **intact**, `⑥ Synthèse/Export` = `GenerationPanel` **intact** ; `①②④⑤` = placeholders honnêtes (« Écran en cours », aucune donnée factice) en attendant 054/055/057/058. Bandeau bas : un seul CTA par étape placeholder, aucun CTA dupliqué sur ③/⑥ (qui ont déjà leur action primaire). **Verrouillage réel** : `⑤⑥` grisées/`disabled`/non cliquables (`title="… — nécessite l'intégration"`) tant que non intégré ; `①②③④` groupées et déblocables tant que 054/055/057 n'exposent pas de gate plus fin — limitation documentée en commentaire, pas une règle simulée. Post-intégration : bandeau « Déclaration intégrée — lecture seule » + bannière au-dessus de `①②③`, `⑤⑥` déverrouillées. **Bug réel corrigé** : le code hérité comparait `info.statut` à des chaînes (`'Clôturée'`/`'Générée'`, vestige `mockServer.ts`) alors que la vraie API renvoie `statut` **entier** (`StatutDeclaration` : 0=EnCours…3=Deposee, cf. `WorkflowEntities.cs:5-11`) → le gate ⑤⑥ n'aurait jamais basculé ; corrigé `isIntegree(statut)=statut!==0` + libellé traduit (`STATUT_LABELS`). **Périmètre strict** : `WorkstationPanel`/`GenerationPanel`/`App.tsx` **non modifiés** (non-régression 4 interrogations 035/037/041 + exports), aucun appel API/calcul ajouté (seul `fetchInfo` existant réutilisé). **Vérifié par l'architecte** : source relue ↔ VERIFY (6 étapes, gate ⑤⑥, placeholders honnêtes, bug entier ↔ enum `StatutDeclaration` confirmé), `WorkstationPanel`/`GenerationPanel` absents de la liste des fichiers modifiés (git), `npx tsc --noEmit` **rejoué 0 erreur**. **Réserve non bloquante** (checklist item ouvert, honnêtement déclaré) : preuve visuelle du chemin **post-intégration** (bannière lecture seule + déblocage ⑤⑥) faite **par lecture de code seule** — non exercée en live car clôturer une déclaration réelle pose le verrou `DT_Id` (TASK-028, triggers d'immuabilité) et exclut ses règlements des recherches futures ; action jugée trop irréversible pour un test UI. À rejouer par le PO sur une déclaration de test dédiée. **Socle du lot 053→059.** | 2026-07-11 |
| TASK-051 | **Suppression du fallback SQLite (code mort) + fail-fast connexion** (`Declaration.Infrastructure/Factories/DbConnectionFactory.cs` + `.csproj`) : SQLite était un vestige du scaffolding (TASK-012), jamais exécuté (aucun `tva.db`, SQL = T-SQL pur qui planterait sur SQLite). Le repli silencieux + `optional:true` sur `connections.json` avait **masqué** l'incident du 11/07 (« Erreur de chargement du checkup ») : config non chargée → connexion `(localdb)` bidon → obscure *error 52* au lieu d'un échec clair. **Correctif** : branches `SqliteConnection` (Grf/Sage/Persistence) + détection `.db`/`Data Source=` remplacées par un `throw InvalidOperationException` nommant la clé manquante quand `string.IsNullOrEmpty(cs)` ; `GetGrfConnectionString()` sans défaut `tva.db` ; `using Microsoft.Data.Sqlite;` + `PackageReference Microsoft.Data.Sqlite` retirés (warning `NU1903` disparu). Chemin nominal `new SqlConnection(cs)` **strictement inchangé**. **Périmètre strict** : aucun changement métier/SQL/repository/contrat API, `connections.json`/`appsettings.json`/`Program.cs` inchangés. **Vérifié par l'architecte** : source relue (fail-fast aux 4 méthodes, `using`+package Sqlite absents), `grep Sqlite` sur `Declaration.*` = **0 occurrence** rejoué, build `Declaration.Infrastructure` **0 erreur** rejoué (aucun `NU1903`, aucun warning Sqlite). Le seul test rouge (`Declaration.Controle.Tests.GenererRapportVerification`) est un test d'intégration live-DB **préexistant** qui instancie directement un `SqlConnection` codé en dur (`ComparateurTests.cs:26-28`) — indépendant du factory/fallback, échoue sur donnée absente (`DT_Id 66`) ; les 8 « erreurs » du build solution complet = verrous de copie DLL (API en cours d'exécution), pas de compilation. | 2026-07-11 |
| TASK-050 | **Valorisation facture-first + abandon du filtre `MV_DECAISSE`** (`Declaration.Selection`/`Declaration.Application`) : (A) prédicat `MV_DECAISSE` retiré des **3 surensembles** (`GetSurensembleFournisseurSql`/`GetSurensembleDepenseSql`/`GetSurensembleClientSql`) — direction bornée par `MV_Domaine` seul, espèce (`MV_Type=0`) conservée ; (B) pivot **facture-first** : nouvelle `LireFacturesDepuisPeriodeAsync` + `GetFactureFirstSql` (pivot `RT_ECHEANCE` `EC_Type=0` sur `DO_Date`, LEFT JOIN règlement/déclaration), `RafraichirValorisationAsync` filtre `EstValorisable` (déclaration reste gated `EstEligible` — `RecalculerLignesCandidatesAsync` **inchangé**). **Preuve réelle `GR_EMA_DISTRIBUTION` (SO_Id=1, exécutée en revue archi 2026-07-11)** : cache OM `EC_Id=24047` (FC2600896) `BaseHT=5748.24`/`TVA=1149.65` (20 %)/`TotalTtc=6897.46`=en-tête, `Source=OM`, `Token_MV_Id NULL` → **valorisée mais non déclarable** ; bilan cache **1755 lignes, 100 % `Source=OM` (0 illisible), 736 token-NULL** ; non-régression déclaration **Q3=0** (aucune ligne non-rapprochée déclarée) ; lecture seule (RT_* intacts). Build + `Declaration.Selection.Tests` **50/50** (dont test d'intégration DB superset 824→2060). **Réserves non bloquantes (VERIFY corrigé en revue) :** (1) l'annotation worker « FC2600896 `MV_Point=1`→Eligible » était fausse — réel `MV_Point=0`→NonRapproche (système correct : token NULL non déclarable) ; (2) ~~le chiffre « 465 factures juin 2026 » ne se reproduit pas (juin 2026 = 23 EC_Type=0)~~ **CORRIGÉ le 2026-07-11 (TASK-052)** : mon « 23 » était **faux** — piège de locale serveur `dmy` (littéral `'2026-06-01'` mal parsé). Réel juin 2026 = **218 factures EC_Type=0** (dont 157 sans affectation), ordre de grandeur cohérent avec le « 465 » du worker. Le code .NET n'est pas concerné (paramètres `DateTime` Dapper) ; superset global prouvé par le test d'intégration. | 2026-07-11 | **ICE/IF fournisseur sourcés dynamiquement depuis `P_SOCIETE`** (`Declaration.Selection`) : les 7 requêtes de sélection (`SelectionnerAffectationsService` ×4, `SelectionExpliqueeService` ×3) lisaient l'identité fiscale sur le **mouvement** (`M.MV_Ice`/`M.MV_Identifiant`, snapshot **systématiquement vide** ici → 1223/1223) → chaque facture levait `TIERS_SANS_ICE` **+** `TIERS_SANS_IF` = le faux `2×N` (run : 824 traitées / **1648** erreurs). Correctif : nouvelle classe `IdentiteFiscaleFournisseurConfig` qui **charge à l'exécution** les **noms de colonnes** ERP depuis `P_SOCIETE` (`SO_DecTvaColNameIceFrs`/`SO_DecTvaColNameIdentifiantFrs`, mapping ORM legacy `DeclarationTvaEncaissementErpColumnName*` résolu via `INFORMATION_SCHEMA` — schéma produit identique multi-clients, seule la valeur varie), les valide (**whitelist `^[A-Za-z0-9_]+$`** + quotage `[ ]`, tolérance nom déjà quoté) puis les injecte dans le SELECT sur le **maître tiers** `F_COMPTET` (jointure `T.CT_Num = M.CT_Code`) — réplique de la recette legacy `GetAllIceTiersToMaroc`. **Aucun nom de colonne figé en dur**, les valeurs (codes tiers) restent paramétrées. **Config absente/invalide → `ConfigurationIdentiteFiscaleException` levée au niveau société** (message clair) et **rethrow** (non masquée en liste vide dans `SelectionnerAffectationsService`, propagée dans `SelectionExpliquee`) — plus d'erreur silencieuse par facture. **Lecture seule stricte** (SELECT uniquement), `DT_Id` lu jamais écrit (verrou TASK-028 intact), indépendant OM (TASK-045/046/047) et cache (TASK-024). **Vérifié par l'architecte** (source relue ↔ VERIFY) : bascule `MV_*`→`T.[<colConfig>]` confirmée aux 7 requêtes (0 lecture `MV_Ice`/`MV_Identifiant` résiduelle hors commentaire), rethrow config confirmé, whitelist + quotage relus. Build `Declaration.Selection.Tests` **0 erreur**, **35/35 tests verts** (dont 6 nouveaux `IdentiteFiscaleFournisseurConfigTests` : quotage, tolérance pré-quotée, blocage ICE/IF absent, 6 payloads d'injection rejetés, alphanum+underscore accepté). **Preuve réelle `.\sql2022`/`GR_EMA_DISTRIBUTION` (SO_Id=1, domaines 0/1/6)** : bascule `MV_*`→`F_COMPTET` = 1223 vides (100 %) → **110 ICE / 113 IF** réellement absents (= vraie qualité de données, plus le faux 2×N) ; échantillon décaissement ICE 15 car./IF 8 car. passant `ValiderAffectation` (`F1001`/`F0025`/`F0125`/`F0129`). | 2026-07-10 |
| TASK-045 | **Worker OM « Valeur invalide ! » sur 100 % des lectures de facture Sage (`EC_Type=0`)** (`SageTaxReader.Core/SageTaxReaderService.cs`) : le blocage systémique (run janvier 2026 = 170/170 `FACTURE_ILLISIBLE_OM`) n'était **pas** dans `doc.Valorisation` (candidat n°1 de la TASK) mais **en amont, dès `ExistPiece`**, à cause d'un **énum `DocumentType` codé en dur avec une valeur inexistante**. Cause racine **isolée par instrumentation** (`Marshal.GetHRForException` étape par étape sur `25FA00993`) : le fallback « facture comptabilisée » utilisait `(DocumentType)7`/`(DocumentType)17` (numérotation SQL `DO_Type`) alors que l'énum OM réel (dumpé de `Interop.Objets100cLib.dll`) est **numéroté par dizaines** → `DocumentTypeVenteFactureCpta=70`, `DocumentTypeAchatFactureCpta=160`. Les valeurs 7/17 n'existent pas dans l'énum → l'OM rejette « Valeur invalide ! » (HRESULT `0xFFFFFFFF`). **Pourquoi 170/170 et pourquoi le PoC passait** : `DISTRI_DEMO` = pièces vivantes (DO_Statut=0, type 6/16, jamais le fallback) ; `NEW_EMA DISTRIBUTION` = quasi 100 % comptabilisées (DO_Statut=2, type 7/17) → 100 % heurtent l'énum invalide. **Correctif ciblé net48 pur** : remplacement des casts en dur par les **membres nommés** de l'énum dans les **3 chemins / 4 branches** (`LireFactureVente`, `LireFactureAchat`, batch `LireFactures` vente+achat) — aucune autre logique modifiée, isolation par pièce (`EnErreur`/`MotifErreur`) et timeout par pièce conservés, aucune valeur fabriquée. **Vérifié par l'architecte** : correctif relu en source aux 4 emplacements (l.158/196/411/430), **aucun cast `(DocumentType)7/17` résiduel**, **instrumentation temporaire retirée** (helper `Diag`/`DIAG-045`/`GetHRForException` absents — grep 0 match). **Preuves réelles** (worker net48 out-of-process, lecture seule stricte, `IHEB-PC\SQL2022`) : `25FA00993` (DISTRI_DEMO, vente cpta) HT 9750/TVA 1950/TTC 11700 = en-tête Sage ; `FC2600025` (NEW_EMA, achat cpta) HT 743/TVA 103,71/TTC 846,71 = **triple recoupement** OM=`F_DOCLIGNE`(D0/D20/D10)=en-tête ; batch NEW_EMA **20/20 puis 60/60 OK, 0 KO** (~10 s, session réutilisée TASK-023, pas de `Parallel.ForEach`), ventilation par taux recoupée `valo.Taxes`↔`F_DOCLIGNE`. **Réserve non bloquante correctement isolée** : défaut préexistant distinct de `ExtraireTaxes` (rollup HT document depuis `doc.DO_TotalHT` **nul sur certaines comptabilisées**, ex. `FC2501768` DO_TotalHT=0/DO_TotalHTNet=420 → `EcartArrondi` faux sur ~7/60 ; **la TVA par taux reste exacte**) — révélé en rendant les pièces enfin lisibles, relève de la ventilation TASK-004, **déjà capturé en TASK-046** (dépend de 045). Débloque la valorisation famille B (écran Factures, cache TASK-024) et le fallback Sage de TASK-043. | 2026-07-10 |
| TASK-041 | **Écran « Factures » (interrogation, filtre date obligatoire)** (`Declaration.API`/`Declaration.Application`/`Declaration.Infrastructure`/`declaration-tva-web`) : 2ᵉ entrée du menu INTERROGATION, **pivot facture** (`RT_ECHEANCE.EC_Id`), lecture seule stricte. **Back** : `GET /api/factures?debut&fin[+filtres/tri/page]` + `/distincts` (`FacturesController`), entités `FactureInterrogationRow`/`StatutDeclarationFacture`, DTO explicite `FactureInterrogationDto` (une propriété par colonne, `[JsonPropertyName]`, motif inclus), méthodes repo `GetFacturesInterrogation(Async|CountAsync|DistinctsAsync)`. **3 familles de colonnes** : **A** identité (`RT_ECHEANCE`, TTC=`EC_Montant`, Solde=TTC−Réglé, gratuit) ; **C** statut déclaration (agrégat `RT_AFFECTATION` : Réglé=Σ`AF_Montant`, Déclaré=Σ`AF_Montant WHERE DT_Id NOT NULL`, Reste=Réglé−Déclaré, **statut à 3 valeurs** NonDeclarable/Partiel/Total dérivé, `DT_Id` **lu jamais écrit** — TASK-028) ; **B** valorisation TVA depuis le **cache TASK-024** (`GRC_VENTILATION_SAGE_CACHE`, SUM BaseHT/MontantTva par EC_Id) sinon `null`+**motif** (jamais un 0 silencieux, jamais un forfait — piège ancien GRF ; Autre taxe/Écart/Escompte non ventilés en cache restent `null`). **Période bornée OBLIGATOIRE** (400 sinon) = garde-fou perf famille B. **Front** : écran `FactureInterrogation.tsx` câblé `App.tsx` (entrée « Factures » `soon`→`live`), période obligatoire (rien chargé sans période), grille dense, tri/filtres/pagination serveur, statut 3 valeurs + colonnes Réglé/Déclaré/Reste, cellules B « non valorisé » explicites. **Piliers respectés** : (1) `FacturesFromWhere` = fragment FROM/WHERE **unique partagé** liste + `COUNT` + distincts (leçon TASK-040 : compteur cohérent quelle que soit la page) ; (2) filtres `origine`/`statut` comparés à la **MÊME expression `CASE`** que la projection (`FactureOrigineExpr`↔`LibelleEcType`, `FactureStatutExpr`↔`FactureInterrogationRow.Statut` — répliquées à l'identique C#↔SQL, sinon le filtre mentirait) ; (3) `ORDER BY` whitelisté + tiebreaker `EC_Id` (anti-injection, pagination stable) ; (4) cache B sur **connexion de persistance distincte** (`CreatePersistenceConnection`, try/catch → non valorisé), **aucune lecture Sage synchrone de masse**, aucune jointure SQL cross-base ; (5) `DoDomaine=1` (Achat) = factures fournisseur uniquement. **Périmètre strict** : option 2 (cases à cocher, sélection, action « déclarer », écriture `DT_Id`, export) **différée** ; verrou TASK-028 non touché. **Vérifié par l'architecte** (source relue ↔ VERIFY) : garde 400 contrôleur, `FacturesFromWhere` partagé confirmé en source, expressions `CASE` SQL ↔ dérivations C# alignées ligne à ligne, DTO ↔ entité champ à champ, `AppliquerCacheB` (null→motif) et `EnrichirFamilleBDepuisCache` (SELECT-only, try/catch) relus, front `factures`→`live` confirmé. Builds : `Declaration.Infrastructure` **0 erreur** (2 warns NuGet préexistants), `Declaration.API` = seules erreurs MSB3026/3027 (verrou copie DLL, API en cours d'exécution ; **0 erreur C#**), `npm run build` (tsc+vite) **0 erreur TS**. **Preuves réelles `.\sql2022`/`GR_EMA_DISTRIBUTION`** (SO_Id=1, DO_Domaine=1, lecture seule) : 1408 factures fournisseur (`DO_Date` 2025-02-05→2026-06-29 ; EC_Type 0=1321/111=86/4=1) ; famille A `Solde=EC_Montant−ΣAF_Montant` vérifié aux deux bornes (FC2600902 Réglé=0→Solde=TTC ; FC2600001/2 Réglé=plein→Solde≈0) ; famille C = **0/3053 affectations avec `DT_Id`** (verrou jamais posé sur cette base) → toutes NonDeclarable en live, **logique 3 valeurs prouvée par simulation what-if SANS écriture** (CTE sur facture réelle EC_Id=21497, expression CASE identique à la projection : 0→NonDeclarable / 7000 partiel→Partiel / 40 569,76 tout→Total) ; famille B = **0/1408 dans le cache** → 100 % « non valorisé »+motif (EC_Type 0/111/4 rendus avec motif distinct, aucune ligne silencieuse, aucun forfait). **Réserves non bloquantes** (cadrages PO, pas des défauts) : (1) cas Partiel/Total **non observable en live** (0 `DT_Id` dans la base, verrou TASK-028 jamais posé ici) — preuve « bout en bout » à rejouer après une 1ʳᵉ déclaration ; (2) cache B vide → toutes non valorisées (cohérent avec la politique (a) retenue ; option (b) valorisation bornée à la demande = à cadrer, ne pas ouvrir de lecture Sage massive sans décision) ; (3) frontière option 2 (sélection/déclaration actionnable) = task suivante. | 2026-07-09 |
| TASK-039 | **Filtre `MV_Domaine IN (0,1)` sur l'endpoint rapprochement** (`Declaration.Infrastructure/Repositories/DeclarationRepository.cs`) : `RapprochementFromWhere` (fragment FROM/WHERE partagé par `GetReglementsRapprochementAsync` **et** `GetReglementsRapprochementCountAsync`) ne restreignait pas la nature du mouvement → un **bordereau de remise** (`BORD26060022`, `MV_Domaine=2`, entête de regroupement, pas un règlement) remontait dans la liste + risque de double comptage. Régression : filtre présent dès TASK-001 (`AND m.MV_Domaine = 1`), perdu à l'écriture de TASK-036. Ajout du prédicat `AND M.MV_Domaine IN (0, 1)` (0=encaissement, 1=décaissement : vrais règlements) à **3 emplacements identiques** — le WHERE partagé (l.287, donc liste **et** `COUNT` alignés : pagination + `TotalCount` cohérents) **et** les 2 sous-requêtes `distincts` de `GetReglementsRapprochementDistinctsAsync` (modes l.411 + origines l.419, pour que les valeurs de filtres ne référencent plus de mouvements exclus). **Décision de périmètre PO : `IN (0,1)` retenu** (pas `= 1`). **Périmètre strict** : aucun changement de contrat API/DTO (`MvDomaine` toujours projeté, `RapprochementDomaineExpr` inchangé), aucune écriture, aucun recalcul TVA, tampon `DT_Id` (TASK-028) non touché — 1 prédicat de lecture seule. **Vérifié par l'architecte** : les 3 filtres relus en source (l.287/411/419), constante `RapprochementFromWhere` bien partagée liste+COUNT confirmée, DTO intact. Build **0 erreur**, `Task036RapprochementProjectionTests` **15/15**. **Preuve réelle `.\sql2022`/`GR_EMA_DISTRIBUTION` (SO_Id=1, juin 2026)** : AVANT 713 lignes dont `BORD26060022` présent (répartition 99 dom.0 + 595 dom.1 + 17 dom.2 + 1 dom.5 + 1 dom.6) → APRÈS 694 (`BORD` absent, 19 non-règlements exclus = 17 bordereaux + 1 autre + 1 frais banc.) ; **0 `MV_Domaine` NULL** sur la période (aucun règlement légitime écarté) ; les 694 règlements 0/1 strictement conservés. Débloque TASK-042/043 (mêmes projection/DTO). | 2026-07-09 |
| TASK-040 | **Compteur « Règlements : N » aligné sur les filtres de la grille** (`Declaration.Infrastructure`/`Declaration.API`/`declaration-tva-web`) : les 3 filtres `numeroReglement`/`origine`/`domaine`, jusque-là appliqués **côté client sur la seule page courante** (`RapprochementInterrogation.tsx:146-157`) alors que le compteur affichait `totalCount` serveur global, sont **déplacés côté serveur** dans le `WHERE` partagé `RapprochementFromWhere` (appliqué **identiquement** à `GetReglementsRapprochementAsync` et `…CountAsync`) → `total`, pagination et liste coïncident, quelle que soit la page, **sans toucher la ligne d'affichage** (ligne 263 inchangée). **`numero`** = `M.MV_Numero LIKE '%…%'` ; **`origine`/`domaine`** = multi-sélection `IN @origines`/`IN @domaines`, gardés par `@hasOrigines`/`@hasDomaines` (0 = inactif, listes vides sans fausser). **Pilier anti-mensonge** : les prédicats `origine`/`domaine` comparent la **MÊME expression `CASE`** (`RapprochementOrigineExpr`/`RapprochementDomaineExpr`) que la projection `ReglementRapprochementRow.LibelleOrigine`/`LibelleDomaine` (`SansAffectation`/`Mixte`/`Sage`/`FGR`/`SoldeInitial`/`Autre (n)` ; `Encaissement`/`Décaissement`/`Frais bancaire`/`Autre (n)`), source unique — un filtre dérivé d'une autre expression aurait menti. Contrôleur : `[FromQuery] numero`, `origine[]`, `domaine[]` ; front : `params` en clés répétées (`paramsSerializer: { indexes: null }`), **filtrage client supprimé**, options `filterOptionsFor` exposant le **domaine métier complet** des libellés (décision étape 4 documentée : peupler depuis la page mentirait sous filtrage serveur). **Séquencement TASK-039 respecté** (prédicats ajoutés à la suite de `MV_Domaine IN (0,1)`, aucune collision). **Périmètre strict** : aucune écriture, aucun recalcul TVA, tampon `DT_Id`/tri/pagination/sémantique DTO inchangés — lecture seule stricte. **Vérifié par l'architecte** : expressions `CASE` SQL relues ligne à ligne ↔ `LibelleOrigine`/`LibelleDomaine` (correspondance exacte, y c. format `Autre (n)` et accents), interface `IDeclarationRepository` alignée, `WHERE` unique partagé liste+count confirmé en source, filtrage client bien retiré. Build `Declaration.Infrastructure` **0 erreur** + `Declaration.Orchestration.Tests` **43/43** (rejoués par l'architecte). **Réserve non bloquante** : preuve produite au niveau **SQL** sur moteur/base de production (`.\sql2022`/`GR_EMA_DISTRIBUTION`) — l'endpoint enveloppe exactement ce `WHERE` partagé (vérifié en code) — plutôt qu'un capture HTTP avant/après (API en cours = build antérieur ; non redémarrée pour ne pas perturber la session) ; répartition de référence `MV_Domaine IN(0,1)` (TOTAL 1116) et 4 `COUNT(*)` filtrés coïncidant avec les sous-totaux, dont le prédicat combiné accentué `Décaissement` (535). Commande de rejeu HTTP fournie au PO. | 2026-07-09 |
| TASK-038 | **Traçabilité TVA inline dans « Factures à déclarer »** (`declaration-tva-web` + back) : rendre visible **en colonne, sans clic**, l'**origine** de valorisation TVA (discriminant `EC_Type`) de chaque ligne, en plus du taux et du motif déjà présents ; preuve détaillée conservée **à la demande** via `ProofModal` (clic ligne), **aucun panneau latéral**. L'origine n'étant **pas snapshotée** dans la table figée, la donnée descend de bout en bout : entité `LigneCandidate.EcType` (nouveau champ) ← `MapLignesCandidates` (`EcType = c.Affectation.EC_Type`, 3 blocs) ← INSERT `SaveLignesCandidatesAsync` ← colonne SQL `[EcType] INT NOT NULL DEFAULT 0` (`001_Schema_TVA.sql` + `DeclarationTVA.sql`) → `LigneCandidateDto.Origine` (`[JsonPropertyName("origine")]`) → colonne front `{ key: 'origine' }` dans `defaultColumns`. **Mapping EC_Type→origine = source UNIQUE** : `ReglementRapprochementRow.LibelleEcType(int)` (existant TASK-036) rendue `public static` et **réutilisée** par le DTO — pas de mapping dupliqué, vocabulaire strictement identique entre l'écran Rapprochement et l'écran Déclaration (Sage/FGR/SoldeInitial/Autre (N)). **Périmètre strict** : aucun changement de calcul TVA (TASK-030) ni de sélection, décisions inchangées, `ProofModal` intact, aucune nouvelle dépendance projet, aucun couplage `gocom-web`. **Vérifié par l'architecte** : REJET initial sur duplication du mapping (dette silencieuse, incohérence de vocabulaire) → correction re-soumise conforme (source unique confirmée en source, `MapOrigine` supprimé), diffs relus fichier par fichier ; build back `dotnet build DeclarationTVA.slnx` 0 erreur, build front `tsc + vite` OK, `oxlint` 0 erreur, `Declaration.Orchestration.Tests` 43/43. **Réserves non bloquantes** : (1) capture navigateur e2e impossible dans l'environnement (grille sur API réelle, pas de chemin mock `/lignes`, stack SQL+COM Sage requise) — vérif par compilation/typage + trace de flux ; (2) filtre liste `origine` dégrade proprement (dropdown vide, `distincts` non calculés côté serveur — comportement identique à `source`/`tauxTVA`, aucune régression) ; (3) **données historiques** : déclarations déjà figées ont `EcType=0` → affichées « Sage » — re-figeage nécessaire pour l'exactitude, aucun back-fill (hors périmètre). Étape #5 (dernière) de l'ordre PO 034→035→036→037→038. | 2026-07-09 |
| TASK-037 | **Écran « Rapprochement bancaire »** (front `declaration-tva-web`, `RapprochementInterrogation.tsx`) : interrogation **globale, lecture seule, pivot règlement**, consommant `GET /api/rapprochement` + `/distincts` (TASK-036). Grille dense pleine H/L (virtualisation `@tanstack/react-virtual`), 10 colonnes (N° règlement/date/mode/tiers/montant/**rapproché banque** badge O/N/nb factures/**reste à affecter**/origine/**déclaré**), tri serveur whitelisté (date/montant/reste), filtres `ExcelFilter` (`selectedValues || []` → pas de régression TASK-029) scalaires côté serveur (mode/rapprocheBanque/declare/tiers) + filtre n° borné à la page (honnête), pagination, période bornée obligatoire (défaut 1er janv.→aujourd'hui). **Pilier confiance — reste à affecter ≠ 0 rendu VISIBLE** (orange gras + ⚠ en grille, encart d'alerte dans le drill « aucune somme absorbée en silence »). Drill-down **à la demande** (panneau latéral, pas permanent — « 0 espace perdu ») : décomposition (montant / affecté / reste explicite) + état & preuve TVA (rapproché banque, **origine `EC_Type` OM/FGR** = pointeur de preuve, déclaré/verrou). Câblé dans le shell TASK-035 (remplace le `Placeholder` de l'entrée « Rapprochement bancaire »). **Lecture seule stricte** : aucune écriture, aucun verrou, aucune modif back, aucun couplage `gocom-web`, aucune donnée factice (empty-state honnête). **Décision PO (2026-07-09) : drill-down AGRÉGÉ accepté** — l'endpoint TASK-036 (contrat gelé, approuvé) n'expose que l'agrégat par règlement (nb factures + montant affecté + reste + origine) ; TASK-037 interdit toute modif back → la ventilation facture-par-facture relève du poste « Déclaration TVA », pas de l'interrogation. Le dev a arbitré dans le sens des règles absolues (pas de modif back + jamais de données factices) et documenté la limite. **Vérifié par l'architecte** : source relu ↔ VERIFY (colonnes, tri/filtre/pagination, reste visible, drill à la demande) ; contrats croisés OK — `ExcelFilter` (`filterType/options/selectedValues/textValue/onChange`), DTO `ReglementRapprochementDto` (clés `[JsonPropertyName]` = clés lues par le front, `mode` filtre = code int des distincts, `rapprocheBanque`/`declare` = bool), aucun champ inventé. Build `tsc -b && vite build` OK (seul avert. pré-existant `INEFFECTIVE_DYNAMIC_IMPORT`), `oxlint` 0 nouveau warning. Preuves navigateur (mock-contrat jetable non commité, produit sans donnée embarquée) : grille 8 règlements, tri reste_desc (15 750→0), filtre Rappr.=Oui (8→5), empty-state, drill RG-0005 (reste ⚠ 15 750, encart écart), endpoints 200. **Cœur de valeur du module (rapprochement + TVA) rendu visible sans angle mort.** | 2026-07-09 |
| TASK-036 | **Endpoint d'interrogation « Rapprochement bancaire » global** (`GET /api/rapprochement` + `/distincts`, lecture seule stricte, indépendant de toute déclaration) : pivot **règlement** (`RT_MOUVEMENT.MV_Id`) joignant `RT_AFFECTATION`/`RT_ECHEANCE`, exposant montant/mode/date/tiers, **rapproché banque** (`MV_Point`), nb factures affectées + montant affecté + **reste à affecter explicite** (montant − Σ affectations, jamais absorbé), **origine** (`EC_Type`→Sage/FGR/SoldeInitial/Mixte/SansAffectation), **déclaré** (`DT_Id`, lu jamais écrit). DTO explicite `[JsonPropertyName]` (discipline TASK-034), période **bornée obligatoire** (400 sinon), pagination `OFFSET/FETCH`, `ORDER BY` whitelisté (anti-injection), params Dapper, `IDbConnectionFactory.CreateGrfConnection()` (aucune string/port en dur). **SELECT-only prouvé** : les 3 méthodes repo n'utilisent que `QueryAsync`/`ExecuteScalarAsync<int>`, aucun `INSERT/UPDATE/DELETE`, aucun appel DLL ; tampon `DT_Id` (TASK-028) et endpoint `/lignes` scellé **non touchés**. **Vérifié par l'architecte** : méthodes repo relues (lignes 305–386, 100 % lecture), UPDATE du fichier confinés aux méthodes tampon TASK-028 préexistantes ; VERIFY conforme. Build `Declaration.API` 0 erreur, `Task036` 15/15 tests, **preuve réelle `GR_EMA_DISTRIBUTION` (SO_Id=1)** : `COF25120086` montant 3798.00 / affecté 3797.42 / **resteAAffecter 0.58 exposé**, `BORD26040040` non rapproché reste=montant total, filtre `rapprocheBanque=true` (totalCount 673), distincts origines Sage(0)/FGR(111)/SoldeInitial(4), borne absente → HTTP 400. **Débloque TASK-037** (chemin critique 036→037). Points à confirmer côté front : route préfixée `api/`, libellés `Mixte`/`SansAffectation`, pas de filtre `MV_Domaine` (vue globale voulue). | 2026-07-09 |
| TASK-035 | **Shell de navigation « menu groupé » + écran dense** (front-only `declaration-tva-web`) : sidebar remplacée par un **menu latéral groupé à 3 sections** — INTERROGATION (Rapprochement bancaire 🟢 live · Factures ⚪ soon) / DÉCLARATION (Déclaration TVA 🟢 live · Relevé de déductions ⚪ soon) / À VENIR (RAS · Télédéclaration SIMPL · Tableau de bord — 🔒 todo grisés, **non cliquables**). Config déclarative `MENU_GROUPS` dans `App.tsx` (état `activeSection`, défaut `'declaration'` → aucune régression du flux existant) ; entrées `todo` désactivées (`handleSelect` retourne tôt, `aria-disabled`, cadenas), aucune route morte ; entrées `soon` → composant `Placeholder` **honnête** (« Écran en cours », aucune donnée factice). Routage : `declaration` → flux existant `DeclarationList` → `DeclarationStepper` (poste de travail « Factures à déclarer ») **inchangé** ; `rapprochement`/`factures`/`releve` → placeholder honnête (écran réel = TASK-037). **Densification** : `.main-content` padding 0 + `min-width:0`, sidebar 224px `flex-shrink:0` (collapsed 60px), paddings réduits ; `DeclarationList` conteneur `flex:1`+`overflowY:auto` pour scroll interne. **Front-only strict** : aucun changement d'API/calcul/workflow, aucune dépendance `gocom-web`/GRC_WEB (imports = `lucide-react` + composants locaux). **Vérifié par l'architecte** : source relu ↔ VERIFY (menu, statuts, non-clic, routage, densité conformes à la cible IA), `npm run build` (`tsc -b && vite build`) **rejoué 0 erreur** (seul avert. pré-existant `INEFFECTIVE_DYNAMIC_IMPORT`), `npm run lint` (oxlint) **rejoué : 0 erreur**, warnings pré-existants uniquement, aucun nouveau dans `App.tsx`/`index.css`/`DeclarationList.tsx`. Rend la valeur ajoutée (portée du produit) visible sans la construire toute — pas de dérive vers les écrans « à venir ». (Note : le `M DomainGrid.tsx` du working tree relève de TASK-034, hors périmètre 035.) | 2026-07-09 |
| TASK-034 | **Contrat DTO explicite des lignes candidates** (`undefined%` / `0,00 MAD` dans le poste de travail) : l'endpoint `GET /declarations/{id}/lignes` renvoyait `LigneCandidate` **brute**, sérialisée en camelCase par défaut d'ASP.NET (1re lettre seulement → `HT`→`hT`, `TVA`→`tVA`, `Taux`→`taux`, `TiersNom`→`tiersNom`, `Etat`→`etat`, `Conformite`→`conformite`…) ≠ clés lues par le front (`montantHT`, `tauxTVA`, `tiers`, `statutLigne`, `statutConformite`) → `row[col.key]=undefined` → `` `${undefined}%` `` et `formatMoney(undefined)`=« 0,00 MAD ». **Option A** (reco architecte) : nouveau `Declaration.API/Dtos/LigneCandidateDto.cs` — projection **pure lecture seule** avec `[JsonPropertyName]` figés sur le vocabulaire front (`statutLigne`=`(int)Etat`, `statutConformite`=`Conformite.ToString()`), **aucun recalcul TVA** (valorisation TASK-030 inchangée) ; `DeclarationsController.GetLignes` projette `lignes.Select(l => new LigneCandidateDto(l))`. Front : colonne « Désignation » **retirée** de `defaultColumns` (décision PO — aucune source, pas de colonne muette). **Non-régression filtres/tri vérifiée** : la projection ne touche que la réponse ; les clés de requête (`etat`, tri whitelisté) sont parsées indépendamment par le repo — inchangées. **Vérifié par l'architecte** : DTO ↔ entité `LigneCandidate` alignés champ à champ (source relue), `dotnet build` **0 erreur** rejoué, build front `tsc+vite`/`oxlint` OK annoncés, capture live 4 onglets sur `GR_EMA_DISTRIBUTION` (déclaration `TVA001-2026-01`, 247 lignes) — taux « 20% » / HT / TTC réels, plus aucun `undefined%` ni `0,00 MAD` parasite (les `0%`/`0,00 MAD` résiduels = valeurs réelles des lignes non valorisées). Prérequis d'affichage de tout le Track A front (034 → 035/036/037/038). | 2026-07-09 |
| TASK-023 | **Session Sage réutilisée** (`EC_Type=0`) : mode lot `SageTaxReaderService.LireFactures` — la session COM Sage est ouverte **une seule fois** sur un **unique thread STA** dédié (`1 OpenSession → N ReadPiece → 1 CloseAndRelease`) qui consomme les pièces via une `BlockingCollection`, au lieu d'un `Open/Close` par facture. Invariants **cachés hors boucle** : devise société (1 lecture/lot) + types de taxe (`cacheTypesTaxes` par code). **Timeout PAR PIÈCE** (30 s, identique à la lecture unitaire) rétabli via `SemaphoreSlim` côté thread appelant — plus de timeout global. **Isolation d'erreur** : nouveau helper `EntreeEnErreur` + champs DTO `EnErreur`/`MotifErreur` (rétro-compatibles JSON) → une pièce introuvable/sens inconnu/exception COM/timeout ressort avec sa clé `piece_sens` présente (N traité = N demandé, zéro drop silencieux) ; sur exception la boucle poursuit, sur timeout (thread STA figé, `IsBackground`) le lot est arrêté proprement et les pièces restantes ressortent en erreur avec motif. Branchement consommateur : `ConstructeurDeclaration` émet l'alerte `Error` `FACTURE_ILLISIBLE_OM` (jamais de ventilation ni ligne silencieuse), `OrchestrateurDeclaration` refuse de matérialiser une pièce `EnErreur` en cache SQL. **❌ Aucun `Parallel.ForEach` / multi-session** (contrainte d'architecture respectée). **Re-soumis après rejet initial** (timeout global unique de ~8h + drop silencieux `Console.Error` → corrigés) et **rectification honnête** d'un précédent VERIFY affirmant à tort « non mesurable, pas de serveur COM Sage ». **Preuves réellement exécutées** (OM Sage 100c + `DISTRI_DEMO`/`IHEB-PC\SQL2022`, banc 12 pièces réelles) : 12/12 montants HT/TVA/TTC/lignes **identiques** unitaire vs batch (avoirs, gros achats, multi-taxes FODEC/TVA/TF, pièces sans taxe) ; **1 seule** ouverture de session ; chrono **30,04 s → 2,84 s (×10,6)** ; isolation prouvée (lot de 3 dont 1 inexistante → 3 retournées, 1 `EnErreur`, lot poursuit `rc=0`). **Vérifié par l'architecte** : source relu (session unique + timeout par pièce + `EnErreur`/`FACTURE_ILLISIBLE_OM` + garde cache), `dotnet build DeclarationTVA.slnx` = **0 erreur** rejoué, 76 tests / 0 échec (crash teardown net10.0 confirmé préexistant). Réserves non bloquantes : (1) timeout d'une pièce interrompt le reste du lot (transparent, jamais silencieux) ; (2) perf mesurée sur base démo 12 pièces (extrapolation au volume prod 2085 argumentée : l'`Open()` domine) ; (3) TFM worker net48 vs `net8.0-windows` de la spec — préexistant, aucune frontière d'assembly modifiée. Optimise le chemin OM lui-même et sert d'accélérateur au 1er import de TASK-024. | 2026-07-09 |
| TASK-028 | **Verrou d'intégration déclaration** (tampon `RT_AFFECTATION.DT_Id` + triggers d'immuabilité, portée globale GRFN inclus). Livrables : `003_Verrou_DT_Id.sql` (index non filtré `(MV_Id, DT_Id)` + `TR_RT_AFFECTATION_Immuabilite` : DELETE et UPDATE financier — `AF_Montant`/`MV_Id`/`EC_Id`/`AF_Date` — refusés si `DT_Id NOT NULL`, transitions `DT_Id` val→NULL et NULL→val autorisées ; `TR_RT_MOUVEMENT_Immuabilite` : `MV_Point 1→0` et `MV_Compta 1→0` refusés si ≥1 affectation déclarée), triggers **set-based** + `THROW 50028` avec n° déclaration ; côté module `TamponnerAffectationsAsync`/`DetamponnerAffectationsAsync` (`RT_*` uniquement, batch 1 000, `GrfConnection`). **Re-soumis après REJET PO** : le trou d'intégrité **silencieux** (collision de `dtId = Math.Abs(Guid.GetHashCode())` + détamponnage non borné `WHERE DT_Id=@dtId` libérant le verrou d'une AUTRE déclaration) est fermé — `DeriveDtId = GetHashCode() & int.MaxValue` (plus d'`OverflowException`) et détamponnage **borné aux `MV_Numero` de la déclaration** (miroir de la pose). Preuves : 10 scénarios sur base isolée `GRF_TASK028_TEST` en `QUOTED_IDENTIFIER OFF` (pire cas legacy) tous ✅, + **preuve de collision `GRF_TASK028_COLL`** (réouverture A → A libérées, B toujours verrouillées ; contre-preuve ancien SQL → B tombe à 0). C1 (SET options anti-msg 1934) et C2 (index filtré→ordinaire pour ne pas casser les writers GRFN/Sage en QI OFF) appliqués. `AF_Taux` confirmé absent de `RT_AFFECTATION`. Build 0 erreur, tests non-régression verts (Core 28, Selection 14, Orchestration 28, Export XML 4, Excel 1, Controle 1). **Vérifié en source par l'architecte** (`DeriveDtId` ligne 337, détamponnage borné lignes 328-333). Débloque l'immuabilité exigée par TASK-024. | 2026-07-09 |
| TASK-026 | Réparer `Declaration.Core.Tests` + intégrer les projets de tests au `.slnx` (dette révélée par la revue TASK-022) : migration des lambdas `(id, sens)` → `Func<AffectationADeclarer, DocumentTaxesInfo?>` (CS1593 résorbé), ajout des **5 projets de tests hors solution** au `.slnx` (`Core.Tests`, `Orchestration.Tests`, `Selection.Tests`, `Export.Excel.Tests`, `Export.Xml.Tests` → 6 au total avec `Controle.Tests`) pour que « build/test solution » soit exhaustif, et **3 tests du chemin FGR** de `ConstructeurDeclaration` (`FgrValidationException`→`ECART_FGR`+document partiel ventilé ; `InvalidOperationException`→`CODE_TAXE_INCONNU`+aucune ventilation ; `EC_Type=4`→`SOLDE_INITIAL_NON_GERE` sans appel à `resoudreFacture`). Périmètre strict tenu (tests + `.slnx`, aucun code de production modifié). **Rejoué par l'architecte** : `dotnet build DeclarationTVA.slnx` = 0 erreur, `dotnet test` (hors `Controle.Tests` DB, filtrable comme prévu par la TASK) = **75 réussites / 0 échec** (Core 28, Orchestration 28, Selection 14, Export.Xml 4, Export.Excel 1), code source ↔ assertions vérifiés. | 2026-07-09 |
| TASK-025 | Cadrage TVA du solde initial (`EC_Type=4`) : inventaire réel sur `GR_EMA_DISTRIBUTION` — **34 lignes / 854 793,29 MAD**, 2 sous-types (33 coffres fin d'exercice `S-COF…` = à-nouveaux de trésorerie client + 1 solde initial pur `SI- 4411ELEC` = frais de retard fournisseur). **0 TVA déclarable** prouvée sur données réelles (`RT_HISTCOMPTA` vide pour les 34, `MV_Tva=0`/`MV_TvaMontant=0` sur mouvement lié, comptes trésorerie/client jamais TVA) ; **0 occurrence dans les déclarations TVA passées** (`RT_LigneDeclarationTva`). **Décision PO : maintenir l'alerte (statu quo)** — l'alerte explicite `SOLDE_INITIAL_NON_GERE` (TASK-022) reste active pour tout `EC_Type=4`, aucune exclusion, aucune implémentation (règle n°1 : aucune ligne silencieuse). Investigation lecture seule, requêtes SQL reproductibles livrées. | 2026-07-09 |
| TASK-024 | Cache des ventilations Sage (`EC_Type=0`) : table **SQL Server exclusive** `GRC_VENTILATION_SAGE_CACHE` (clé `(EC_Id, Taux)`, buckets par taux + token de paiement `(MV_Id, MV_Point)` + audit `DateLecture`/`Source`). Écriture à la 1re lecture OM, relecture 100 % SQL. **Fraîcheur = paiement** : validation à la lecture (`RT_AFFECTATION`/`RT_MOUVEMENT`, `MV_Point=1`) — token divergent/absent → cache non servi, OM rappelé (aucun code GRFN modifié, aucun trigger de purge). `VentilationSageCacheRepository` **SQL Server exclusif via MERGE** (fallback SQLite + `Data Source=tva.db` **retiré** après rejet architecte). Preuves : 8 tests unitaires orchestrateur (stub in-memory) + **IT1-IT6 d'intégration réels sur `.\sql2022`/`GR_EMA_DISTRIBUTION`** (DDL, MERGE idempotent, 2e lecture 0 OM, dépointage→OM, égalité cache↔OM), EC_Id ≥ 900000 isolés + teardown. Build 0 erreur. | 2026-07-09 |
| TASK-009 | Mode Contrôle (`Declaration.Controle`) : recalcul corrigé vs résultat GRFN stocké (`RT_LigneDeclarationTva`), aligné par clé `(NumeroFacture \| Taux \| NumeroRapprochement)`, tolérance 0,05. **Comparaison réelle sur déclaration EMA `DT_Id=66` (06/2026, `GR_EMA_DISTRIBUTION`/`SO_Id=1`, lecture seule stricte)** : 4 249 lignes GRFN, 3 173 concordants (99,6 % sur taux>0), 14 `ManquantRecalcul` (bug legacy `DT_Id` posé partiellement sur mouvement multi-factures, chiffrés/causés), 95 `ManquantGRFN` (sauts silencieux ancien module : « Solde=Montant », chèques non rapprochés, espèces hors période — Σ\|AF_Montant\|=475 811,82 MAD, ventilés par cause). Corrections `SelectionnerAffectationsService` (JOIN `RT_ECHEANCE`→`E.DO_Numero` + filtre date conditionné à `dtId`) vérifiées en source ; `GrfnDeclarationRepository` `SELECT` seul ; build 0 erreur. Réconciliation §2/§3.2/§5 (Σ\|AF\| vs Σ algébrique, écart 80 028,30 documenté) corrigée avant approbation. | 2026-07-09 |
| TASK-019 | Poste de travail 4 interrogations (Rapprochement · Affectation · Conformité IF/ICE · Factures à déclarer, 2 faces) — front sur API réelle (`GR_EMA_DISTRIBUTION`/`SO_Id=1`), supersède TASK-018. Réconciliation 5 segments `total = candidates = intégrées+exclues+reportées+écartées+proposées` (règle TASK-013, aucune ligne silencieuse), 3 vues de preuves différenciées (colonnes par domaine + prop `domaine`), actions de masse (Exclure/Réintégrer/Forcer report), clôture verrouillée tant qu'il reste des lignes à traiter. 5 captures e2e Playwright (1 passed), build tsc+vite + oxlint 0 erreur. Bug TVA 0% diagnostiqué = correctif backend Gap A extrait et tracé sous **TASK-030**. | 2026-07-09 |
| TASK-030 | Correction Gap A partiel (Ventilateur et FGR) : lignes avec TVA = 0% et TTC = 0,00 signalées au test E2E de TASK-019, corrigées dans le backend. Filtre `taxe.Type` de `Ventilateur.cs` trop restrictif → excluait silencieusement les taxes FGR (Type `""`) et OM (enum résolu `"0"`). Ajout du type explicite `TaxeTypeTVA` dans `LecteurTvaFgr.cs` + assouplissement du filtre (`"0"`/`""` acceptés). Preuves réelles `GR_EMA_DISTRIBUTION` (taux 20, tva 75,3, ttc 451,8). | 2026-07-09 |
| TASK-021 | Avenant back (Gap B) : sélection des reportées (non-rapprochées `MV_Point≠Oui`, datées `MV_Date`) marquées `Reportee` + motif « Règlement non rapproché » ; garde-fous universels priment sur `NonRapproche` (motif honnête) ; volume réel 304 reportées / 1309 candidats, réconciliation bouclée. Tests 13/13 hors DB. | 2026-07-08 |
| TASK-002 | Worker autonome `SageTaxReader` : lecture facture achat/vente + extraction taxes par taux via Objets Métier Sage (COM) | 2026-07-08 |
| TASK-003 | Nombre de décimales d'arrondi (`n`) lu dynamiquement depuis la devise société via OM (`IBPDossier2.DeviseCompte` → `IBPDevise2.D_Format`) | 2026-07-08 |
| TASK-004 | Ventilation / proratisation : cœur de calcul pur (`Declaration.Core.Ventiler`) — DTO neutres extraits dans `SageTaxReader.Contracts`, calcul `decimal`, prorata direct + `AwayFromZero` | 2026-07-08 |
| TASK-005 | Modèle déclaration + contrôle : `ConstruireDeclaration(...)` pur (`Declaration.Core`, net10.0) — lignes enrichies, récaps source/taux/activité, contrôle d'équilibre à écart nul, 6 familles d'alertes | 2026-07-08 |
| TASK-006 | Corrections revue TASK-004/005 : contrôle d'équilibre réel (résidu non-TVA décomposé affecté-vs-déclaré, plus de tautologie), alerte `TIERS_SANS_IF` symétrique de `TIERS_SANS_ICE`, `Prorata`+`Designation` portés sur `LigneDeclarationEnrichie`, test blanc `Any(char.IsWhiteSpace)` | 2026-07-08 |
| TASK-001 | Vérification synchro rapprochement GRF↔Sage sur base prod `GR_EMA_DISTRIBUTION` : source locale `MV_Point`/`MV_PointDate` prouvée suffisante (352/352 sync, dates identiques, 1008/1008 déclarations historiques hors espèces) → option B validée, module fondé sur le local uniquement | 2026-07-08 |
| TASK-007 | Orchestration + cache : `OrchestrateurDeclaration` (`Declaration.Orchestration`, net10.0 pur) relie worker OM out-of-process ↔ ventilation ↔ `ConstruireDeclaration` ; cache `(facture, sens)` (1 lecture/facture prouvée), invocateur process séparé avec timeout worker robuste (lecture async + WaitForExit borné + Kill), erreur/timeout → alerte sans crash | 2026-07-08 |
| TASK-008 | Sélection SQL locale (GRF) : adaptateur `Declaration.Selection`, `SelectionnerAffectationsService` paramétré, domaines et requêtes testées sur `GR_EMA_DISTRIBUTION` | 2026-07-08 |
| TASK-014 | Cadrage mapping domaines → formulaires & schémas XML DGI (Simpl-TVA) : regroupement décaissement/dépense/frais dans Relevé de déductions, schéma CA introuvable (escalade PO) | 2026-07-08 |
| TASK-015 | Sélection expliquée (surensemble + motifs) : brique `SelectionExpliqueeService` abolissant les sauts silencieux. Code en mémoire (évaluateur) et requêtes validés par test d'intégration stricte vs TASK-008. | 2026-07-08 |
| TASK-013 | Front web `declaration-tva-web` (React 19/Vite/TS) : workflow par domaine (stepper création→intégration→checkup→clôture→génération) sur mock serveur. Grilles paginées/filtrées/triées côté serveur + virtualisation, statut système `Écartée`+motif filtrable (Règle n°1), checkup avec réconciliation/équilibre/récaps (source/taux/activité) + gating clôture, génération multi-artefacts. Build tsc+vite OK, e2e Playwright vert. | 2026-07-08 |
| TASK-010 | Export Excel (artefact dépôt/archive) : adaptateur `Declaration.Export.Excel` (`net10.0` pur, ClosedXML, sans COM Office ni Sage/SQL) sérialisant `DeclarationModele` → `.xlsx` 2 feuilles (Détail = 1 ligne/enrichie ; Récap = totaux source/taux/activité + contrôle d'équilibre/résidu + alertes). Test de génération sur fixture (1/1 vert), .xlsx d'exemple conforme (2 feuilles Détail+Récap). | 2026-07-08 |
| TASK-011 | Export XML Simpl-TVA + zip : adaptateur `Declaration.Export.Xml` (`net10.0` pur) sérialisant `DeclarationModele` → XML zippé (format dépôt DGI). Format reconstitué via rétro-ingénierie stricte sans dépendance GRFN (`<ord>`, `<mht>`, `.`, mapping paiement 1-7). Validation Maroc bloquante (IF 8, ICE 15 sans espaces). Aucun re-arrondi `ToEven`. Modèle enrichi (`EnTeteDeclaration`, `IsReport`). | 2026-07-08 |
| TASK-016 | Ajustements UI : Drill-down depuis le checkup vers la grille filtrée avec maintien du contexte. Refonte de l'action de masse sur filtre (mockServer, UI, contrat API mis à jour dans TASK-012). Tests e2e Playwright ajoutés. Alimentation du récapitulatif par code d'activité branché sur `F_COMPTET` via SQL. | 2026-07-08 |
| TASK-012 | API TVA (`Declaration.API`, Clean Architecture alignée GRC_WEB, JWT Bearer, Dapper) : persistance déclaration isolée (SQLite `tva.db`, écritures limitées à `DeclarationEntete`/`LigneCandidate`, zéro `RT_*`), numérotation `TVA{Societe}-{Exercice}-{Periode}` + unicité 409, endpoints paginés/filtrés/triés côté serveur, état par ligne persisté (unitaire + bulk par ids ou domaine/filtre). **Transparence (règle n°1)** prouvée via `FixtureSelectionExpliqueeService` (2 éligibles Proposée + 3 écartées avec `MotifRejet` explicite, aucune ligne silencieuse). Checkup réel (`GetCheckupAsync`, non mock) exposant les exclues en alerte Info + équilibre ; clôture gardée (alerte Error `AUCUNE_LIGNE_INTEGREE` → 400, preuve négative). Injection SQL `ORDER BY` corrigée (whitelist). Génération/fichiers (TASK-010/011) et contrôle (TASK-009) honnêtement délégués en 501. Captures réelles vérifiées ligne à ligne contre le code source. | 2026-07-08 |
| TASK-017 | Câblage figeage API sur prod réelle + montants réels (avenant TASK-012) : `DbConnectionFactory` lit la config (multi-client, aucune chaîne en dur), vraie chaîne GRF transmise au workflow, bascule Production → sélection réelle sur `GR_EMA_DISTRIBUTION` (`SO_Id=1`). Étape A : `GET /lignes` remonte les vraies candidates, écartées avec motif visible (dont échec worker → `Exclue`). Étape B : forfait 20 % **retiré**, HT/Taux/TVA/TTC issus du worker OM out-of-process + ventilation (cas taux réel 10 % prouvé), comparaison **source (worker) ↔ figé (SQLite)** strictement identique sur échantillon. Checkup à résidu 0 / clôture OK sur déclaration non vide, isolation confirmée (aucune écriture `RT_*`). | 2026-07-08 |
| TASK-022 | Routing par `EC_Type` + lecteur TVA FGR en SQL : `AffectationADeclarer` enrichi (`EC_Type`/`EC_Id`), dispatcher orchestrateur (111→FGR SQL `RT_HISTCOMPTA`, 0→OM inchangé, 4→alerte `SOLDE_INITIAL_NON_GERE`), `LecteurTvaFgr` (reconstruction par `HC_Indice`, contrôle Σ(HT+TVA)=TTC → `FgrValidationException` avec document partiel, aucun drop silencieux). **Taux résolu via `F_TAXE.TA_Taux` (connexion Sage), sans parsing de `HC_TaxeCode`** ; code absent de `F_TAXE` → alerte `CODE_TAXE_INCONNU`. Deux connexions (GRF `RT_HISTCOMPTA` + Sage `F_TAXE`) propagées. Build solution 0 erreur (vérifié par l'architecte), Orchestration.Tests 28/28 (7 tests FGR mono/multi/exo/écart/bucket/C20/inconnu). **✅ Preuve réelle fournie (VERIFY re-soumis 2026-07-09, re-exécuté en direct par l'architecte sur `.\sql2022`/`GR_EMA_DISTRIBUTION`)** : `FF260070`/`EC_Id=22297` = TTC 451.80 = HT 376.50 (`D20`) + TVA 75.30 (Σ=TTC, conforme spec §47) + 2 autres taux réels (`D10`, `D18`) ; part hors OM **56/745 = 7,52 %** ; perf batch 56 FGR ≈ 0,28 s (vs 28-56 s OM). Multi-taux/exonérée **absents de la base** (86 FGR mono-bucket, prouvé par comptage réel) → couverts par tests unitaires, jamais maquillés. `Declaration.Core.Tests` → **TASK-026 approuvée**. Réserve résiduelle #2 (SQL `F_TAXE` non exercé par les tests) non bloquante. | 2026-07-08 (preuve réelle 2026-07-09) |
| TASK-020 | Avenant back (Gap A) — n° de règlement sur la ligne de déclaration : `NumeroRapprochement` ajouté à `LigneCandidate`, renseigné depuis `Affectation.NumeroRapprochement` (=`M.MV_Numero`, déjà réel via sélection TASK-017) dans les **3 branches** de `MapLignesCandidates` (non-éligible / éligible sans taxes / éligible avec taxes), persisté (`INSERT`) et relu (`SELECT *`/Dapper, `NULL`→`""` pour l'historique) dans `DeclarationRepository`, schéma `DeclarationTVA.sql` mis à jour, exposé dans `GET /declarations/{id}/lignes` → regroupement par règlement possible côté front. Périmètre étroit respecté (pas de reshaping, pas de modif sélection/éligibilité). Conformité IF/ICE (§2) **reportée en 2ᵉ vague** (décision PO 08/07). Test unitaire `MapLignesCandidates_ConserveNumeroRapprochement_PourToutesLesBranches` (3 branches, même `REG-001` sur 4 lignes) ; build + `Declaration.Orchestration.Tests` **13/13** rejoués par l'architecte. **⚠️ Réserve : preuve de regroupement fournie sur données mockées, pas sur `GR_EMA_DISTRIBUTION` — la branche éligible-avec-taxes n'est prouvée qu'au niveau test unitaire ; source `MV_Numero` déjà prouvée réelle en TASK-017.** | 2026-07-08 |
| TASK-027 | Conformité IF/ICE calculée au DTO + extraction du validateur TASK-011 : règle IF/ICE extraite dans `Declaration.Core/ValidationIdentiteFiscale.cs` (source unique — `EstIfValide`/`EstIceValide`/`Evaluer`/`ValiderPourExport`), exporter XML rebranché sans régression (messages d'exception conservés, `Export.Xml.Tests` inchangés), `LigneCandidate.Conformite` (getter lecture seule → `EtatConformite`) exposé par ligne dans `GET /lignes`. Build 0 erreur, `Core.Tests` **25/25** + `Export.Xml.Tests` **4/4** (rejoués par l'architecte). Réserves non bloquantes : warnings `xUnit1012`/`CS8602` pré-existant, `Core.Tests` hors `.slnx` (TASK-026). | 2026-07-08 |
| TASK-018 | Refonte Checkup en hub « tour de contrôle » (front `declaration-tva-web`, mock-first) : le tunnel linéaire est remplacé par un hub d'accueil (`DeclarationStepper` affiche `CheckupPanel` par défaut, stepper linéaire retiré). Héros = preuve de transparence : équilibre `Σ sources = Σ candidates` + écart (badge vert à 0), **barre de ventilation 5 segments** (Intégrées/Exclues/Reportées/Écartées/Proposées, « 0 ligne silencieuse ») avec chips cliquables → drill-down filtré (réutilise TASK-016). Tuiles source (avancement + reste-à-décider) → drill-down domaine. Bouton **Clôturer verrouillé** dans le hub avec libellé du reste-à-faire (« Clôturer · N à traiter »). Récaps masqués si vides. 6 règles de transparence TASK-013 préservées. Build `tsc -b && vite build` + `oxlint` OK, e2e Playwright vert. Avenant contrat checkup (ventilation 5 statuts) signalé à TASK-012. **⚠️ Approuvé sur mock — Gate PO résiduelle : rendu à valider sur données réelles (`GR_EMA_DISTRIBUTION`, TASK-001/017) avant mise en production.** | 2026-07-08 |
| TASK-059 | Écran ⑥ Synthèse & Export (front `declaration-tva-web`) : nouveau `SynthesePanel.tsx` câblé dans `DeclarationStepper.tsx` (remplace le placeholder `GenerationPanel` pour l'étape ⑥ du tunnel 053→059). Bandeau synthèse dense (source unique `GET /checkup`, aucun recalcul front : nb lignes intégrées, TVA/HT/TTC totaux, écart équilibre, état ✔/🔴/🔒) + répartition par taux (lecture seule). Actions câblées sur l'existant : Export Excel (TASK-010) / Générer XML (TASK-011) via `POST /generation`, Clôturer via `POST /cloture` (garde back TASK-012 conservée). Garde-fou : export **et** clôture `disabled` tant qu'une alerte `type=bloquant` subsiste ; clôture aussi désactivée si déjà clôturée. Aucun état factice (501 remonté honnêtement en toast). Modules export/clôture back **non retouchés**. `tsc/build/lint` verts ; anti-régression rejouée `Export.Excel.Tests` 1/1 + `Export.Xml.Tests` 4/4. **⚠️ Réserve signalée (hors périmètre 059) : `POST /generation` + `GET /fichiers/{type}` restent des stubs 501 — les modules `Declaration.Export.Excel/Xml` (TASK-010/011) existent et passent leurs tests mais ne sont câblés dans aucun endpoint ; les boutons Export Excel/XML de l'écran ⑥ resteront inertes (501) tant qu'une task de câblage back n'est pas créée. La clôture, elle, est pleinement fonctionnelle.** | 2026-07-12 |
