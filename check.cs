using System; using System.Data.SqlClient; class P { static void Main() { using var c = new SqlConnection(@"Server=.\sql2022;Database=GR_EMA_DISTRIBUTION;User Id=sa;Password=1234;TrustServerCertificate=True;"); c.Open(); using var cmd = c.CreateCommand(); cmd.CommandText = "SELECT TOP 1 MV_PointDate FROM RT_MOUVEMENT WHERE MV_Point = 1 AND MV_PointDate IS NOT NULL AND MV_Domaine IN (1,3) AND MV_DECAISSE = 1"; var d = cmd.ExecuteScalar(); Console.WriteLine(d); } }
