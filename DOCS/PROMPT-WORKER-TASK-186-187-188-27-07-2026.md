Tu es le **Développeur & Worker** de ce projet (rôle défini dans `D:\_vibe\ARCHITECTURE.md`).
Avant toute action, lis dans l'ordre : `D:\_vibe\ARCHITECTURE.md`, `D:\_vibe\GRF\TODO.md` (section
« 🐞 « Date Facture » erronée dans la Déclaration », en tête de fichier), puis **chaque fichier TASK
ci-dessous en entier** avant de coder quoi que ce soit dessus. Ne suppose jamais un contexte manquant.

## Contexte

Retour PO (27/07/2026), déclenché par un test comptable qui a rapporté « il manque les factures de
2025 » sur la Déclaration. Diagnostic architecte (lecture code + vérification base réelle
`GR_EMA_DISTRIBUTION`, lecture seule) : **la sélection elle-même est correcte** (règle « date de
rapprochement », TASK-062) — le vrai défaut est que la colonne « Date Facture » (grilles + export Excel,
feuille « Factures à déclarer ») affiche en réalité `RT_AFFECTATION.AF_Date` (date d'enregistrement de
l'affectation) au lieu de `RT_ECHEANCE.DO_Date` (date réelle de la facture). Vérifié sur le cas concret
`FC2501193` (`EC_Id=21466`) : `DO_Date` réel = 18/07/2025, valeur affichée dans l'export = 15/04/2026 =
`AF_Date` exacte de son affectation (`AF_Id=20522`). Confirmé sur 4 autres échantillons. Cause exacte :
`Declaration.Selection/SelectionExpliqueeService.cs`, les 3 requêtes du **chemin règlement-first**
(`GetSurensembleFournisseurSql`/`GetSurensembleDepenseSql`/`GetSurensembleClientSql`) aliasent
`A.AF_Date AS DateFacture` alors que `RT_ECHEANCE` (alias `E`) est déjà jointe et porte la vraie date.
Le chemin facture-first (TASK-050, `GetFactureFirstSql`) est déjà correct — **ne pas y toucher**.

La même session de test a fait remonter deux demandes complémentaires :
1. Ajouter la colonne `RT_ECHEANCE.DO_Reference` à l'export (existe déjà côté écran Factures, TASK-041,
   absente côté Déclaration).
2. Ajouter `RT_MOUVEMENT.MV_Piece`/`MV_Echeance` à la feuille « Règlements sélectionnés » de ce même
   export — `MV_Echeance` est déjà lu ailleurs dans le pipeline (`ReglementRapprochementRow.MvEcheance`,
   déjà utilisé par l'écran Rapprochement), juste à relier ; `MV_Piece` nécessite un `SELECT` neuf.

## Ta mission

Traiter dans l'ordre **TASK-186** → **TASK-187** → **TASK-188** (dossier `TASKS/`) :
- `TASK-186-correction-date-facture-erronee-affichage-declaration.md`
- `TASK-187-ajout-colonne-reference-facture-export-declaration.md`
- `TASK-188-ajout-colonnes-piece-echeance-feuille-reglements.md`

TASK-186 est la correction d'un bug avéré, prioritaire. TASK-187 et TASK-188 sont des extensions
mineures qui touchent des fichiers communs (`Exporter.cs`, `DeclarationWorkflowService.cs`) — enchaîne-
les seulement une fois TASK-186 vérifiée, dans l'ordre, chacune avec son propre VERIFY et son propre
commit.

Pour chaque TASK : vérifie d'abord que les champs Objectif/Périmètre/Livrables/Critères de validation
sont bien remplis (ils le sont). Déplace le fichier de `TASKS/` vers `IN_PROGRESS/`, implémente,
compile, corrige les erreurs, puis écris `VERIFY/TASK-186_verify.md` (puis `TASK-187_verify.md`, puis
`TASK-188_verify.md`) en suivant le même niveau de détail que `VERIFY/TASK-144_verify.md` (sers-t'en de
modèle) : section Périmètre livré, Fichiers modifiés, Checklist, **et une section « Reste à valider »
honnête si tout n'a pas pu être vérifié** — ne déclare jamais un point validé si tu ne l'as pas
réellement vérifié. Le PO attend un **VERIFY complet** sur les 3 — ne t'arrête pas à un simple diff,
rejoue chaque correction contre la base réelle (voir ci-dessous).

## Règles de travail (non négociables)

- **Portée exacte TASK-186** : uniquement les 3 alias SQL dans `SelectionExpliqueeService.cs`
  (`GetSurensembleFournisseurSql`/`GetSurensembleDepenseSql`/`GetSurensembleClientSql`) — remplacer
  `A.AF_Date AS DateFacture` par `E.DO_Date AS DateFacture`. Ne touche pas à `GetFactureFirstSql` (déjà
  correct). Vérifie s'il existe des tests/fixtures qui fixaient une valeur sur l'ancien comportement
  (`AF_Date`) par erreur — corrige-les pour refléter `DO_Date`.
- **Portée exacte TASK-187** : ajouter `E.DO_Reference AS Reference` aux **4** requêtes de
  `SelectionExpliqueeService.cs` (les 3 du règlement-first **et** `GetFactureFirstSql`), puis propager
  un nouveau champ `Reference` (string, nullable) à travers `AffectationCandidateRow`
  (`SelectionExpliqueeModels.cs`), `AffectationCandidate`/`AffectationADeclarer` et
  `LigneDeclarationEnrichie` (`Declaration.Core/Model.cs`), `SelectionExpliqueeEvaluator`,
  `ConstructeurDeclaration.cs`, jusqu'à une nouvelle colonne « Référence » dans `Exporter.cs`
  (`CreerFeuilleFacturesControle`, feuille « Factures à déclarer », positionnée après « N° Facture »
  sauf si tu identifies une meilleure cohérence avec l'existant — documente ton choix dans le VERIFY).
  Ne touche pas à l'export XML Simpl-TVA (le schéma DGI ne prévoit pas ce champ).
- **Portée exacte TASK-188** : ajouter `M.MV_Piece AS MvPiece` au `SELECT` de
  `GetReglementsRapprochementAsync` (`DeclarationRepository.cs`) + champ `MvPiece` sur
  `ReglementRapprochementRow` ; relier le `MvEcheance` déjà existant sur ce même type ; propager les
  deux jusqu'à `ReglementSelectionneInfo` (nouveaux champs `Piece`/`Echeance`, `Declaration.Core/Model.cs`)
  via `DeclarationWorkflowService.ConstruireModeleControleAsync` (lignes ~1337-1348) ; 2 nouvelles
  colonnes « N° Pièce »/« Échéance » dans `CreerFeuilleReglementsSelectionnes` (`Exporter.cs`), format
  date sur « Échéance » identique aux autres colonnes date de cette feuille (TASK-180). Échantillon
  réel déjà vérifié par l'architecte : `MV_Piece` ne semble pas porter de sentinel vide à neutraliser
  (contrairement aux colonnes date) — à reconfirmer sur un échantillon plus large, documenter dans le
  VERIFY si un cas contraire est trouvé.
- Ne touche à aucun fichier hors de ces portées. Pas de refactor, pas de nettoyage non demandé, pas
  d'anticipation d'une TASK suivante dans une TASK antérieure.
- **Tu as un accès réel à la base de données** : `sqlcmd -S DESKTOP-5BFKKEP -U sa -P 1234 -C -d
  GR_EMA_DISTRIBUTION -Q "..."` (identifiants complets dans `D:\_vibe\GRF\connections.json`). Utilise-le
  systématiquement pour rejouer chaque correction contre un cas réel, pas seulement une fixture :
  - TASK-186 : cas `FC2501193` (`EC_Id=21466`) — le VERIFY doit montrer explicitement que la date
    produite passe de 15/04/2026 (`AF_Date`) à 18/07/2025 (`DO_Date`) sur ce cas précis.
  - TASK-187 : export réel (lecture seule, déclaration `EnCours` rejouable) montrant la colonne
    Référence peuplée sur un cas où `DO_Reference` est renseignée et un cas où elle est vide/NULL.
  - TASK-188 : export réel montrant « N° Pièce »/« Échéance » comparés directement aux valeurs SQL
    brutes de `RT_MOUVEMENT` pour le même `MV_Id`.
- Build back (`dotnet build DeclarationTVA.slnx`) ET tests (`Declaration.Core.Tests`,
  `Declaration.Orchestration.Tests`, `Declaration.Export.Excel.Tests`, `Declaration.Selection.Tests`)
  doivent passer avant d'écrire chaque VERIFY.
- **Un commit par TASK**, jamais un commit qui mélange plusieurs TASKs, message clair, jamais
  `--no-verify`, **ne jamais pousser (`git push`)** — les commits restent locaux pour revue architecte.
- Respecte les règles universelles de `ARCHITECTURE.md` §5 (pas de SQL inline hors couche repository,
  pas de secret codé en dur, pas de bypass sécurité, pas de dette technique silencieuse — documente tout
  compromis dans le VERIFY).
- Si un point est réellement ambigu (positionnement exact d'une colonne, sentinel inattendu sur
  `MV_Piece`, ou un cas de source de données qui ne rentre proprement dans aucun des chemins
  existants) : **arrête-toi sur ce point précis, documente-le clairement dans le VERIFY** plutôt que
  d'inventer une réponse, et continue avec les TASKs suivantes non bloquées.
- **Point ouvert à ne pas trancher toi-même** (documenté dans TASK-186, § Risques) : les déclarations
  déjà clôturées/déposées avec la mauvaise date ne sont **pas** à retraiter rétroactivement dans le
  cadre de cette TASK — signale juste dans le VERIFY si tu identifies des cas réels concernés, sans
  les corriger.

## À la fin

Laisse TASK-186/187/188 dans `VERIFY/` (ne les déplace pas toi-même vers `DONE_DETAIL/` — c'est le rôle
de l'architecte de les reviewer et de faire la clôture). Termine par un résumé listant, pour chacune des
3 TASKs : ✅ VERIFY prêt pour revue / ⏸️ bloquée (et pourquoi) / non commencée (et pourquoi).
